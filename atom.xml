<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://lindamao0753.github.io</id>
    <title>Lindamao</title>
    <updated>2021-03-20T07:20:29.852Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://lindamao0753.github.io"/>
    <link rel="self" href="https://lindamao0753.github.io/atom.xml"/>
    <subtitle>好好学习</subtitle>
    <logo>https://lindamao0753.github.io/images/avatar.png</logo>
    <icon>https://lindamao0753.github.io/favicon.ico</icon>
    <rights>All rights reserved 2021, Lindamao</rights>
    <entry>
        <title type="html"><![CDATA[Springboot]]></title>
        <id>https://lindamao0753.github.io/post/springboot/</id>
        <link href="https://lindamao0753.github.io/post/springboot/">
        </link>
        <updated>2021-03-18T04:00:31.000Z</updated>
        <content type="html"><![CDATA[<h1 id="springboot01helloworld">SpringBoot01Hello,World！</h1>
<h2 id="学习路线">学习路线</h2>
<figure data-type="image" tabindex="1"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219141907374.png" alt="image-20210219141907374" loading="lazy"></figure>
<p>SpringBoot简介</p>
<h2 id="回顾什么是spring">回顾什么是Spring</h2>
<p>Spring是一个开源框架，2003 年兴起的一个轻量级的Java 开发框架，作者：Rod Johnson  。</p>
<p><strong>Spring是为了解决企业级应用开发的复杂性而创建的，简化开发。</strong></p>
<h2 id="spring是如何简化java开发的">Spring是如何简化Java开发的</h2>
<p>为了降低Java开发的复杂性，Spring采用了以下4种关键策略：</p>
<p>1、<strong>基于POJO的轻量级和最小侵入性编程，所有东西都是bea</strong>n；</p>
<p>2、<strong>通过IOC，依赖注入（DI）和面向接口实现松耦合</strong>；</p>
<p>3、<strong>基于切面（AOP）和惯例进行声明式编程</strong>；</p>
<p>4、<strong>通过切面和模版减少样式代码</strong>，RedisTemplate，xxxTemplate；</p>
<h2 id="什么是springboot">什么是SpringBoot</h2>
<p>学过javaweb的同学就知道，开发一个web应用，从最初开始接触Servlet结合Tomcat, 跑出一个Hello Wolrld程序，是要经历特别多的步骤；后来就用了框架Struts，再后来是SpringMVC，到了现在的SpringBoot，过一两年又会有其他web框架出现；你们有经历过框架不断的演进，然后自己开发项目所有的技术也在不断的变化、改造吗？建议都可以去经历一遍；</p>
<p>言归正传，什么是SpringBoot呢，就是一个javaweb的开发框架，和SpringMVC类似，对比其他javaweb框架的好处，官方说是简化开发，约定大于配置，  you can &quot;just run&quot;，能迅速的开发web应用，几行代码开发一个http接口。</p>
<p>所有的技术框架的发展似乎都遵循了一条主线规律：从一个复杂应用场景 衍生 一种规范框架，人们只需要进行各种配置而不需要自己去实现它，这时候强大的配置功能成了优点；发展到一定程度之后，人们根据实际生产应用情况，选取其中实用功能和设计精华，重构出一些轻量级的框架；之后为了提高开发效率，嫌弃原先的各类配置过于麻烦，于是开始提倡“约定大于配置”，进而衍生出一些一站式的解决方案。</p>
<p>是的这就是Java企业级应用-&gt;J2EE-&gt;spring-&gt;springboot的过程。</p>
<p>随着 Spring 不断的发展，涉及的领域越来越多，项目整合开发需要配合各种各样的文件，慢慢变得不那么易用简单，违背了最初的理念，甚至人称配置地狱。Spring Boot 正是在这样的一个背景下被抽象出来的开发框架，目的为了让大家更容易的使用 Spring 、更容易的集成各种常用的中间件、开源软件；</p>
<p>Spring Boot 基于 Spring 开发，Spirng Boot 本身并不提供 Spring 框架的核心特性以及扩展功能，只是用于快速、敏捷地开发新一代基于 Spring 框架的应用程序。也就是说，它并不是用来替代 Spring 的解决方案，而是和 Spring 框架紧密结合用于提升 Spring 开发者体验的工具。Spring Boot 以<strong>约定大于配置的核心思想</strong>，默认帮我们进行了很多设置，多数 Spring Boot 应用只需要很少的 Spring 配置。同时它集成了大量常用的第三方库配置（例如 Redis、MongoDB、Jpa、RabbitMQ、Quartz 等等），Spring Boot 应用中这些第三方库几乎可以零配置的开箱即用。</p>
<p>简单来说就是SpringBoot其实不是什么新的框架，它默认配置了很多框架的使用方式，就像maven整合了所有的jar包，spring boot整合了所有的框架 。</p>
<p>Spring Boot 出生名门，从一开始就站在一个比较高的起点，又经过这几年的发展，生态足够完善，Spring Boot 已经当之无愧成为 Java 领域最热门的技术。</p>
<p><strong>Spring Boot的主要优点：</strong></p>
<ul>
<li><strong>为所有Spring开发者更快的入门</strong></li>
<li><strong>开箱即用</strong>，提供各种默认配置来简化项目配置</li>
<li><strong>内嵌式容器简化Web项目</strong></li>
<li><strong>没有冗余代码生成和XML配置的要求</strong></li>
</ul>
<p>真的很爽，我们快速去体验开发个接口的感觉吧！</p>
<p>Hello，World</p>
<h2 id="准备工作">准备工作</h2>
<p>我们将学习如何快速的创建一个Spring Boot应用，并且实现一个简单的Http请求处理。通过这个例子对Spring Boot有一个初步的了解，并体验其结构简单、开发快速的特性。</p>
<p>我的环境准备：</p>
<ul>
<li>java version &quot;1.8.0_181&quot;</li>
<li>Maven-3.6.1</li>
<li>SpringBoot 2.x 最新版</li>
</ul>
<p>开发工具：</p>
<ul>
<li>IDEA</li>
</ul>
<h2 id="创建基础项目说明">创建基础项目说明</h2>
<p>Spring官方提供了非常方便的工具让我们快速构建应用</p>
<p>Spring Initializr：https://start.spring.io/</p>
<p>**项目创建方式一：**使用Spring Initializr 的 Web页面创建项目</p>
<p>1、打开  https://start.spring.io/</p>
<p>2、填写项目信息</p>
<p>3、点击”Generate Project“按钮生成项目；下载此项目</p>
<p>4、解压项目包，并用IDEA以Maven项目导入，一路下一步即可，直到项目导入完毕。</p>
<p>5、如果是第一次使用，可能速度会比较慢，包比较多、需要耐心等待一切就绪。</p>
<p>**项目创建方式二：**使用 IDEA 直接创建项目</p>
<p>1、创建一个新项目</p>
<p>2、选择spring initalizr ， 可以看到默认就是去官网的快速构建工具那里实现</p>
<p>3、填写项目信息</p>
<p>4、选择初始化的组件（初学勾选 Web 即可）</p>
<p>5、填写项目路径</p>
<p>6、等待项目构建成功</p>
<figure data-type="image" tabindex="2"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219161811854.png" alt="image-20210219161811854" loading="lazy"></figure>
<p><strong>项目结构分析：</strong></p>
<p>通过上面步骤完成了基础项目的创建。就会自动生成以下文件。</p>
<p>1、程序的主启动类</p>
<p>2、一个 application.properties 配置文件</p>
<p>3、一个 测试类</p>
<p>4、一个 pom.xml</p>
<h2 id="pomxml-分析">pom.xml 分析</h2>
<p>打开pom.xml，看看Spring Boot项目的依赖：</p>
<pre><code class="language-xml">&lt;!-- 父依赖 --&gt;&lt;parent&gt;    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;    &lt;artifactId&gt;spring-boot-starter-parent&lt;/artifactId&gt;    &lt;version&gt;2.2.5.RELEASE&lt;/version&gt;    
    &lt;relativePath/&gt;
&lt;/parent&gt;
&lt;dependencies&gt; 
    &lt;!-- web场景启动器 --&gt; 
    &lt;dependency&gt;        &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;        &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;    &lt;/dependency&gt; 
    &lt;!-- springboot单元测试 --&gt;  
    &lt;dependency&gt;        &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;        &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;        &lt;scope&gt;test&lt;/scope&gt;  
        &lt;!-- 剔除依赖 --&gt;     
        &lt;exclusions&gt;     
            &lt;exclusion&gt;                &lt;groupId&gt;org.junit.vintage&lt;/groupId&gt;                &lt;artifactId&gt;junit-vintage-engine&lt;/artifactId&gt;    
           &lt;/exclusion&gt;       
        &lt;/exclusions&gt; 
    &lt;/dependency&gt;
&lt;/dependencies&gt;
&lt;build&gt;  
    &lt;plugins&gt;      
        &lt;!-- 打包插件 --&gt;     
        &lt;plugin&gt;            &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;            &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt;        &lt;/plugin&gt;  
    &lt;/plugins&gt;
&lt;/build&gt;
</code></pre>
<h2 id="编写一个http接口">编写一个http接口</h2>
<p>1、在主程序的同级目录下，新建一个controller包，一定要在同级目录下，否则识别不到</p>
<p>2、在包中新建一个HelloController类</p>
<pre><code class="language-java">package com.lin.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping(&quot;/hello&quot;)
public class HelloController {
    @RequestMapping(&quot;/first&quot;)
    public String first(){
        return &quot;hello&quot;;
    }

}

</code></pre>
<p>3、编写完毕后，从主程序启动项目，浏览器发起请求，看页面返回；控制台输出了 Tomcat 访问的端口号！</p>
<figure data-type="image" tabindex="3"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219160114249.png" alt="image-20210219160114249" loading="lazy"></figure>
<figure data-type="image" tabindex="4"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>输入地址测试</p>
<figure data-type="image" tabindex="5"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219160156535.png" alt="image-20210219160156535" loading="lazy"></figure>
<p>简单几步，就完成了一个web接口的开发，SpringBoot就是这么简单。所以我们常用它来建立我们的微服务项目！</p>
<h2 id="将项目打成jar包点击-maven的-package">将项目打成jar包，点击 maven的 package</h2>
<figure data-type="image" tabindex="6"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>如果遇到以上错误，可以配置打包时 跳过项目运行测试用例</p>
<pre><code class="language-xml">&lt;!--    在工作中,很多情况下我们打包是不想执行测试用例的    可能是测试用例不完事,或是测试用例会影响数据库数据    跳过测试用例执    --&gt;
&lt;plugin&gt;    
    &lt;groupId&gt;org.apache.maven.plugins&lt;/groupId&gt;    
    &lt;artifactId&gt;maven-surefire-plugin&lt;/artifactId&gt;    
    &lt;configuration&gt;        
        &lt;!--跳过项目运行测试用例--&gt;        
        &lt;skipTests&gt;true&lt;/skipTests&gt;    
    &lt;/configuration&gt;
&lt;/plugin&gt;
</code></pre>
<p>如果打包成功，则会在target目录下生成一个 jar 包</p>
<figure data-type="image" tabindex="7"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219160754063.png" alt="image-20210219160754063" loading="lazy"></figure>
<p>打包完成之后在target当中去找到该jar包</p>
<figure data-type="image" tabindex="8"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219160853133.png" alt="image-20210219160853133" loading="lazy"></figure>
<p>在文件夹中通过shell命令打开</p>
<h2 id="改动banner图案">改动banner图案</h2>
<p>如何更改启动时显示的字符拼成的字母，SpringBoot呢？也就是 banner 图案；</p>
<p>只需一步：到项目下的 resources 目录下新建一个banner.txt 即可。</p>
<p>图案可以到：https://www.bootschool.net/ascii 这个网站生成，然后拷贝到文件中即可！</p>
<figure data-type="image" tabindex="9"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>SpringBoot这么简单的东西背后一定有故事，我们之后去进行一波源码分析！</strong></p>
<h2 id="注意点">注意点</h2>
<p>包的建立要建在和springboot启动程序的同一级下面 不然会报错</p>
<figure data-type="image" tabindex="10"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210219162005090.png" alt="image-20210219162005090" loading="lazy"></figure>
<h1 id="springboot02运行原理初探">SpringBoot02：运行原理初探</h1>
<p>运行原理探究</p>
<p>我们之前写的HelloSpringBoot，到底是怎么运行的呢，Maven项目，我们一般从pom.xml文件探究起；</p>
<p><strong>pom.xml</strong></p>
<figure data-type="image" tabindex="11"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220144457771.png" alt="image-20210220144457771" loading="lazy"></figure>
<p>spring-boot-dependencies ：核心依赖在父工程中</p>
<h2 id="父依赖">父依赖</h2>
<p>其中它主要是依赖一个父项目，主要是管理项目的资源过滤及插件！</p>
<figure data-type="image" tabindex="12"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220151415875.png" alt="image-20210220151415875" loading="lazy"></figure>
<pre><code class="language-xml">   &lt;parent&gt;
        &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
        &lt;artifactId&gt;spring-boot-starter-parent&lt;/artifactId&gt;
        &lt;version&gt;2.3.4.RELEASE&lt;/version&gt;
        &lt;relativePath/&gt; &lt;!-- lookup parent from repository --&gt;
    &lt;/parent&gt;
</code></pre>
<p>点进去，发现还有一个父依赖</p>
<figure data-type="image" tabindex="13"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220151447511.png" alt="image-20210220151447511" loading="lazy"></figure>
<pre><code class="language-xml">  &lt;parent&gt;
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;
    &lt;artifactId&gt;spring-boot-dependencies&lt;/artifactId&gt;
    &lt;version&gt;2.3.4.RELEASE&lt;/version&gt;
  &lt;/parent&gt;
</code></pre>
<p><strong>这里才是真正管理SpringBoot应用里面所有依赖版本的地方，SpringBoot的版本控制中心；</strong></p>
<p><strong>以后我们导入依赖默认是不需要写版本；但是如果导入的包没有在依赖中管理着就需要手动配置版本了；</strong></p>
<h2 id="启动器-spring-boot-starter">启动器 spring-boot-starter</h2>
<pre><code class="language-xml">&lt;dependency&gt;    
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p><strong>springboot-boot-starter-xxx</strong>：<strong>就是spring-boot的场景启动器</strong></p>
<p><strong>spring-boot-starter-web</strong>：帮我们导入了web模块正常运行所依赖的组件；</p>
<p>SpringBoot将所有的功能场景都<strong>抽取</strong>出来，做成一个个的<strong>starter</strong> （启动器），只需要在项目中引入这些starter即可，所有相关的依赖都会导入进来 ， 我们要用什么功能就导入什么样的场景启动器即可 ；</p>
<p>我们未来也可以自己自定义 starter；</p>
<p>在官网当中也可以查询到相关的</p>
<p><strong>主启动类</strong></p>
<p>分析完了 pom.xml 来看看这个启动类</p>
<h2 id="默认的主启动类">默认的主启动类</h2>
<pre><code class="language-java">//@SpringBootApplication 来标注一个主程序类
//说明这是一个Spring Boot应用：自动类下面的所有资源被导入
@SpringBootApplication
public class SpringbootApplication {
   public static void main(String[] args) {    
       //以为是启动了一个方法，没想到启动了一个服务
       SpringApplication.run(SpringbootApplication.class, args);   
   }
}
</code></pre>
<h3 id="springbootapplication">@SpringBootApplication</h3>
<p>作用：标注在某个类上说明这个类是SpringBoot的主配置类 ， SpringBoot就应该运行这个类的main方法来启动SpringBoot应用</p>
<p>没有这个注解就启动不了程序</p>
<p>但是**一个简单的启动类并不简单！**点击进去查看细致的内容</p>
<p>进入这个注解：可以看到上面还有很多其他注解！</p>
<figure data-type="image" tabindex="14"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220150813874.png" alt="image-20210220150813874" loading="lazy"></figure>
<h4 id="componentscan">@ComponentScan</h4>
<p>这个注解在Spring中很重要 ,它对应XML配置中的元素。</p>
<p><strong>作用</strong>：自动扫描并加载符合条件的组件或者bean ， 将这个bean定义加载到IOC容器中</p>
<h4 id="springbootconfiguration">@SpringBootConfiguration</h4>
<p>作用：SpringBoot的配置类 ，标注在某个类上 ， 表示这是一个SpringBoot的配置类；</p>
<p>我们继续进去这个注解查看</p>
<figure data-type="image" tabindex="15"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220151035128.png" alt="image-20210220151035128" loading="lazy"></figure>
<ul>
<li><strong>@Configuration，说明这是一个配置类 ，配置类就是对应Spring的xml 配置文件</strong>；</li>
<li><strong>@Component</strong> 这就说明，<strong>启动类本身也是Spring中的一个组件而已，负责启动应用</strong>！</li>
</ul>
<p>我们回到 SpringBootApplication 注解中继续看。</p>
<h4 id="enableautoconfiguration">@EnableAutoConfiguration</h4>
<p><strong>@EnableAutoConfiguration ：开启自动配置功能</strong></p>
<p>以前我们需要自己配置的东西，而现在SpringBoot可以自动帮我们配置 ；@EnableAutoConfiguration告诉SpringBoot开启自动配置功能，这样自动配置才能生效；</p>
<p>点进注解接续查看：</p>
<figure data-type="image" tabindex="16"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220151207760.png" alt="image-20210220151207760" loading="lazy"></figure>
<p><strong>@AutoConfigurationPackage ：自动配置包</strong></p>
<p><strong>@import</strong> ：<strong>Spring底层注解@import ， 给容器中导入一个组件</strong></p>
<p><strong>Registrar.class 作用：将主启动类的所在包及包下面所有子包里面的所有组件扫描到Spring容器</strong> ；<strong>自动配置包注册</strong></p>
<p>这个分析完了，退到上一步，继续看</p>
<p><strong>@Import({AutoConfigurationImportSelector.class}) ：给容器导入组件 ；</strong> <strong>自动配置导入选择</strong></p>
<p><strong>AutoConfigurationImportSelecto</strong>r ：<strong>自动配置导入选择器</strong>，那么它会导入哪些组件的选择器呢？我们点击去这个类看源码：</p>
<p>1、这个类中有一个这样的方法</p>
<p>获取所有的配置</p>
<pre><code class="language-java">List&lt;String&gt; configurations = this.getCandidateConfigurations(annotationMetadata, attributes);
</code></pre>
<p>2.获取候选配置</p>
<pre><code class="language-java">  protected AnnotationAttributes getAttributes(AnnotationMetadata metadata) {
        String name = this.getAnnotationClass().getName();
        AnnotationAttributes attributes = AnnotationAttributes.fromMap(metadata.getAnnotationAttributes(name, true));
        Assert.notNull(attributes, () -&gt; {
            return &quot;No auto-configuration attributes found. Is &quot; + metadata.getClassName() + &quot; annotated with &quot; + ClassUtils.getShortName(name) + &quot;?&quot;;
        });
        return attributes;
    }
</code></pre>
<p>3.META-INF/spring.factories里有一个核心配置文件</p>
<figure data-type="image" tabindex="17"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220153101958.png" alt="image-20210220153101958" loading="lazy"></figure>
<p>所有的资源记载到配置类中！</p>
<pre><code class="language-java">Properties properties = PropertiesLoaderUtils.loadProperties(resource);
</code></pre>
<p><strong>SpringFactoriesLoader</strong></p>
<figure data-type="image" tabindex="18"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220153930205.png" alt="image-20210220153930205" loading="lazy"></figure>
<p>4.这个方法又调用了  SpringFactoriesLoader 类的静态方法！我们进入SpringFactoriesLoader类loadFactoryNames() 方法</p>
<pre><code class="language-java">public static List&lt;String&gt; loadFactoryNames(Class&lt;?&gt; factoryClass, @Nullable ClassLoader classLoader) {    String factoryClassName = factoryClass.getName();    //这里它又调用了 loadSpringFactories 方法    
                                                                                                      return (List)loadSpringFactories(classLoader).getOrDefault(factoryClassName, Collections.emptyList());}
</code></pre>
<p>3、我们继续点击查看 loadSpringFactories 方法</p>
<pre><code class="language-java">private static Map&lt;String, List&lt;String&gt;&gt; loadSpringFactories(@Nullable ClassLoader classLoader) {    //获得classLoader ， 我们返回可以看到这里得到的就是EnableAutoConfiguration标注的类本身    MultiValueMap&lt;String, String&gt; result = (MultiValueMap)cache.get(classLoader);    if (result != null) {        return result;    } else {        try {            //去获取一个资源 &quot;META-INF/spring.factories&quot;            Enumeration&lt;URL&gt; urls = classLoader != null ? classLoader.getResources(&quot;META-INF/spring.factories&quot;) : ClassLoader.getSystemResources(&quot;META-INF/spring.factories&quot;);            LinkedMultiValueMap result = new LinkedMultiValueMap();
            //将读取到的资源遍历，封装成为一个Properties            while(urls.hasMoreElements()) {                URL url = (URL)urls.nextElement();                UrlResource resource = new UrlResource(url);                Properties properties = PropertiesLoaderUtils.loadProperties(resource);                Iterator var6 = properties.entrySet().iterator();
                while(var6.hasNext()) {                    Entry&lt;?, ?&gt; entry = (Entry)var6.next();                    String factoryClassName = ((String)entry.getKey()).trim();                    String[] var9 = StringUtils.commaDelimitedListToStringArray((String)entry.getValue());                    int var10 = var9.length;
                    for(int var11 = 0; var11 &lt; var10; ++var11) {                        String factoryName = var9[var11];                        result.add(factoryClassName, factoryName.trim());                    }                }            }
            cache.put(classLoader, result);            return result;        } catch (IOException var13) {            throw new IllegalArgumentException(&quot;Unable to load factories from location [META-INF/spring.factories]&quot;, var13);        }    }}
</code></pre>
<p>4、发现一个多次出现的文件：spring.factories，全局搜索它</p>
<h4 id="springfactories">spring.factories</h4>
<p>我们根据源头打开spring.factories ， 看到了很多自动配置的文件；这就是自动配置根源所在！</p>
<p>核心配置注解在里面</p>
<pre><code class="language-java">@ConfigurationXXX
</code></pre>
<p><strong>WebMvcAutoConfiguration</strong></p>
<p>我们在上面的自动配置类随便找一个打开看看，比如 ：WebMvcAutoConfiguration</p>
<figure data-type="image" tabindex="19"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>可以看到这些一个个的都是JavaConfig配置类，而且都注入了一些Bean，可以找一些自己认识的类，看着熟悉一下！</p>
<p>所以，自动配置真正实现是从classpath中搜寻所有的META-INF/spring.factories配置文件 ，并将其中对应的 org.springframework.boot.autoconfigure. 包下的配置项，通过反射实例化为对应标注了 @Configuration的JavaConfig形式的IOC容器配置类 ， 然后将这些都汇总成为一个实例并加载到IOC容器中。</p>
<p><strong>结论：</strong></p>
<ol>
<li>SpringBoot在启动的时候从类路径下的META-INF/spring.factories中获取EnableAutoConfiguration指定的值</li>
<li>将这些值作为自动配置类导入容器 ， 自动配置类就生效 ， 帮我们进行自动配置工作；</li>
<li>整个J2EE的整体解决方案和自动配置都在springboot-autoconfigure的jar包中；</li>
<li>它会给容器中导入非常多的自动配置类 （xxxAutoConfiguration）, 就是给容器中导入这个场景需要的所有组件 ， 并配置好这些组件 ；</li>
<li>有了自动配置类 ， 免去了我们手动编写配置注入功能组件等的工作；</li>
</ol>
<p><strong>现在大家应该大概的了解了下，SpringBoot的运行原理，后面我们还会深化一次！</strong></p>
<p><strong>SpringApplication</strong></p>
<figure data-type="image" tabindex="20"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210220155509096.png" alt="image-20210220155509096" loading="lazy"></figure>
<h2 id="不简单的方法">不简单的方法</h2>
<p>我最初以为就是运行了一个main方法，没想到却开启了一个服务；</p>
<pre><code class="language-java">@SpringBootApplicationpublic class SpringbootApplication {    public static void main(String[] args) {        SpringApplication.run(SpringbootApplication.class, args);    }}
</code></pre>
<p><strong>SpringApplication.run分析</strong></p>
<p>分析该方法主要分两部分，一部分是SpringApplication的实例化，二是run方法的执行；</p>
<h2 id="结论">结论</h2>
<p>springboot所有的自动配置都是在启动的时候扫描并加载“spring.factories所有的自动装配类都在这里面，但是不一定生效，要帕努单条件是否成立，只要导入了对应的start，就有对应的启动器了，有了启动器，我们自动装配就会生效，然后配置成功。</p>
<ol>
<li>springboot在启动的时候，从类路径下/MEA-TA/sprin.factories</li>
<li>将这些自动配置的类导入容器，自动配置就会生效，版我们进行自动装配！</li>
<li>以前我们需要自动装配的东西，现在springboot帮我们做了！</li>
<li>整合javaEE，解决方案和自动装配的东西都在spring-boot-autoconfigure-2.3.4.RELEASE.jar这个包下</li>
<li>它会把所有需要导入的组件，以类名的方式返回，这些组件就会被添加到容器中；</li>
<li>容器中也会存在非常多的xxxAutoConfiguration的文件（@Bean）,就是这些类给容器中导入了这个场景需要的所有组件；并自动配置，@Configuration，javaConfig！</li>
<li>有了自动配置类，免去了我们手动编写配置的麻烦。</li>
</ol>
<h2 id="springapplication">SpringApplication</h2>
<p><strong>这个类主要做了以下四件事情：</strong></p>
<p>1、<strong>推断应用的类型是普通的项目还是Web项目</strong></p>
<p>2、<strong>查找并加载</strong>所有<strong>可用初始化器</strong> ， 设置到<strong>initializers</strong>属性中</p>
<p>3、找出所有的<strong>应用程序监听器</strong>，设置到<strong>listeners</strong>属性中</p>
<p>4、<strong>推断</strong>并<strong>设置</strong>main方法的定义类，<strong>找到运行的主类</strong></p>
<p>查看构造器：</p>
<pre><code class="language-java">public SpringApplication(ResourceLoader resourceLoader, Class... primarySources) {    // ......    this.webApplicationType = WebApplicationType.deduceFromClasspath();    this.setInitializers(this.getSpringFactoriesInstances();    this.setListeners(this.getSpringFactoriesInstances(ApplicationListener.class));    this.mainApplicationClass = this.deduceMainApplicationClass();}
</code></pre>
<h2 id="run方法流程分析">run方法流程分析</h2>
<p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7L1vFQMnaRIJSmeZ58T2eZicjafiawQLp9u8wc4ic1Mjy6OyfibzfjVofeL5pnS1NSFKVjlIg6neI9ySg/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="img" loading="lazy"></p>
<p>跟着源码和这幅图就可以一探究竟了！</p>
<p><strong>关于springboot个，谈谈关于你的理解</strong></p>
<ul>
<li>自动装配</li>
<li>run()</li>
</ul>
<p>全面接管springMVC的配置！ 实操！</p>
<h1 id="springboot03yaml配置注入">SpringBoot03：yaml配置注入</h1>
<p>yaml语法学习</p>
<h2 id="配置文件">配置文件</h2>
<p>SpringBoot使用一个全局的配置文件 ， 配置文件名称是固定的</p>
<ul>
<li>
<p>application.properties</p>
</li>
<li>
<ul>
<li>语法结构 ：key=value</li>
</ul>
</li>
<li>
<p>application.yml</p>
</li>
<li>
<ul>
<li>语法结构 ：key：空格 value</li>
</ul>
</li>
</ul>
<p>**配置文件的作用 ：**修改SpringBoot自动配置的默认值，因为SpringBoot在底层都给我们自动配置好了；</p>
<p>比如我们可以在配置文件中修改Tomcat 默认启动的端口号！测试一下！</p>
<pre><code class="language-properties">server.port=8081
</code></pre>
<h2 id="yaml概述">yaml概述</h2>
<p>YAML是 &quot;YAML Ain't a Markup Language&quot; （YAML不是一种标记语言）的递归缩写。在开发的这种语言时，YAML 的意思其实是：&quot;Yet Another Markup Language&quot;（仍是一种标记语言）</p>
<p><strong>这种语言以数据<strong><strong>作</strong></strong>为中心，而不是以标记语言为重点！</strong></p>
<p>以前的配置文件，大多数都是使用xml来配置；比如一个简单的端口配置，我们来对比下yaml和xml</p>
<p>传统xml配置：</p>
<pre><code class="language-XML">&lt;server&gt;  
    &lt;port&gt;8081&lt;port&gt;
        &lt;/server&gt;
</code></pre>
<p>yaml配置：</p>
<pre><code class="language-yaml">server： 
prot: 8080
</code></pre>
<h2 id="yaml基础语法">yaml基础语法</h2>
<p>说明：语法要求严格！</p>
<p>1、<strong>空格不能省略</strong></p>
<p>2、以<strong>缩进</strong>来控制<strong>层级关系</strong>，只要是左边对齐的一列数据都是同一个层级的。</p>
<p>3、<strong>属性</strong>和<strong>值</strong>的大小写都是十分敏感的。</p>
<p><strong>字面量：普通的值  [ 数字，布尔值，字符串  ]</strong></p>
<p>字面量直接写在后面就可以 ， 字符串<strong>默认不用加上双引号或者单引号</strong>；</p>
<ul>
<li></li>
</ul>
<pre><code>k: v
</code></pre>
<p>注意：</p>
<ul>
<li>
<p>“ ” 双引号，不会转义字符串里面的特殊字符 ， <strong>特殊字符会作为本身想表示的意思</strong>；</p>
<p>比如 ：name: &quot;kuang \n shen&quot;  输出 ：kuang  换行  shen</p>
</li>
<li>
<p>'' 单引号，<strong>会转义特殊字符 ， 特殊字符最终会变成和普通字符一样输出</strong></p>
<p>比如 ：name: ‘kuang \n shen’  输出 ：kuang  \n  shen</p>
</li>
</ul>
<p><strong>对象、Map（键值对）</strong></p>
<pre><code class="language-yaml">#对象、Map格式k:     v1:    v2:
</code></pre>
<p>在下一行来写对象的属性和值得关系，注意缩进；比如：</p>
<pre><code class="language-yaml">student:    name: qinjiang    age: 3
</code></pre>
<p>行内写法</p>
<pre><code class="language-yaml">student: {name: qinjiang,age: 3}
</code></pre>
<p><strong>数组（ List、set ）</strong></p>
<p>用 - 值表示数组中的一个元素,比如：</p>
<pre><code class="language-yaml">pets: - cat - dog - pig
</code></pre>
<p>行内写法</p>
<pre><code class="language-yaml">pets: [cat,dog,pig]
</code></pre>
<p><strong>修改SpringBoot的默认端口号</strong></p>
<p>配置文件中添加，端口号的参数，就可以切换端口；</p>
<pre><code class="language-yaml">server:  port: 8082
</code></pre>
<p><strong>注入配置文件</strong></p>
<p>yaml文件更强大的地方在于，<strong>他可以给我们的实体类直接注入匹配值</strong>！</p>
<h2 id="yaml注入配置文件">yaml注入配置文件</h2>
<p>1、在springboot项目中的resources目录下新建一个文件 application.yml</p>
<p>2、编写一个实体类 Dog；</p>
<pre><code class="language-java">package com.kuang.springboot.pojo;
@Component  
//注册bean到容器中
public class Dog {    
    private String name;    
    private Integer age;        
    //有参无参构造、get、set方法、toString()方法  
                }
</code></pre>
<p>3、思考，我们原来是如何给bean注入属性值的！@Value，给狗狗类测试一下：</p>
<p>通过value 注解的方式</p>
<pre><code class="language-java">@Component //注册bean
public class Dog {    
    @Value(&quot;旺财&quot;)
    private String name;
    @Value(&quot;3&quot;)
    private Integer age;
                 }
</code></pre>
<p>4、在SpringBoot的测试类下注入狗狗输出一下；</p>
<pre><code class="language-java">@SpringBootTestclass DemoApplicationTests {
    @Autowired //将狗狗自动注入进来    
    Dog dog;
    @Test    
    public void contextLoads() {        System.out.println(dog); //打印看下狗狗对象    }
}
</code></pre>
<figure data-type="image" tabindex="21"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221172653642.png" alt="image-20210221172653642" loading="lazy"></figure>
<p>结果成功输出，@Value注入成功，这是我们原来的办法对吧。</p>
<figure data-type="image" tabindex="22"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>5、我们在编写一个复杂一点的实体类：Person 类</p>
<pre><code class="language-java">@Component //注册bean到容器中
public class Person {   
    private String name;
    private Integer age;
    private boolean happy;
    private Date birth;
    private Map&lt;String,Object&gt; maps;
    private List&lt;Object&gt;list;
    private Dog dog;       //有参无参构造、get、set方法、toString()方法  }
</code></pre>
<p>6、我们来使用yaml配置的方式进行注入，大家写的时候注意区别和优势，我们编写一个yaml配置！</p>
<pre><code class="language-yaml">person:
  name: lindamao
  age: 3
  happy: false
  birth: 2018/01/01
  maps: {k1: v1,k2: v2}
  list:
    -code
    -music
    -girl
  dog:
    name: 旺财
    age: 3
</code></pre>
<p>7、我们刚才已经把person这个对象的所有值都写好了，我们现在来注入到我们的类中！</p>
<pre><code class="language-java">/*@ConfigurationProperties将配置文件中配置的每一个属性的值，映射到这个组件中；
告诉SpringBoot将本类中的所有属性和配置文件中相关的配置进行绑定参数 prefix = “person” :
 将配置文件中的person下面的所有属性一一对应
 这里的person为配置文件中设置的对象名字 这个可以进行自定义


 */
//注册bean
@Component
@ConfigurationProperties(prefix = &quot;person&quot;)
public class Person {
    private String name;
    private Integer age;
    private boolean happy;
    private Date birth;
    private Map&lt;String,Object&gt; maps;
    private List&lt;Object&gt;list;
    private Dog dog;
}

</code></pre>
<p>8、IDEA 提示，springboot配置注解处理器没有找到，让我们看文档，我们可以查看文档，找到一个依赖！</p>
<figure data-type="image" tabindex="23"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221173612298.png" alt="image-20210221173612298" loading="lazy"></figure>
<pre><code class="language-xml">&lt;!-- 导入配置文件处理器，配置文件进行绑定就会有提示，需要重启 --&gt;&lt;dependency&gt;  &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;  &lt;artifactId&gt;spring-boot-configuration-processor&lt;/artifactId&gt;  &lt;optional&gt;true&lt;/optional&gt;&lt;/dependency&gt;
</code></pre>
<p>9、确认以上配置都OK之后，我们去测试类中测试一下：</p>
<p>在测试类当中“</p>
<pre><code class="language-java">@SpringBootTest
class Springboot02ConfigApplicationTests {
    //person对象自动注入
    @Autowired
    Person person;

    @Test
    void contextLoads() {
        System.out.println(person);
    }

}

</code></pre>
<p>结果：所有值全部注入成功！</p>
<figure data-type="image" tabindex="24"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221174353470.png" alt="image-20210221174353470" loading="lazy"></figure>
<figure data-type="image" tabindex="25"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>yaml配置注入到实体类完全OK！</strong></p>
<p>课堂测试：</p>
<p>1、将配置文件的key 值 和 属性的值设置为不一样，则结果输出为null，注入失败</p>
<p>2、在配置一个person2，然后将 @ConfigurationProperties(prefix = &quot;person2&quot;) 指向我们的person2；</p>
<p>测试结果</p>
<ul>
<li>如果不加自动注入注解 会输出为空</li>
<li>在实体类中@ConfigurationProperties中的配置后面配置绑定错误 得到值将不是在配置文件中对应的</li>
</ul>
<h2 id="加载指定的配置文件">加载指定的配置文件</h2>
<p>这里注意要在setting里面设置相关的编码格式才不会乱码</p>
<figure data-type="image" tabindex="26"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221180757390.png" alt="image-20210221180757390" loading="lazy"></figure>
<p>**@PropertySource ：**加载指定的配置文件；</p>
<p><strong>@configurationProperties</strong>：默认从全局配置文件中获取值；</p>
<p>1、我们去在resources目录下新建一个<strong>person.properties</strong>文件</p>
<pre><code class="language-properties">name=kuangshen
</code></pre>
<p>2、然后在我们的代码中指定加载person.properties文件</p>
<pre><code class="language-java">@PropertySource(value = &quot;classpath:person.properties&quot;)
@Component //注册bean
public class Person {
    @Value(&quot;${name}&quot;)    
    private String name;
    ......  }
</code></pre>
<p>3、再次输出测试一下：指定配置文件绑定成功！</p>
<figure data-type="image" tabindex="27"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221175625707.png" alt="img" loading="lazy"></figure>
<h2 id="配置文件占位符">配置文件占位符</h2>
<p>配置文件还可以编写占位符生成随机数</p>
<pre><code class="language-yaml">person3:
  name: qinjiang${random.uuid} #随机uuid
  age: ${random.int}  # 随机int
  happy: false
  birth: 2000/01/01
  maps: {k1: v1,k2: v2}
  list:
    - code
    - girl
    - music
  hello: 笨蛋
  dog:
    name: ${person3.hello:other}_旺财
    age: 1
</code></pre>
<figure data-type="image" tabindex="28"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221182242765.png" alt="image-20210221182242765" loading="lazy"></figure>
<p>测试成功！</p>
<h2 id="回顾properties配置">回顾properties配置</h2>
<p>我们上面采用的yaml方法都是最简单的方式，开发中最常用的；也是springboot所推荐的！那我们来唠唠其他的实现方式，道理都是相同的；写还是那样写；配置文件除了yml还有我们之前常用的properties ， 我们没有讲，我们来唠唠！</p>
<p>【注意】properties配置文件在写中文的时候，会有乱码 ， 我们需要去IDEA中设置编码格式为UTF-8；</p>
<p>settings--&gt;FileEncodings 中配置；</p>
<figure data-type="image" tabindex="29"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>测试步骤：</strong></p>
<p>1、新建一个实体类User</p>
<pre><code class="language-java">@Component //注册bean

public class User {    
    private String name;
    private int age;  
    private String sex;}
</code></pre>
<p>2、编辑配置文件 user.properties</p>
<pre><code class="language-properties">user.name=lindamao
user.age=18
user.sex=男
</code></pre>
<p>3、我们在User类上使用@Value来进行注入！</p>
<pre><code class="language-java">@Component
@PropertySource(value = &quot;classpath:user.properties&quot;)
public class User {
    //直接使用@value
    @Value(&quot;${user.name}&quot;)
    //从配置文件中取值
    private String name;
    @Value(&quot;${user.age}&quot;)
    // #{SPEL} Spring表达式
    private int age;
    @Value(&quot;${user.sex}&quot;)  // 字面量
    private String sex;
  //有参无参构造和get set方法 以及toString 
}
</code></pre>
<p>4、Springboot测试</p>
<pre><code class="language-java">@SpringBootTestclass DemoApplicationTests {
    @Autowired    
    User user;
    @Test   
    public void contextLoads() {        System.out.println(user);    }
}
</code></pre>
<p>结果正常输出：</p>
<figure data-type="image" tabindex="30"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221183404624.png" alt="image-20210221183404624" loading="lazy"></figure>
<h2 id="对比小结">对比小结</h2>
<p>@Value这个使用起来并不友好！我们需要为每个属性单独注解赋值，比较麻烦；我们来看个功能对比图</p>
<figure data-type="image" tabindex="31"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221170255947.png" alt="image-20210221170255947" loading="lazy"></figure>
<p>1、@ConfigurationProperties只需要写一次即可 ， @Value则需要每个字段都添加</p>
<p>2、<strong>松散绑定</strong>：这个什么意思呢? 比如我的yml中写的last-name，这个和lastName是一样的，横杆 <strong>- 后面跟着的字母默认是大写的</strong>。这就是松散绑定。可以测试一下</p>
<p>3、JSR303数据校验 ， 这个就是我们可以在字段是增加一层过滤器验证 ， 可以保证数据的合法性</p>
<p>4、复杂类型封装，yml中可以封装对象 ， 使用value就不支持</p>
<p><strong>结论：</strong></p>
<p>配置yml和配置properties都可以获取到值 ， <strong>强烈推荐 yml</strong>；</p>
<p>如果我们在某个业务中，<strong>只需要获取配置文件中的某个值</strong>，可以使用一下 <strong>@value</strong>；</p>
<p>如果说，我们专门编写了一个JavaBean来和配置文件进行一一映射，就直接**@configurationProperties**，不要犹豫！</p>
<h1 id="springboot04jsr303数据校验及多环境切换">SpringBoot04:JSR303数据校验及多环境切换</h1>
<p>JSR303数据校验</p>
<h2 id="先看看如何使用">先看看如何使用</h2>
<p>Springboot中可以用@validated来校验数据，如果数据异常则会统一抛出异常，方便异常中心统一处理。我们这里来写个注解让我们的name只能支持Email格式；</p>
<p>@validated 注意要记得写上这个注解 否则就不会完成相关的校验操作</p>
<pre><code class="language-java">@Component //注bean
@ConfigurationProperties(prefix = &quot;person&quot;)
@Validated  //数据校验
public class Person {
    @Email(message=&quot;邮箱格式错误&quot;) 
    //name必须是邮箱格式    
    private String name;
}
</code></pre>
<p>运行结果 ：default message [不是一个合法的电子邮件地址];</p>
<figure data-type="image" tabindex="32"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbDYdR5f05BDysj0YVJMxadN0psDJKzXe7zyTrL9wFpTEHoiba0MsM11Fw/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p><strong>使用数据校验，可以保证数据的正确性；</strong></p>
<figure data-type="image" tabindex="33"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221211958964.png" alt="image-20210221211958964" loading="lazy"></figure>
<h2 id="常见参数">常见参数</h2>
<p>@NotNull(message=&quot;名字不能为空&quot;)<br>
private String userName;</p>
<p>@Max(value=120,message=&quot;年龄最大不能查过120&quot;)<br>
private int age</p>
<p>@Email(message=&quot;邮箱格式错误&quot;)<br>
private String email;</p>
<p>@Null       验证对象是否为null</p>
<p>@NotNull    验证对象是否不为null, 无法查检长度为0的字符串</p>
<p>@NotBlank   检查约束字符串是不是Null还有被Trim的长度是否大于0,只对字符串,且会去掉前后空格.</p>
<p>@NotEmpty 检查约束元素是否为NULL或者是EMPTY.    Booelan检查</p>
<p>@AssertTrue     验证 Boolean 对象是否为 true  @AssertFalse    验证 Boolean 对象是否为 false      长度检查</p>
<p>@Size(min=, max=) 验证对象（Array,Collection,Map,String）长度是否在给定的范围之内</p>
<p>@Length(min=, max=) string is between min and max included.<br>
日期检查</p>
<p>@Past       验证 Date 和 Calendar 对象是否在当前时间之前  @Future     验证 Date 和 Calendar 对象是否在当前时间之后  @Pattern    验证 String 对象是否符合正则表达式的规则<br>
.......等等除此以外，我们还可以自定义一些数据校验规则</p>
<h2 id="多环境切换"><strong>多环境切换</strong></h2>
<p>profile是Spring对不同环境提供不同配置功能的支持，可以通过激活不同的环境版本，实现快速切换环境；</p>
<p><strong>相关的注解放在context想关的包中</strong></p>
<figure data-type="image" tabindex="34"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210221212208028.png" alt="image-20210221212208028" loading="lazy"></figure>
<h2 id="多配置文件">多配置文件.</h2>
<p>我们在主配置文件编写的时候，文件名可以是 application-{profile}.properties/yml , 用来指定多个环境版本；</p>
<p><strong>例如：</strong></p>
<p>application-<strong>test</strong>.properties 代表测试环境配置</p>
<p>application-<strong>dev</strong>.properties 代表开发环境配置</p>
<p>但是Springboot并不会直接启动这些配置文件，它<strong>默认使用application.properties主配置文件</strong>；</p>
<figure data-type="image" tabindex="35"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222165310704.png" alt="image-20210222165310704" loading="lazy"></figure>
<p>我们需要通过一个配置来选择需要激活的环境：</p>
<p>#比如在配置文件中指定使用dev环境，我们可以通过设置不同的端口号进行测试；#我们启动SpringBoot，就可以看到已经切换到dev下的配置了；spring.profiles.<strong>active</strong>=test</p>
<figure data-type="image" tabindex="36"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222165405267.png" alt="image-20210222165405267" loading="lazy"></figure>
<p>如图就激活了test 的配置</p>
<h2 id="yaml的多文档块">yaml的多文档块</h2>
<p>和properties配置文件中一样，但是使用yml去实现不需要创建多个配置文件，使得整个的过程不必那么繁琐</p>
<pre><code class="language-YAML">server:
  port: 8081 #选择要激活那个环境块
spring:
  profiles:
---
server:
  port: 8083
spring:
  profiles: dev #配置环境的名称
---
server:
  port: 8084
spring:
  profiles: prod  #配置环境的名称
</code></pre>
<p><strong>注意：如果yml和properties同时都配置了端口，并且没有激活其他环境 ， 默认会使用properties配置文件的！</strong></p>
<p>如图</p>
<figure data-type="image" tabindex="37"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222165907862.png" alt="image-20210222165907862" loading="lazy"></figure>
<p>选择了激活配置之后就会选择那个对应的配置</p>
<figure data-type="image" tabindex="38"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222170005206.png" alt="image-20210222170005206" loading="lazy"></figure>
<h2 id="配置文件加载位置">配置文件加载位置</h2>
<p><strong>外部加载配置文件的方式十分多，我们选择最常用的即可，在开发的资源文件中进行配置！</strong></p>
<p><strong>官方外部配置文件说明参考文档</strong></p>
<p>springboot 启动会扫描以下位置的application.properties或者application.yml文件作为Spring boot的默认配置文件：</p>
<p><strong>优先级1</strong>：项目路径下的config文件夹配置文件</p>
<p><strong>优先级2</strong>：项目路径下配置文件</p>
<p><strong>优先级3</strong>：资源路径下的config文件夹配置文件</p>
<p><strong>优先级4</strong>：资源路径下配置文件</p>
<p>优先级由高到底，<strong>高优先级的配置会覆盖低优先级的配置</strong>；</p>
<p>如图</p>
<figure data-type="image" tabindex="39"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222164751020.png" alt="image-20210222164751020" loading="lazy"></figure>
<figure data-type="image" tabindex="40"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222165017896.png" alt="image-20210222165017896" loading="lazy"></figure>
<p><strong>SpringBoot会从这四个位置全部加载主配置文件；互补配置；</strong></p>
<p>我们在最低级的配置文件中设置一个项目访问路径的配置来测试互补问题；</p>
<p>#配置项目的访问路径server.servlet.context-path=/kuang</p>
<h2 id="拓展运维小技巧">拓展，运维小技巧</h2>
<p><strong>指定位置加载配置文件</strong></p>
<p>我们还可以通过<strong>spring.config.location</strong>来改变默认的配置文件位置</p>
<p>项目打包好以后，我们可以使用命令行参数的形式，启动项目的时候来指定配置文件的新位置；</p>
<p>这种情况，一般是后期运维做的多，相同配置，<strong>外部指定的配置文件优先级最高</strong></p>
<pre><code>java -jar spring-boot-config.jar --spring.config.location=F:/application.properties
</code></pre>
<h1 id="springboot05自动配置原理">SpringBoot05：自动配置原理</h1>
<p><strong>自动配置原理</strong></p>
<p>配置文件到底能写什么？怎么写？</p>
<p>SpringBoot官方文档中有大量的配置，我们无法全部记住</p>
<figure data-type="image" tabindex="41"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbD7ibqw837BhN1F7lHdAMhMmYNCYF2tSdvUGv0y3X48tzetuuYc8tUMLg/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<h2 id="分析自动配置原理">分析自动配置原理</h2>
<p>我们以**HttpEncodingAutoConfiguration（Http编码自动配置）**<strong>为例解释自动配置原理</strong>；</p>
<p>在<strong>spring.factroies</strong>当中点进去就能看到 这个只是其中前一个例子</p>
<pre><code class="language-java">//表示这是一个配置类，和以前编写的配置文件一样，也可以给容器中添加组件；
@Configuration 
//启动指定类的ConfigurationProperties功能； 
//进入这个HttpProperties查看，将配置文件中对应的值和HttpProperties绑定起来；  
//并把HttpProperties配置加入到ioc容器中
@EnableConfigurationProperties({HttpProperties.class}) 
//Spring底层
@Conditional注解  
//根据不同的条件判断，如果满足指定的条件，整个配置类里面的配置就会生效；  
//这里的意思就是判断当前应用是否是web应用，如果是，当前配置类生效
@ConditionalOnWebApplication(    type = Type.SERVLET)
//判断当前项目有没有这个类CharacterEncodingFilter；SpringMVC中进行乱码解决的过滤器；
@ConditionalOnClass({CharacterEncodingFilter.class})
//判断配置文件中是否存在某个配置：spring.http.encoding.enabled；  
//如果不存在，判断也是成立的  //即使我们配置文件中不配置pring.http.encoding.enabled=true，也是默认生效的；@ConditionalOnProperty(    prefix = &quot;spring.http.encoding&quot;,    value = {&quot;enabled&quot;},    matchIfMissing = true)
public class HttpEncodingAutoConfiguration {    
    //他已经和SpringBoot的配置文件映射了    
    private final Encoding properties;    
    //只有一个有参构造器的情况下，参数的值就会从容器中拿    
    public HttpEncodingAutoConfiguration(HttpProperties properties) {        
        this.properties = properties.getEncoding();
    }        
    //给容器中添加一个组件，这个组件的某些值需要从properties中获取    
    @Bean    
    @ConditionalOnMissingBean 
    //判断容器没有这个组件？   
    public CharacterEncodingFilter characterEncodingFilter() {        
        CharacterEncodingFilter filter = new OrderedCharacterEncodingFilter();        
        filter.setEncoding(this.properties.getCharset().name());        
        filter.setForceRequestEncoding(this.properties.shouldForce(org.springframework.boot.autoconfigure.http.HttpProperties.Encoding.Type.REQUEST));        filter.setForceResponseEncoding(this.properties.shouldForce(org.springframework.boot.autoconfigure.http.HttpProperties.Encoding.Type.RESPONSE));        return filter;    }
    //。。。。。。。
}
</code></pre>
<p><strong>一句话总结 ：根据当前不同的条件判断，决定这个配置类是否生效！</strong></p>
<ul>
<li><strong>一但这个配置类生效；这个配置类就会给容器中添加各种组件</strong>；</li>
<li>这些组件的属性是从对应的<strong>properties类</strong>中获取的，这些类里面的每<strong>一个属性又是和配置文件绑定的</strong>；</li>
<li>所有在配置文件中能配置的属性都是在<strong>xxxxProperties类中封装着</strong>；</li>
<li>配置文件能配置什么就可以参照某个功能<strong>对应的这个属性类</strong></li>
</ul>
<p>举例</p>
<pre><code class="language-java">//从配置文件中获取指定的值和bean的属性进行绑定
//我们开一个根据其配置类中的属性并在相关的配置文件中自定义自己想要修改的变量值
@ConfigurationProperties(prefix = &quot;spring.http&quot;//很久不同的配置类绑定不同相对应的配置文件) 
public class HttpProperties {    
    // .....
}
</code></pre>
<p>我们去配置文件里面试试前缀，看提示！</p>
<figure data-type="image" tabindex="42"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>这就是自动装配的原理！</strong></p>
<h2 id="精髓">精髓</h2>
<p>1、<strong>SpringBoot启动会加载大量的自动配置类</strong></p>
<p>2、我们看我们需要的<strong>功能</strong>有没有在SpringBoot默认写好的自动配置类当中；</p>
<p>3、我们再来看这个自动配置类中到底配置了哪些组件；（只要我们要用的组件存在在其中，我们就不需要再手动配置了）</p>
<p>4、给容器中自动配置类添加组件的时候，会从properties类中获取某些属性。我们只需要在配置文件中指定这些属性的值即可；</p>
<p>**xxxxAutoConfigurartion：自动配置类；**<strong>给容器中添加组件</strong></p>
<p><strong>xxxxProperties:封装配置文件中相关属性；</strong></p>
<figure data-type="image" tabindex="43"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222172942587.png" alt="image-20210222172942587" loading="lazy"></figure>
<h2 id="查找自动转配">查找自动转配</h2>
<p>当我们需要查看自动装配配置类的时候可以通过以下方式查看</p>
<figure data-type="image" tabindex="44"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222174815614.png" alt="image-20210222174815614" loading="lazy"></figure>
<figure data-type="image" tabindex="45"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222174714535.png" alt="image-20210222174714535" loading="lazy"></figure>
<figure data-type="image" tabindex="46"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222175102270.png" alt="image-20210222175102270" loading="lazy"></figure>
<figure data-type="image" tabindex="47"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222175231608.png" alt="image-20210222175231608" loading="lazy"></figure>
<p><strong>可以发现这里面的东西都是类似的</strong> ！！！</p>
<p>出现爆红的问题</p>
<figure data-type="image" tabindex="48"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222175433717.png" alt="image-20210222175433717" loading="lazy"></figure>
<p>这里的依赖就是相关的依赖 导入相关的依赖就可以生效了 这些依赖就是对应的启动器</p>
<h2 id="了解conditional">了解：@Conditional</h2>
<p>了解完自动装配的原理后，我们来关注一个细节问题，<strong>自动配置类必须在一定的条件下才能生效；</strong></p>
<p><strong>@Conditional派生注解（Spring注解版原生的@Conditional作用）</strong></p>
<p>：必须是**@Conditional**指定的条件成立，才给容器中添加组件，配置配里面的所有内容才生效；</p>
<p><strong>那么多的自动配置类，必须在一定的条件下才能生效；也就是说，我们加载了这么多的配置类，但不是所有的都生效了。</strong></p>
<p>我们怎么知道哪些自动配置类生效？</p>
<p><strong>我们可以通过启用 debug=true属性；</strong></p>
<p><strong>来让控制台打印自动配置报告，这样我们就可以很方便的知道哪些自动配置类生效；</strong></p>
<p>#<strong>开启springboot的调试类debug: true</strong></p>
<p><strong>Positive matches:（自动配置类启用的：正匹配）</strong></p>
<p><strong>Negative matches:（没有启动，没有匹配成功的自动配置类：负匹配）</strong></p>
<p><strong>Unconditional classes: （没有条件的类）</strong></p>
<h2 id="查看输出的日志">查看输出的日志</h2>
<figure data-type="image" tabindex="49"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222173314847.png" alt="image-20210222173314847" loading="lazy"></figure>
<p><strong>分类</strong></p>
<p>第一类</p>
<figure data-type="image" tabindex="50"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222173438178.png" alt="image-20210222173438178" loading="lazy"></figure>
<p>第二类</p>
<figure data-type="image" tabindex="51"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222173603304.png" alt="image-20210222173603304" loading="lazy"></figure>
<p>第三类</p>
<figure data-type="image" tabindex="52"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222173709072.png" alt="image-20210222173709072" loading="lazy"></figure>
<p>掌握吸收理解原理，即可以不变应万变！</p>
<h1 id=""><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222171649852.png" alt="image-20210222171649852" loading="lazy"></h1>
<h1 id="springboot06自定义starter">SpringBoot06：自定义starter</h1>
<p><strong>知识点</strong></p>
<h6 id="requestmapping和getmapping区别">@RequestMapping和@GetMapping区别</h6>
<ul>
<li>@RequestMapping可以指定GET、POST请求方式</li>
<li>@GetMapping等价于@RequestMapping的GET请求方式</li>
</ul>
<p><strong>自定义Starter</strong></p>
<p>我们分析完毕了源码以及自动装配的过程，我们可以尝试自定义一个启动器来玩玩！</p>
<h2 id="说明">说明</h2>
<p>启动器模块是一个 空 jar 文件，仅提供辅助性依赖管理，这些依赖可能用于自动装配或者其他类库；</p>
<p><strong>命名归约：</strong></p>
<p>官方命名：</p>
<ul>
<li>前缀：spring-boot-starter-xxx</li>
<li>比如：spring-boot-starter-web....</li>
</ul>
<p>自定义命名：</p>
<ul>
<li>xxx-spring-boot-starter</li>
<li>比如：mybatis-spring-boot-starter</li>
</ul>
<h2 id="编写启动器">编写启动器</h2>
<p>1、在IDEA中新建一个空项目 spring-boot-starter-diy</p>
<p>2、新建一个普通Maven模块：kuang-spring-boot-starter</p>
<figure data-type="image" tabindex="53"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbD3ibDQhiaQib3L94Y8joxBzUfegDicVpSamYoysTZ54lMp3oXw0cprPIPQA/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>3、新建一个Springboot模块：kuang-spring-boot-starter-autoconfigure</p>
<figure data-type="image" tabindex="54"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbDhebhQQvGxRRLX8FezGo8Qk3bGEQElY6K2SqeDazGUZ6XqLDMvq6RDA/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>4、点击apply即可，基本结构</p>
<figure data-type="image" tabindex="55"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbDTPIzxXJqk27EGiaIBJtSpic5NcibXFX2eHXMxezmURLbibFXahiaFk5uW4A/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>5、在我们的 starter 中 导入  autoconfigure 的依赖！</p>
<pre><code class="language-xml">&lt;!-- 启动器 --&gt;&lt;dependencies&gt;    &lt;!--  引入自动配置模块 --&gt;    &lt;dependency&gt;        &lt;groupId&gt;com.kuang&lt;/groupId&gt;        &lt;artifactId&gt;kuang-spring-boot-starter-autoconfigure&lt;/artifactId&gt;       
    &lt;version&gt;0.0.1-SNAPSHOT&lt;/version&gt;    &lt;/dependency&gt;&lt;/dependencies&gt;
</code></pre>
<p>6、将 autoconfigure 项目下多余的文件都删掉，Pom中只留下一个 starter，这是所有的启动器基本配置！</p>
<figure data-type="image" tabindex="56"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7IPEXZtUAUBhnSZvUmrPzbDNLrzEP3HC2HYHicHmmLGian64wdOw3wiauDttoUbQLMdX4XcFV2HqyVvw/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>7、我们编写一个自己的服务</p>
<pre><code class="language-java">package com.lin;
public class HelloService {
    HelloProperties helloProperties;
    public HelloProperties getHelloProperties() { 
        return helloProperties;  
                                                }
    public void setHelloProperties(HelloProperties helloProperties)
    {    
        this.helloProperties = helloProperties;  
    }
    public String sayHello(String name){ 
        return helloProperties.getPrefix() + name + helloProperties.getSuffix();   
    }
}
</code></pre>
<p>8、编写HelloProperties 配置类</p>
<pre><code class="language-java">package com.kuang;
import org.springframework.boot.context.properties.ConfigurationProperties;
// 前缀 kuang.hello
@ConfigurationProperties(prefix = &quot;kuang.hello&quot;)public class HelloProperties {
    private String prefix;    
    private String suffix;
    public String getPrefix() {  
        return prefix;    
    }
    public void setPrefix(String prefix) {   
        this.prefix = prefix;   
                                         }
    public String getSuffix() {  
        
        return suffix; 
    }
    public void setSuffix(String suffix) { 
        this.suffix = suffix;    }
}
</code></pre>
<p>9、编写我们的自动配置类并注入bean，测试！</p>
<pre><code class="language-java">package com.kuang;
import org.springframework.beans.factory.annotation.Autowired;import org.springframework.boot.autoconfigure.condition.ConditionalOnWebApplication;import org.springframework.boot.context.properties.EnableConfigurationProperties;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;
@Configuration@ConditionalOnWebApplication //web应用生效
@EnableConfigurationProperties(HelloProperties.class)
public class HelloServiceAutoConfiguration {
    @Autowired   
    HelloProperties helloProperties;
    @Bean    
    public HelloService helloService(){        
        HelloService service = new HelloService();        
        service.setHelloProperties(helloProperties);        
        return service;    }
}
</code></pre>
<p>10、在resources编写一个自己的 META-INF\spring.factories</p>
<pre><code># Auto Configureorg.springframework.boot.autoconfigure.EnableAutoConfiguration=\com.kuang.HelloServiceAutoConfiguration
</code></pre>
<p>11、编写完成后，可以安装到maven仓库中！</p>
<figure data-type="image" tabindex="57"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<h2 id="新建项目测试我们自己写的启动器">新建项目测试我们自己写的启动器</h2>
<p>1、新建一个SpringBoot 项目</p>
<p>2、导入我们自己写的启动器</p>
<pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;com.kuang&lt;/groupId&gt;    &lt;artifactId&gt;kuang-spring-boot-starter&lt;/artifactId&gt;    &lt;version&gt;1.0-SNAPSHOT&lt;/version&gt;&lt;/dependency&gt;
</code></pre>
<p>3、编写一个 HelloController  进行测试我们自己的写的接口！</p>
<pre><code class="language-java">package com.kuang.controller;
@RestControllerpublic class HelloController {
    @Autowired    HelloService helloService;
    @RequestMapping(&quot;/hello&quot;)   
    public String hello(){      
        return helloService.sayHello(&quot;zxc&quot;);   
    }
}
</code></pre>
<p>4、编写配置文件 application.properties</p>
<pre><code>kuang.hello.prefix=&quot;ppp&quot;kuang.hello.suffix=&quot;sss&quot;
</code></pre>
<p>5、启动项目进行测试，结果成功 !</p>
<figure data-type="image" tabindex="58"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>温馨提示：学完的东西一定要多下去实践！</strong></p>
<h1 id="springboot07整合jdbc">SpringBoot07：整合JDBC</h1>
<p>SpringData简介</p>
<p>对于数据访问层，无论是 SQL(关系型数据库) 还是 NOSQL(非关系型数据库)，Spring Boot 底层都是采用 Spring Data 的方式进行统一处理。</p>
<p>Spring Boot 底层都是采用 Spring Data 的方式进行统一处理各种数据库，Spring Data 也是 Spring 中与 Spring Boot、Spring Cloud 等齐名的知名项目。</p>
<p>Sping Data 官网：https://spring.io/projects/spring-data</p>
<p>数据库相关的启动器 ：可以参考官方文档：</p>
<p>https://docs.spring.io/spring-boot/docs/2.2.5.RELEASE/reference/htmlsingle/#using-boot-starter</p>
<p><strong>整合JDBC</strong></p>
<h2 id="创建测试项目测试数据源">创建测试项目测试数据源</h2>
<p>1、新建一个项目测试：springboot-data-jdbc ; 引入相应的模块！基础模块</p>
<p>还得把web项目勾上！</p>
<figure data-type="image" tabindex="59"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7LYLicOHVGnwu7ibGvbwXibYeuW0A5wz8gu4q4AMoBCoYic2Juyiate9VBZe4S0sgkCZVFV2lD6quLKVZw/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>2、项目建好之后，发现自动帮我们导入了如下的启动器：</p>
<pre><code class="language-xml">&lt;dependency&gt;   
    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;    &lt;artifactId&gt;spring-boot-starter-jdbc&lt;/artifactId&gt;
&lt;/dependency&gt;&lt;dependency&gt;   
    &lt;groupId&gt;mysql&lt;/groupId&gt;   
    &lt;artifactId&gt;mysql-connector-java&lt;/artifactId&gt;    &lt;scope&gt;runtime&lt;/scope&gt;
&lt;/dependency&gt;
</code></pre>
<p>3、编写yaml配置文件连接数据库；</p>
<pre><code class="language-yaml">spring: 
	datasource:  
    	username: root    
    	password:    
    	#?serverTimezone=UTC解决时区的报错   
        url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=utf-8   
        driver-class-name: com.mysql.cj.jdbc.Driver
</code></pre>
<p>这里的配置想要点击去查看源码也可以查看的到</p>
<figure data-type="image" tabindex="60"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301160542488.png" alt="image-20210301160542488" loading="lazy"></figure>
<p>4、配置完这一些东西后，我们就可以直接去使用了，因为SpringBoot已经默认帮我们进行了自动配置；去测试类测试一下</p>
<pre><code class="language-java">@SpringBootTestclass SpringbootDataJdbcApplicationTests {
    //DI注入数据源   
    @Autowired   
    DataSource dataSource;
    @Test  
    public void contextLoads() throws SQLException {      
        //看一下默认数据源       
        System.out.println(dataSource.getClass());      
        //获得连接     
        Connection connection =   dataSource.getConnection();       
        System.out.println(connection);    
        //关闭连接        
        connection.close(); 
    }
}
</code></pre>
<p>结果：我们可以看到他默认给我们配置的数据源为 : class com.zaxxer.hikari.HikariDataSource ， <strong>我们并没有手动配置</strong></p>
<p>我们来全局搜索一下，找到数据源的所有自动配置都在 ：DataSourceAutoConfiguration文件：</p>
<figure data-type="image" tabindex="61"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301160336664.png" alt="image-20210301160336664" loading="lazy"></figure>
<pre><code class="language-java">@Import(    {Hikari.class, Tomcat.class, Dbcp2.class, Generic.class, DataSourceJmxConfiguration.class})protected static class PooledDataSourceConfiguration {    protected PooledDataSourceConfiguration() {    }}
</code></pre>
<p>这里导入的类都在 <strong>DataSourceConfiguration</strong> 配置类下，</p>
<p>也可以通过包查看</p>
<figure data-type="image" tabindex="62"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301160725893.png" alt="image-20210301160725893" loading="lazy"></figure>
<p>可以看出 Spring Boot 2.2.5 默认使用HikariDataSource 数据源，而以前版本，如 Spring Boot 1.5 默认使用 org.apache.tomcat.jdbc.pool.DataSource 作为数据源；</p>
<p><strong>HikariDataSource 号称 Java WEB 当前速度最快的数据源，相比于传统的 C3P0 、DBCP、Tomcat jdbc 等连接池更加优秀；</strong></p>
<p><strong>可以使用 spring.datasource.type 指定自定义的数据源类型，值为 要使用的连接池实现的完全限定名。</strong></p>
<p>关于数据源我们并不做介绍，有了数据库连接，显然就可以 CRUD 操作数据库了。但是我们需要先了解一个对象 JdbcTemplate</p>
<h2 id="jdbctemplate">JDBCTemplate</h2>
<p>1、有了数据源(com.zaxxer.hikari.HikariDataSource)，然后可以拿到数据库连接(java.sql.Connection)，有了连接，就可以使用原生的 JDBC 语句来操作数据库；</p>
<p>2、即使不使用第三方第数据库操作框架，如 MyBatis等，Spring 本身也对原生的JDBC 做了轻量级的封装，即JdbcTemplate。</p>
<p>3、数据库操作的所有 CRUD 方法都在 JdbcTemplate 中。</p>
<p>4、Spring Boot 不仅提供了默认的数据源，同时默认已经配置好了 JdbcTemplate 放在了容器中，程序员只需<strong>自己注入</strong>即可使用</p>
<p>5、JdbcTemplate 的自动配置是依赖 org.springframework.boot.autoconfigure.jdbc 包下的 JdbcTemplateConfiguration 类</p>
<p><strong>JdbcTemplate主要提供以下几类方法：</strong></p>
<ul>
<li>execute方法：可以用于执行任何SQL语句，一般用于执行DDL语句；</li>
<li>update方法及batchUpdate方法：update方法用于执行新增、修改、删除等语句；batchUpdate方法用于执行批处理相关语句；</li>
<li>query方法及queryForXXX方法：用于执行查询相关语句；</li>
<li>call方法：用于执行存储过程、函数相关语句。</li>
<li></li>
</ul>
<h2 id="测试">测试</h2>
<p>编写一个Controller，注入 jdbcTemplate，编写测试方法进行访问测试；</p>
<pre><code class="language-java">package com.lin.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;


import java.util.List;
import java.util.Map;
@RestController
public class JdbController {
    //要把jdbctemplate 中的数据先注入进来
    @Autowired
    JdbcTemplate jdbcTemplate;

    //查询数据库中的所有信息
    // 通过map取代实体类来获取数据库中的东西

    @GetMapping(&quot;/getall&quot;)
    public List&lt;Map&lt;String,Object&gt;&gt; userlist(){
        String sql=&quot;select * from user&quot;;
        List&lt;Map&lt;String, Object&gt;&gt; mapList = jdbcTemplate.queryForList(sql);
        return mapList;

    }
    //添加一个信息
    @GetMapping(&quot;/addUser&quot;)
    public String addUser(){
        String sql=&quot;insert into mybatis.user(id,name,pwd) values (7,'小红','123456')&quot;;
        jdbcTemplate.update(sql);
        return &quot;adduser-OK!&quot;;
    }
    //通过id更新数据
    @GetMapping(&quot;/update/{id}&quot;)
    public String update(@PathVariable(&quot;id&quot;) int id){
        String sql = &quot;update mybatis.user set name =?,pwd=? where id=&quot;+id;

        //通过OBject封装数据
        Object[] objects = new Object[2];
        objects[0]=&quot;小张&quot;;
        objects[1]=&quot;789456&quot;;

        jdbcTemplate.update(sql,objects);
        return &quot;update-OK!&quot;;
    }
    //通过id删除用户
    @GetMapping(&quot;/delete/{id}&quot;)
    public String delete(@PathVariable(&quot;id&quot;) int id){
        String sql = &quot;delete from mybatis.user where id=?&quot;;
        jdbcTemplate.update(sql,id);
        return &quot;dalete-OK!&quot;;
    }



}

</code></pre>
<p>这里注意的是：</p>
<p>jdbcTemplate.update() 后面还可以进行传递参数 具体根据具体的业务来进行实现。</p>
<p>测试请求，结果正常；</p>
<p>到此，CURD的基本操作，使用 JDBC 就搞定了。</p>
<pre><code class="language-java">   @GetMapping(&quot;/delete/{id}&quot;)
    public String delete(@PathVariable(&quot;id&quot;) int id){
        String sql = &quot;delete from mybatis.user where id=?&quot;;
        jdbcTemplate.update(sql,id);
        return &quot;dalete-OK!&quot;;
</code></pre>
<h1 id="springboot08整合druid">SpringBoot08：整合Druid</h1>
<p>集成Druid</p>
<h2 id="druid简介">Druid简介</h2>
<p>Java程序很大一部分要操作数据库，为了提高性能操作数据库的时候，又不得不使用数据库连接池。</p>
<p>Druid 是阿里巴巴开源平台上一个数据库连接池实现，结合了 C3P0、DBCP 等 DB 池的优点，同时加入了日志监控。</p>
<p>Druid 可以很好的监控 DB 池连接和 SQL 的执行情况，天生就是针对监控而生的 DB 连接池。</p>
<p>Druid已经在阿里巴巴部署了超过600个应用，经过一年多生产环境大规模部署的严苛考验。</p>
<p>Spring Boot 2.0 以上默认使用 Hikari 数据源，可以说 Hikari 与 Driud 都是当前 Java Web 上最优秀的数据源，我们来重点介绍 Spring Boot 如何集成 Druid 数据源，如何实现数据库监控。</p>
<p>Github地址：https://github.com/alibaba/druid/</p>
<p><strong>com.alibaba.druid.pool.DruidDataSource 基本配置参数如下：</strong></p>
<figure data-type="image" tabindex="63"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7LYLicOHVGnwu7ibGvbwXibYeupdhDcaDPRLHgnULFbaJB5kPtC8n5QVLaUbbTRfa4ZyqficzZYrd2llA/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<figure data-type="image" tabindex="64"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7LYLicOHVGnwu7ibGvbwXibYeubiciawTdz0tg1EKDjZ1xaIgjRW9CZ4Apr4hvNz3iaQVQIKS3sXy629Lgg/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<figure data-type="image" tabindex="65"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7LYLicOHVGnwu7ibGvbwXibYeuaVD6mK3LJrtZ4B6fRKCLDgYicAVGzTUTzWdCNB5lF4tLpbcCT0uq1EA/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<h2 id="配置数据源">配置数据源</h2>
<p>1、添加上 Druid 数据源依赖。</p>
<pre><code class="language-xml">&lt;!-- https://mvnrepository.com/artifact/com.alibaba/druid --&gt;&lt;dependency&gt;  
    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;   
    &lt;artifactId&gt;druid&lt;/artifactId&gt;  
    &lt;version&gt;1.1.22&lt;/version&gt;&lt;
    /dependency&gt;
</code></pre>
<p>2、切换数据源；之前已经说过 Spring Boot 2.0 以上默认使用 com.zaxxer.hikari.HikariDataSource 数据源，但可以 通过 spring.datasource.type 指定数据源。</p>
<pre><code class="language-yaml">spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    password:
    type: com.alibaba.druid.pool.DruidDataSource
#然后下面是自定义数据源配置
</code></pre>
<p>3、数据源切换之后，在测试类中注入 DataSource，然后获取到它，输出一看便知是否成功切换；</p>
<figure data-type="image" tabindex="66"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301163758964.png" alt="图片" loading="lazy"></figure>
<p>4、切换成功！既然切换成功，就可以设置数据源连接初始化大小、最大连接数、等待时间、最小连接数 等设置项；可以查看源码</p>
<pre><code class="language-yaml">spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    password:
    type: com.alibaba.druid.pool.DruidDataSource

    #Spring Boot 默认是不注入这些属性值的，需要自己绑定   
    #druid 数据源专有配置    
    initialSize: 5   
    minIdle: 5   
    maxActive: 20   
    maxWait: 60000  
    timeBetweenEvictionRunsMillis: 60000    
    minEvictableIdleTimeMillis: 300000    
    validationQuery: SELECT 1 FROM DUAL   
    testWhileIdle: true  
    testOnBorrow: false
    testOnReturn: false   
    poolPreparedStatements: true
    #配置监控统计拦截的filters，stat:监控统计、log4j：日志记录、wall：防御sql注入   
    #如果允许时报错  java.lang.ClassNotFoundException: org.apache.log4j.Priority   
    #则导入 log4j 依赖即可，Maven 地址：https://mvnrepository.com/artifact/log4j/log4j   
    filters: stat,wall,log4j    
    maxPoolPreparedStatementPerConnectionSize: 20    
    useGlobalDataSourceStat: true    connectionProperties: 
    druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
</code></pre>
<p>5、导入Log4j 的依赖</p>
<pre><code class="language-xml">&lt;!-- https://mvnrepository.com/artifact/log4j/log4j --&gt;&lt;dependency&gt;    &lt;groupId&gt;log4j&lt;/groupId&gt;    &lt;artifactId&gt;log4j&lt;/artifactId&gt;    &lt;version&gt;1.2.17&lt;/version&gt;&lt;/dependency&gt;
</code></pre>
<p>6、现在需要程序员自己为 DruidDataSource 绑定全局配置文件中的参数，再添加到容器中，而不再使用 Spring Boot 的自动生成了；我们需要 自己添加 DruidDataSource 组件到容器中，并绑定属性；</p>
<pre><code class="language-java">package com.lin.config;
import com.alibaba.druid.pool.DruidDataSource;import org.springframework.boot.context.properties.ConfigurationProperties;import org.springframework.context.annotation.Bean;import org.springframework.context.annotation.Configuration;
import javax.sql.DataSource;
@Configuration
public class DruidConfig {
    /*       将自定义的 Druid数据源添加到容器中，不再让 Spring Boot 自动创建       绑定全局配置文件中的 druid 数据源属性到 com.alibaba.druid.pool.DruidDataSource从而让它们生效       
    @ConfigurationProperties(prefix = &quot;spring.datasource&quot;)：作用就是将 全局配置文件中       前缀为 spring.datasource的属性值注入到 com.alibaba.druid.pool.DruidDataSource 的同名参数中     */     
    @Bean
    @ConfigurationProperties(prefix = &quot;spring.datasource&quot;)
    public DataSource druidDataSource() {
        return new DruidDataSource();
    }
}
</code></pre>
<p>7、去测试类中测试一下；看是否成功！</p>
<p>测试成功之后在写</p>
<pre><code class="language-java">@SpringBootTestclass SpringbootDataJdbcApplicationTests {
    //DI注入数据源  
    @Autowired    
    DataSource dataSource;
    @Test  
    public void contextLoads() throws SQLException {        
        //看一下默认数据源        
        System.out.println(dataSource.getClass());       
        //获得连接       
        Connection connection =   dataSource.getConnection();        System.out.println(connection);
        
        DruidDataSource druidDataSource = (DruidDataSource) dataSource;       
        System.out.println(&quot;druidDataSource 数据源最大连接数：&quot; + druidDataSource.getMaxActive());        
        System.out.println(&quot;druidDataSource 数据源初始化连接数：&quot; + druidDataSource.getInitialSize());
        //关闭连接     
        connection.close();    }
}
</code></pre>
<p>输出结果 ：可见配置参数已经生效！</p>
<figure data-type="image" tabindex="67"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301171301477.png" alt="img" loading="lazy"></figure>
<h2 id="配置druid数据源监控">配置Druid数据源监控</h2>
<p>Druid 数据源具有监控的功能，并提供了一个 web 界面方便用户查看，类似安装 路由器 时，人家也提供了一个默认的 web 页面。</p>
<p>所以第一步需要设置 Druid 的后台管理页面，比如 登录账号、密码 等；配置后台管理；</p>
<p>这些东西都是写死的</p>
<pre><code class="language-java">    //配置 Druid 监控管理后台的Servlet;
    //内置 Servlet 容器时没有web.xml文件，所以使用 Spring Boot 的注册 Servlet 方式 就相当于web.xml
@Bean
    //这里的路径也是固定好的
    public ServletRegistrationBean statViewServlet() {    ServletRegistrationBean bean = new ServletRegistrationBean(new StatViewServlet(), &quot;/druid/*&quot;);
        // 这些参数可以在 com.alibaba.druid.support.http.StatViewServlet     // 的父类 com.alibaba.druid.support.http.ResourceServlet 中找到
        Map&lt;String, String&gt; initParams = new HashMap&lt;&gt;();
        initParams.put(&quot;loginUsername&quot;, &quot;lindamao&quot;);
        //后台管理界面的登录账号
        initParams.put(&quot;loginPassword&quot;, &quot;123456&quot;);
        //后台管理界面的登录密码
        //后台允许谁可以访问
        //
        initParams.put(&quot;allow&quot;, &quot;localhost&quot;);   //表示只有本机可以访问
        initParams.put(&quot;allow&quot;, &quot;&quot;);//为空或者为null时，表示允许所有访问
        //den：Druid 后台拒绝谁访问
        initParams.put(&quot;kuangshen&quot;, &quot;192.168.1.20&quot;);//表示禁止此ip访问
        //设置初始化参数
        bean.setInitParameters(initParams);
        return bean;
    }
</code></pre>
<p>配置完毕后，我们可以选择访问 ：http://localhost:8080/druid/login.html</p>
<figure data-type="image" tabindex="68"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301171851049.png" alt="image-20210301171851049" loading="lazy"></figure>
<p>进入之后</p>
<figure data-type="image" tabindex="69"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7LYLicOHVGnwu7ibGvbwXibYeuaibKTBtqpoSiaZib9WTRdZaZhIYBRLB8Em7a5aLGw88uKjR8UCian11yCg/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>此时我们执行sql语句会在次页面上能看到</p>
<figure data-type="image" tabindex="70"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210301172007557.png" alt="image-20210301172007557" loading="lazy"></figure>
<p><strong>配置 Druid web 监控 filter 过滤器</strong></p>
<pre><code class="language-java"> @Bean
    public FilterRegistrationBean webStatFilter() {
        FilterRegistrationBean bean = new FilterRegistrationBean();
        bean.setFilter(new WebStatFilter());
        //exclusions：设置哪些请求进行过滤排除掉，从而不进行统计
        Map&lt;String, String&gt; initParams = new HashMap&lt;&gt;();
        initParams.put(&quot;exclusions&quot;, &quot;*.js,*.css,/druid/*,/jdbc/*&quot;);
        bean.setInitParameters(initParams);
        //&quot;/*&quot; 表示过滤所有请求
        bean.setUrlPatterns(Arrays.asList(&quot;/*&quot;));
        return bean;
    }

</code></pre>
<p>平时在工作中，按需求进行配置即可，主要用作监控！</p>
<h1 id="springboot09整合mybatis">SpringBoot09：整合MyBatis</h1>
<p><strong>整合MyBatis</strong></p>
<p>官方文档：http://mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/</p>
<p>Maven仓库地址：https://mvnrepository.com/artifact/org.mybatis.spring.boot/mybatis-spring-boot-starter/2.1.1</p>
<figure data-type="image" tabindex="71"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7KJtP0ZFbTV1gOh8TWzQjj5M0AoJvS3kvTvat9dyzbb71tQicKdTQ31PiaH8nicEx6GpaPgmcA2Qyhxg/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<h2 id="整合测试">整合测试</h2>
<p><strong>配置文件设置</strong></p>
<pre><code class="language-properties">spring.datasource.username=root
spring.datasource.password=
spring.datasource.url=jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=utf-8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
</code></pre>
<p>测试类</p>
<pre><code class="language-java">@SpringBootTest
class Springboot05MybatisApplicationTests {

    @Autowired
    DataSource dataSource;
    @Test
    void contextLoads() throws SQLException {
        System.out.println(dataSource.getClass());
        System.out.println(dataSource.getConnection());


    }
</code></pre>
<p>测试</p>
<p>测试成功</p>
<p>​	<img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210302155142756.png" alt="image-20210302155142756" loading="lazy"></p>
<p>写实体类</p>
<pre><code class="language-java">@Data
@AllArgsConstructor
@NoArgsConstructor
public class User {
    private int id;
    private String name;
    private String pwd;
}
</code></pre>
<p>写mapper接口</p>
<pre><code class="language-java">@Mapper
@Repository
public interface UserMpapper {
    List&lt;User&gt; queryList();

    User queryUserById(int id);

    int addUser(User user);

    int updateUser(User user);

    int deleteuser(int id);

}
</code></pre>
<p>写对应的mapper.xml文件</p>
<p>这时候要在resources的下面建立一个新的文件夹然后在这里面放入mapper对应的文件</p>
<figure data-type="image" tabindex="72"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210302162335066.png" alt="image-20210302162335066" loading="lazy"></figure>
<p>这时候要更新配置文件</p>
<pre><code class="language-properties">#整合mybatis
mybatis.type-aliases-package=com.lin.pojo
mybatis.mapper-locations=classpath:mybatis/mapper/*.xml
</code></pre>
<p>配置文件</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper
        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;
        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;
&lt;mapper namespace=&quot;com.lin.mapper.UserMapper&quot;&gt;
    &lt;select id=&quot;queryList&quot; resultType=&quot;User&quot;&gt;
        select * from mybatis.user
    &lt;/select&gt;
    
    &lt;select id=&quot;queryUserById&quot; resultType=&quot;User&quot;&gt;
        select * from mybatis.user where id=#{id}
    &lt;/select&gt;
    
    &lt;insert id=&quot;addUser&quot; parameterType=&quot;User&quot;&gt;
        insert into mybatis.user (id,name,pwd) values (#{id},#{name},#{pwd})
    &lt;/insert&gt;

    &lt;update id=&quot;updateUser&quot; parameterType=&quot;User&quot;&gt;
        update mybatis.user set name=#{name},pwd=#{pwd} where id=#{id}
    &lt;/update&gt;
    
    &lt;delete id=&quot;deleteuser&quot; parameterType=&quot;int&quot;&gt;
        delete from mybatis.user where id =#{id}
    &lt;/delete&gt;
&lt;/mapper&gt;
</code></pre>
<p>写controller</p>
<pre><code class="language-java">package com.lin;

import com.lin.mapper.UserMapper;
import com.lin.pojo.User;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;

@RestController
public class UsercController {
    @Autowired
    //将mapper接口自动装配
    private UserMapper userMapper;

    @GetMapping(&quot;/queryList&quot;)
    public List&lt;User&gt; queryList(){
        List&lt;User&gt; userList = userMapper.queryList();
        for (User user : userList) {
            System.out.println(user);
        }
        return userList;
    }
    @GetMapping(&quot;/addUser&quot;)
    public String addUser(){
        userMapper.addUser(new User(9,&quot;阿毛&quot;,&quot;456789&quot;));
        return &quot;添加成功&quot;;
    }
    @GetMapping(&quot;/updateUser&quot;)
    public String updateUser(){
        userMapper.updateUser(new User(9,&quot;阿亮&quot;,&quot;987654&quot;));
        return &quot;修改成功&quot;;
    }

    @GetMapping(&quot;/deleteuser&quot;)
    public String deleteuser(){
        userMapper.deleteuser(9);
        return &quot;删除成功&quot;;
    }




}
</code></pre>
<p>最后进行测试</p>
<p>测试成功！</p>
<p>1、导入 MyBatis 所需要的依赖</p>
<p><strong>官方的依赖是以spring‘开头的</strong></p>
<pre><code class="language-xml">&lt;dependency&gt;    &lt;groupId&gt;org.mybatis.spring.boot&lt;/groupId&gt;    &lt;artifactId&gt;mybatis-spring-boot-starter&lt;/artifactId&gt;    &lt;version&gt;2.1.1&lt;/version&gt;&lt;/dependency&gt;
</code></pre>
<p>2、配置数据库连接信息（不变）</p>
<pre><code class="language-yaml">spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/mybatis?serverTimezone=UTC&amp;useUnicode=true&amp;characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    password:
    type: com.alibaba.druid.pool.DruidDataSource

    #Spring Boot 默认是不注入这些属性值的，需要自己绑定   
    #druid 数据源专有配置    
    initialSize: 5   
    minIdle: 5   
    maxActive: 20   
    maxWait: 60000  
    timeBetweenEvictionRunsMillis: 60000    
    minEvictableIdleTimeMillis: 300000    
    validationQuery: SELECT 1 FROM DUAL   
    testWhileIdle: true  
    testOnBorrow: false
    testOnReturn: false   
    poolPreparedStatements: true
    #配置监控统计拦截的filters，stat:监控统计、log4j：日志记录、wall：防御sql注入   
    #如果允许时报错  java.lang.ClassNotFoundException: org.apache.log4j.Priority   
    #则导入 log4j 依赖即可，Maven 地址：https://mvnrepository.com/artifact/log4j/log4j   
    filters: stat,wall,log4j    
    maxPoolPreparedStatementPerConnectionSize: 20    
    useGlobalDataSourceStat: true    connectionProperties: 
    druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
</code></pre>
<p><strong>3、测试数据库是否连接成功！</strong></p>
<p><strong>4、创建实体类，导入 Lombok！</strong></p>
<p>SQL语句</p>
<pre><code class="language-sql">CREATE DATABASE `mybatis`;

USE `mybatis`;

DROP TABLE IF EXISTS `Department`;

CREATE TABLE `Department` (
`id` int(20) NOT NULL,
`departmentName` varchar(30) DEFAULT NULL,
PRIMARY KEY (`id`)
)
insert  into `Department`(`id`,`departmentName`) values 
(101,'教学部'), (102,'市场部'),(103,'运营部'),(104,'教研部'),(105,'后勤部');

部门类
</code></pre>
<p>Department.java</p>
<pre><code class="language-java">package com.lin.pojo;
import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Department {
    private Integer id;    
    private String departmentName;
}
</code></pre>
<p><strong>5、创建mapper目录以及对应的 Mapper 接口</strong></p>
<p>DepartmentMapper.java</p>
<pre><code class="language-java">//@Mapper : 表示本类是一个 MyBatis 的 Mapper
@Mapper
@Repository
public interface DepartmentMapper {
    // 获取所有部门信息    
    List&lt;Department&gt; getDepartments();
    // 通过id获得部门    
Department getDepartment(Integer id);
}
</code></pre>
<p><strong>6、对应的Mapper映射文件</strong></p>
<p>DepartmentMapper.xml</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;
&lt;mapper namespace=&quot;com.lin.mapper.DepartmentMapper&quot;&gt;
    &lt;select id=&quot;getDepartments&quot; resultType=&quot;Department&quot;&gt;
        select * from mybatis.department
     &lt;/select&gt;
    &lt;select id=&quot;getDepartment&quot; resultType=&quot;Department&quot; parameterType=&quot;int&quot;&gt;
     select * from department where id = #{id}
      &lt;/select&gt;
&lt;/mapper&gt;
</code></pre>
<p><strong>7、maven配置资源过滤问题</strong></p>
<pre><code class="language-xml">&lt;resources&gt;   
    &lt;resource&gt;   
        &lt;directory&gt;src/main/java&lt;/directory&gt;   
        &lt;includes&gt;        
            &lt;include&gt;**/*.xml&lt;/include&gt;     
        &lt;/includes&gt;   
        &lt;filtering&gt;true&lt;/filtering&gt;  
    &lt;/resource&gt;
&lt;/resources&gt;
</code></pre>
<p><strong>8、编写部门的 DepartmentController 进行测试！</strong></p>
<pre><code class="language-java">@RestController
public class DepartmentController {
    @Autowired
    DepartmentMapper departmentMapper;
    // 查询全部部门
    @GetMapping(&quot;/getDepartments&quot;)
    public List&lt;Department&gt; getDepartments(){
        return departmentMapper.getDepartments();    }

    // 查询全部部门
    @GetMapping(&quot;/getDepartment/{id}&quot;)
    public Department getDepartment(@PathVariable(&quot;id&quot;) Integer id){
        return departmentMapper.getDepartment(id);
    }
}
</code></pre>
<p><strong>启动项目访问进行测试！</strong></p>
<p>测试成功</p>
<figure data-type="image" tabindex="73"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210302170326504.png" alt="image-20210302170326504" loading="lazy"></figure>
<h2 id="我们增加一个员工类再测试下为之后做准备">我们增加一个员工类再测试下，为之后做准备</h2>
<p>1、新建一个pojo类 Employee ；</p>
<pre><code class="language-java">package com.lin.pojo;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

import java.util.Date;

@Data
@AllArgsConstructor
@NoArgsConstructor
public class Employee {
    private Integer emid;
    private String lastName;
    private String email;
    //1 male, 0 female
    private Integer gender;
    private Integer department;
    private Date birth;
    private Department eDepartment;
    // 冗余设计
}
</code></pre>
<p>SQL语句</p>
<p>这里将id修改一下 避免混淆</p>
<pre><code class="language-mysql">USE `mybatis`;
CREATE TABLE `employee` 
(
`emid` int(30) NOT NULL,
`lastName` varchar(30) DEFAULT NULL,
`email` varchar(30) DEFAULT NULL,
`gender` int(30) DEFAULT NULL,
`department` int(30) NOT NULL,
`birth` varchar(30) DEFAULT null,
PRIMARY KEY (`emid`),#这里还要设置自增
foreign key (`department`) references mybatis.department(`id`)
);
insert  into `employee`(`emid`,`lastName`,`email`,`gender`,`department`,`birth`) values 
(1001,&quot;刘一&quot;,&quot;A1415877347@qq.com&quot;,0,101,&quot;1980-12-17&quot;),
(1002,&quot;王二&quot;,&quot;A1415877347@qq.com&quot;,1,102,&quot;1981-02-20&quot;),
(1003,&quot;潘三&quot;,&quot;A1415877347@qq.com&quot;,0,103,&quot;1987-06-13&quot;),
(1004,&quot;李四&quot;,&quot;A1415877347@qq.com&quot;,1,104,&quot;1981-09-08&quot;),
(1005,&quot;王五&quot;,&quot;A1415877347@qq.com&quot;,0,105,&quot;1981-04-02&quot;);
</code></pre>
<p>2、新建一个 EmployeeMapper 接口</p>
<pre><code class="language-java">//@Mapper : 表示本类是一个 MyBatis 的 Mapper
@Mapper
@Repositorypublic 
interface EmployeeMapper {
    // 获取所有员工信息    
    List&lt;Employee&gt; getEmployees();
    // 新增一个员工    
    int save(Employee employee);
    // 通过id获得员工信息  
    Employee get(Integer id);
    // 通过id删除员工  
    int delete(Integer id);
}
</code></pre>
<p>3、编写 EmployeeMapper.xml 配置文件</p>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; ?&gt;&lt;!DOCTYPE mapper        PUBLIC &quot;-//mybatis.org//DTD Mapper 3.0//EN&quot;        &quot;http://mybatis.org/dtd/mybatis-3-mapper.dtd&quot;&gt;
&lt;mapper namespace=&quot;com.lin.mapper.EmployeeMapper&quot;&gt;
    &lt;resultMap id=&quot;EmployeeMap&quot; type=&quot;Employee&quot;&gt;
        &lt;!-- property：在员工实体中部门实体的属性
           column: 员工表的外键字段，待会儿会传给第二个sql当条件
       --&gt;
        &lt;id property=&quot;emid&quot; column=&quot;emid&quot;/&gt;
        &lt;result property=&quot;lastName&quot; column=&quot;lastName&quot;/&gt;
        &lt;result property=&quot;email&quot; column=&quot;email&quot;/&gt;
        &lt;result property=&quot;gender&quot; column=&quot;gender&quot;/&gt;
        &lt;result property=&quot;department&quot; column=&quot;department&quot;/&gt;
        &lt;result property=&quot;birth&quot; column=&quot;birth&quot;/&gt;
        &lt;association property=&quot;eDepartment&quot;  javaType=&quot;Department&quot; column=&quot;department&quot; &gt;
            &lt;id property=&quot;id&quot; column=&quot;id&quot;/&gt;
            &lt;result property=&quot;departmentName&quot; column=&quot;departmentName&quot;/&gt;
        &lt;/association&gt;
    &lt;/resultMap&gt;
    &lt;select id=&quot;getEmployees&quot; resultMap=&quot;EmployeeMap&quot;&gt;
        select emid,lastName,email,gender,department,birth,id,departmentName
        from department d,employee e
        where d.id = e.department
    &lt;/select&gt;

    &lt;insert id=&quot;save&quot; parameterType=&quot;Employee&quot;&gt;
        insert into employee (lastName,email,gender,department,birth)
        values (#{lastName},#{email},#{gender},#{department},#{birth});
    &lt;/insert&gt;

    &lt;select id=&quot;get&quot; resultMap=&quot;EmployeeMap&quot;&gt;
        select emid,lastName,email,gender,department,birth,id,departmentName
        from department d,employee e
        where emid = #{emid}
    &lt;/select&gt;

    &lt;delete id=&quot;delete&quot; parameterType=&quot;int&quot;&gt;
        delete from employee where emid = #{emid}
    &lt;/delete&gt;

&lt;/mapper&gt;
</code></pre>
<p>4、编写EmployeeController类进行测试</p>
<pre><code class="language-java">package com.lin.controller;

import com.lin.mapper.EmployeeMapper;
import com.lin.pojo.Employee;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RestController;

import java.util.Date;
import java.util.List;

@RestController
public class EmployeeController {
    @Autowired
    EmployeeMapper employeeMapper;
    // 获取所有员工信息
    @GetMapping(&quot;/getEmployees&quot;)
    public List&lt;Employee&gt; getEmployees(){
        return employeeMapper.getEmployees();
    }

    @GetMapping(&quot;/save&quot;)
    public int save(){
        Employee employee = new Employee();
        employee.setLastName(&quot;lindamao&quot;);
        employee.setEmail(&quot;lindaomao@qq.com&quot;);
        employee.setGender(1);
        employee.setDepartment(101);
        employee.setBirth(new Date());
        return employeeMapper.save(employee);
    }
    // 通过id获得员工信息
    @GetMapping(&quot;/get/{emid}&quot;)
    public Employee get(@PathVariable(&quot;emid&quot;) Integer emid){

        return employeeMapper.get(id);
    }
    // 通过id删除员工
    @GetMapping(&quot;/delete/{emid}&quot;)
    public int delete(@PathVariable(&quot;emid&quot;) Integer emid){

        return employeeMapper.delete(emid);
    }
}
</code></pre>
<p>测试结果完成，搞定收功</p>
<h2 id="知识点">知识点！</h2>
<h3 id="mapper和repository注解的区别">@Mapper和@Repository注解的区别</h3>
<p>在用idea写一个实现类时引用了mapper类的来调用dao层的处理，使用@Autowired注解时被标红线，找不到bean。</p>
<figure data-type="image" tabindex="74"><img src="https://upload-images.jianshu.io/upload_images/11094343-a09ef3dc9d58c0cb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/625/format/webp" alt="img" loading="lazy"></figure>
<p>解决办法：在mapper加@mapper或者@repository注解。</p>
<p>这两种注解的区别在于：</p>
<p>1、使用@mapper后，不需要在spring配置中设置扫描地址，通过mapper.xml里面的namespace属性对应相关的mapper类，spring将动态的生成Bean后注入到ServiceImpl中。</p>
<p>2、@repository则需要在Spring中配置扫描包地址，然后生成dao层的bean，之后被注入到ServiceImpl中</p>
<p><strong>@Repository(value=&quot;userDao&quot;)</strong></p>
<p><strong>该注解是告诉Spring，让Spring创建一个名字叫“userDao”的UserDaoImpl实例。</strong></p>
<p><strong>当Service需要使用Spring创建的名字叫“userDao”的UserDaoImpl实例时，就可以使用@Resource(name = &quot;userDao&quot;)注解告诉Spring，Spring把创建好的userDao注入给Service即可。</strong></p>
<h3 id="mysql-的主键">mysql 的主键</h3>
<p>mysql 主键是什么？</p>
<p>主键（PRIMARY KEY），也称“<strong>主键约束</strong>”。</p>
<p><strong>MySQL主键约束是一个列或者多个列的组合，其值能唯一地标识表中的每一行</strong>。</p>
<p>这样的一列或多列称为表的主键，通过它可以强制表的<strong>实体完整性</strong>。</p>
<p>主键主要是用于其他表的外键关联，以及本记录的修改与删除。</p>
<p><strong>mysql 主键的作用</strong></p>
<p>1、主要的作用是确定该数据的<strong>唯一性</strong>。比如说ID=1,NAME=张三。我们要在数据库中，找到这条数据可以使用select * from 表 where id=1 这样就可以把张三查找出来了。而这个张三，也可以出现同名，所有用ID来做主键。</p>
<p>2、insert into 是插入操作。当ID设置为了主键，再插入一个相同的主键值，就为报错误，并不会更新，你想要个更新就必须执行UPDATE。</p>
<p>①<strong>PRIMAPY</strong>就是主键的意思，表示定义的该列值在表中是唯一的意思，不可以有重复。</p>
<p>②<strong>AUTO_INCREMENT可以理解为自动递增的意思，每增加一条记录，值会自动加1</strong>。</p>
<p>可能出现的问题</p>
<p>主键没有设置自增</p>
<figure data-type="image" tabindex="75"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210302211557934.png" alt="image-20210302211557934" loading="lazy"></figure>
<h3 id="map映射问题">map映射问题</h3>
<p>resultMap的用法以及关联结果集映射</p>
<p>正在努力的陈序员 2018-08-05 11:05:48  130027  收藏 304<br>
分类专栏： MyBatis 文章标签： 关联结果集映射 resultMap 一对多 一对一<br>
版权<br>
resultType<br>
resultType可以把查询结果封装到pojo类型中，但必须pojo类的属性名和查询到的数据库表的字段名一致。<br>
如果sql查询到的字段与pojo的属性名不一致，则需要使用resultMap将字段名和属性名对应起来，进行手动配置封装，将结果映射到pojo中</p>
<p>resultMap<br>
resultMap可以实现将查询结果映射为复杂类型的pojo，比如在查询结果映射对象中包括pojo和list实现一对一查询和一对多查询。</p>
<p>先在Mapper文件中，配置基本的sql语句</p>
<!-- 查询所有的订单数据 -->
<pre><code>&lt;!-- resultMap:填入配置的resultMap标签的id值 --&gt;
&lt;select id=&quot;queryOrderAll&quot; resultMap=&quot;orderResultMap&quot;&gt;
    SELECT id, user_id,
    number,
    createtime, note FROM `order`
&lt;/select&gt;
</code></pre>
<p>配置resultMap标签，映射不同的字段和属性名</p>
<!-- resultMap最终还是要将结果映射到pojo上，type就是指定映射到哪一个pojo -->
<pre><code>&lt;!-- id：设置ResultMap的id --&gt;
&lt;resultMap type=&quot;order&quot; id=&quot;orderResultMap&quot;&gt;
    &lt;!-- 定义主键 ,非常重要。如果是多个字段,则定义多个id --&gt;
    &lt;!-- property：主键在pojo中的属性名 --&gt;
    &lt;!-- column：主键在数据库中的列名 --&gt;
    &lt;id property=&quot;id&quot; column=&quot;id&quot; /&gt;

    &lt;!-- 定义普通属性 --&gt;
    &lt;result property=&quot;userId&quot; column=&quot;user_id&quot; /&gt;
    &lt;result property=&quot;number&quot; column=&quot;number&quot; /&gt;
    &lt;result property=&quot;createtime&quot; column=&quot;createtime&quot; /&gt;
    &lt;result property=&quot;note&quot; column=&quot;note&quot; /&gt;
&lt;/resultMap&gt;
</code></pre>
<p>结果就可以封装到pojo类型中</p>
<p>使用resultMap进行关联查询<br>
一对一查询<br>
一对一数据模型：订单用户<br>
一个订单信息只会是一个人下的订单，所以从查询订单信息出发关联查询用户信息为一对一查询。如果从用户信息出发查询用户下的订单信息则为一对多查询，因为一个用户可以下多个订单。</p>
<p>改造pojo类<br>
在订单类中添加User属性，User属性是一个引用类型，用于存储关联查询的用户信息，因为关联关系是一对一，所以只需要添加单个属性即可</p>
<p>配置Mapper.xml配置文件<br>
OrderMapper.xml<br>
先使用id和result属性，映射order类的结果集，然后在使用association映射关联对象User的结果集</p>
<pre><code>&lt;resultMap type=&quot;order&quot; id=&quot;orderUserResultMap&quot;&gt;
    &lt;id property=&quot;id&quot; column=&quot;id&quot; /&gt;
    &lt;result property=&quot;userId&quot; column=&quot;user_id&quot; /&gt;
    &lt;result property=&quot;number&quot; column=&quot;number&quot; /&gt;
    &lt;result property=&quot;createtime&quot; column=&quot;createtime&quot; /&gt;
    &lt;result property=&quot;note&quot; column=&quot;note&quot; /&gt;
</code></pre>
<pre><code>&lt;!-- association ：配置一对一属性 --&gt;
&lt;!-- property:order里面的User属性名 --&gt;
    &lt;!-- javaType:属性类型 --&gt;
&lt;association property=&quot;user&quot; javaType=&quot;user&quot;&gt;
    &lt;!-- id:声明主键，表示user_id是关联查询对象的唯一标识--&gt;
    &lt;id property=&quot;id&quot; column=&quot;user_id&quot; /&gt;
    &lt;result property=&quot;username&quot; column=&quot;username&quot; /&gt;
    &lt;result property=&quot;address&quot; column=&quot;address&quot; /&gt;
&lt;/association&gt;
</code></pre>
</resultMap>
<!-- 一对一关联，查询订单，订单内部包含用户属性 -->
<select id="queryOrderUserResultMap" resultMap="orderUserResultMap">
    SELECT
    o.id,
    o.user_id,
    o.number,
    o.createtime,
    o.note,
    u.username,
    u.address
    FROM
    `order` o
    LEFT JOIN `user` u ON o.user_id = u.id
</select>
<p>测试<br>
@Test<br>
public void testQueryOrderUserResultMap() {<br>
// mybatis和spring整合，整合之后，交给spring管理<br>
SqlSession sqlSession = this.sqlSessionFactory.openSession();<br>
// 创建Mapper接口的动态代理对象，整合之后，交给spring管理<br>
UserMapper userMapper = sqlSession.getMapper(UserMapper.class);<br>
// 使用userMapper执行根据条件查询用户，结果封装到Order类中<br>
List<Order> list = userMapper.queryOrderUserResultMap();<br>
for (Order o : list) {<br>
System.out.println(o);<br>
}<br>
// mybatis和spring整合，整合之后，交给spring管理<br>
sqlSession.close();<br>
结果<br>
一对多查询<br>
查询所有用户信息及相关订单。</p>
<p>修改pojo类，在pojo类添加订单集合属性</p>
<p>修改UserMapper.xml配置文件<br>
先使用id和result配置映射User类的结果，然后使用一对多关系的collection标签配置Order结果</p>
<resultMap type="user" id="userOrderResultMap">
    <id property="id" column="id" />
    <result property="username" column="username" />
    <result property="birthday" column="birthday" />
    <result property="sex" column="sex" />
    <result property="address" column="address" />
<pre><code>&lt;!-- 配置一对多的关系
    property：填写pojo类中集合类类属性的名称
    javaType：填写集合类型的名称 
--&gt;
&lt;collection property=&quot;orders&quot; javaType=&quot;list&quot; ofType=&quot;order&quot;&gt;
    &lt;!-- 配置主键，是关联Order的唯一标识 --&gt;
    &lt;id property=&quot;id&quot; column=&quot;oid&quot; /&gt;
    &lt;result property=&quot;number&quot; column=&quot;number&quot; /&gt;
    &lt;result property=&quot;createtime&quot; column=&quot;createtime&quot; /&gt;
    &lt;result property=&quot;note&quot; column=&quot;note&quot; /&gt;
&lt;/collection&gt;
</code></pre>
</resultMap>
<!-- 一对多关联，查询订单同时查询该用户下的订单 -->
<select id="queryUserOrder" resultMap="userOrderResultMap">
    SELECT
    u.id,
    u.username,
    u.birthday,
    u.sex,
    u.address,
    o.id oid,
    o.number,
    o.createtime,
    o.note
    FROM
    `user` u
    LEFT JOIN `order` o ON u.id = o.user_id
</select>
<p>测试结果# SpringBoot10：Web开发静态资源处理</p>
<h2 id="简介">简介</h2>
<p>好的，同学们，那么接下来呢，我们开始学习SpringBoot与Web开发，从这一章往后，就属于我们实战部分的内容了；</p>
<p>其实SpringBoot的东西用起来非常简单，因为SpringBoot最大的特点就是自动装配。</p>
<p><strong>使用SpringBoot的步骤：</strong></p>
<p>1、创建一个SpringBoot应用，选择我们需要的模块，SpringBoot就会默认将我们的需要的模块自动配置好</p>
<p>2、手动在配置文件中配置部分配置项目就可以运行起来了</p>
<p>3、专注编写业务代码，不需要考虑以前那样一大堆的配置了。</p>
<p>要熟悉掌握开发，之前学习的自动配置的原理一定要搞明白！</p>
<p>比如SpringBoot到底帮我们配置了什么？我们能不能修改？我们能修改哪些配置？我们能不能扩展？</p>
<ul>
<li>向容器中自动配置组件 ：*** Autoconfiguration</li>
<li>自动配置类，封装配置文件的内容：***Properties</li>
</ul>
<p>没事就找找类，看看自动装配原理！</p>
<p>我们之后来进行一个单体项目的小项目测试，让大家能够快速上手开发！</p>
<h2 id="springboot-web开发">Springboot Web开发</h2>
<p>要解决的问题：</p>
<ul>
<li>导入静态资源....</li>
<li>首页</li>
<li>jsp，模块引擎Thymeleaf</li>
<li>装配拓展springMVC</li>
<li>增删改查</li>
<li>拦截器</li>
<li>国际化！</li>
</ul>
<p><strong>静态资源处理</strong></p>
<h2 id="静态资源映射规则">静态资源映射规则</h2>
<p><strong>首先，我们搭建一个普通的SpringBoot项目，回顾一下HelloWorld程序！</strong></p>
<p>写请求非常简单，那我们要引入我们前端资源，我们项目中有许多的静态资源，比如css，js等文件，这个SpringBoot怎么处理呢？</p>
<p>如果我们是一个web应用，我们的main下会有一个webapp，我们以前都是将所有的页面导在这里面的，对吧！但是我们现在的pom呢，打包方式是为jar的方式，那么这种方式SpringBoot能不能来给我们写页面呢？当然是可以的，但是SpringBoot对于静态资源放置的位置，是有规定的！</p>
<p><strong>我们先来聊聊这个静态资源映射规则：</strong></p>
<p>SpringBoot中，SpringMVC的web配置都在 WebMvcAutoConfiguration 这个配置类里面；</p>
<p>我们可以去看看 WebMvcAutoConfigurationAdapter 中有很多配置方法；</p>
<p>有一个方法：addResourceHandlers 添加资源处理</p>
<p>分析一下这一段代码</p>
<pre><code class="language-java">@Override
public void addResourceHandlers(ResourceHandlerRegistry registry) {    if (!this.resourceProperties.isAddMappings()) {        // 已禁用默认资源处理 
    logger.debug(&quot;Default resource handling disabled&quot;);   
    return;   
}   
                                                               
             // 缓存控制  
                                                                            Duration cachePeriod = this.resourceProperties.getCache().getPeriod();    CacheControl cacheControl = this.resourceProperties.getCache().getCachecontrol().toHttpCacheControl();   
                                                                            // webjars 配置  
                                                                            if (!registry.hasMappingForPattern(&quot;/webjars/**&quot;)) {        customizeResourceHandlerRegistration(registry.addResourceHandler(&quot;/webjars/**&quot;)                                             .addResourceLocations(&quot;classpath:/META-INF/resources/webjars/&quot;)                                             .setCachePeriod(getSeconds(cachePeriod)).setCacheControl(cacheControl));    }    
                                                                            // 静态资源配置   
                                                                            String staticPathPattern = this.mvcProperties.getStaticPathPattern();    if (!registry.hasMappingForPattern(staticPathPattern)) {        customizeResourceHandlerRegistration(registry.addResourceHandler(staticPathPattern)                                             .addResourceLocations(getResourceLocations(this.resourceProperties.getStaticLocations()))                                             .setCachePeriod(getSeconds(cachePeriod)).setCacheControl(cacheControl)); 
                                                                            }
                                                                           }
</code></pre>
<p>读一下源代码：比如所有的 /webjars/** ，</p>
<h2 id="什么是webjars-呢">什么是webjars 呢？</h2>
<p>Webjars本质就是以jar包的方式引入我们的静态资源 ， 我们以前要导入一个静态资源文件，直接导入即可。</p>
<p>使用SpringBoot需要使用Webjars，我们可以去搜索一下：</p>
<p>网站：https://www.webjars.org</p>
<p>这里我们导入jquery</p>
<p>要使用jQuery，我们只要要引入jQuery对应版本的pom依赖即可！.</p>
<p>版本会更新</p>
<pre><code class="language-xml">&lt;dependency&gt;
    &lt;groupId&gt;org.webjars&lt;/groupId&gt;
    &lt;artifactId&gt;jquery&lt;/artifactId&gt;
    &lt;version&gt;3.5.1&lt;/version&gt;
&lt;/dependency&gt;
</code></pre>
<p>导入完毕，查看webjars目录结构，并访问Jquery.js文件！</p>
<figure data-type="image" tabindex="76"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222210653904.png" alt="image-20210222210653904" loading="lazy"></figure>
<p><strong>都需要去 classpath:/META-INF/resources/webjars/ 找对应的资源；</strong></p>
<figure data-type="image" tabindex="77"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222210816328.png" alt="image-20210222210816328" loading="lazy"></figure>
<p>重启服务器</p>
<p>访问：只要是静态资源，SpringBoot就会去对应的路径寻找资源，我们这里访问：http://localhost:8080/webjars/jquery/3.4.1/jquery.js</p>
<p>既可以获取对应的资源</p>
<figure data-type="image" tabindex="78"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222211111307.png" alt="image-20210222211111307" loading="lazy"></figure>
<p>访问成功！<img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></p>
<h2 id="第二种静态资源映射规则">第二种静态资源映射规则</h2>
<p>那我们项目中要是使用自己的静态资源该怎么导入呢？我们看下一行代码；</p>
<p>我们去找staticPathPattern发现第二种映射规则 ：/** , 访问当前的项目任意资源，它会去找 resourceProperties 这个类，我们可以点进去看一下分析：</p>
<figure data-type="image" tabindex="79"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222211540754.png" alt="image-20210222211540754" loading="lazy"></figure>
<figure data-type="image" tabindex="80"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222211913488.png" alt="image-20210222211913488" loading="lazy"></figure>
<figure data-type="image" tabindex="81"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222212029782.png" alt="image-20210222212029782" loading="lazy"></figure>
<figure data-type="image" tabindex="82"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222212105226.png" alt="image-20210222212105226" loading="lazy"></figure>
<pre><code class="language-java">// 进入方法
public String[] getStaticLocations() {    
    return this.staticLocations;}
// 找到对应的值
private String[] staticLocations = CLASSPATH_RESOURCE_LOCATIONS;
// 找到路径
private static final String[] CLASSPATH_RESOURCE_LOCATIONS = {     &quot;classpath:/META-INF/resources/&quot;,  &quot;classpath:/resources/&quot;,     &quot;classpath:/static/&quot;,     &quot;classpath:/public/&quot; }
;
</code></pre>
<p>ResourceProperties 可以设置和我们静态资源有关的参数；这里面指向了它会去寻找资源的文件夹，即上面数组的内容。</p>
<p>点击查看</p>
<figure data-type="image" tabindex="83"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222212310011.png" alt="image-20210222212310011" loading="lazy"></figure>
<p>所以得出结论，以下四个目录存放的静态资源可以被我们识别：</p>
<p>在2.4.1版本  这个静态数组被放到了ResourceProperties的父类Resources中</p>
<figure data-type="image" tabindex="84"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222212650392.png" alt="image-20210222212650392" loading="lazy"></figure>
<pre><code>&quot;classpath:/META-INF/resources/&quot;&quot;classpath:/resources/&quot;&quot;classpath:/static/&quot;&quot;classpath:/public/&quot;
</code></pre>
<p>他们各自分别对应不同的路径</p>
<figure data-type="image" tabindex="85"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222213115968.png" alt="image-20210222213115968" loading="lazy"></figure>
<figure data-type="image" tabindex="86"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222213446856.png" alt="image-20210222213446856" loading="lazy"></figure>
<p>我们可以在resources根目录下新建对应的文件夹，都可以存放我们的静态文件；</p>
<p>比如我们访问 http://localhost:8080/1.js ,</p>
<p>他就会去这些文件夹中寻找对应的静态资源文件；</p>
<p>在三个都建立了相同的文件情况下 其访问的优先级为</p>
<figure data-type="image" tabindex="87"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222214056969.png" alt="image-20210222214056969" loading="lazy"></figure>
<p><strong>static是默认本来就有的</strong></p>
<h2 id="自定义静态资源路径">自定义静态资源路径</h2>
<p>我们也可以自己通过配置文件来指定一下，哪些文件夹是需要我们放静态资源文件的，在application.properties中配置；</p>
<pre><code class="language-properties">spring.resources.static-locations=classpath:/coding/,classpath:/lin/
</code></pre>
<p>一旦自己定义了静态文件夹的路径，原来的自动配置就都会失效了！</p>
<p>且在配置文件中会出现</p>
<figure data-type="image" tabindex="88"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222214429129.png" alt="image-20210222214429129" loading="lazy"></figure>
<p>再次访问刚才的路径就会出现页面找不的情况</p>
<figure data-type="image" tabindex="89"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222214512565.png" alt="image-20210222214512565" loading="lazy"></figure>
<p>因此这种情况也是不推荐使用的</p>
<h2 id="首页处理">首页处理</h2>
<p>静态资源文件夹说完后，我们继续向下看源码！可以看到一个欢迎页的映射，就是我们的首页！</p>
<figure data-type="image" tabindex="90"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222215115087.png" alt="image-20210222215115087" loading="lazy"></figure>
<figure data-type="image" tabindex="91"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210222215231071.png" alt="image-20210222215231071" loading="lazy"></figure>
<p>说明这个在刚才的那三个目录当中任意放一个都能访问的到</p>
<p>但是建议是放在static下面</p>
<pre><code class="language-java">@Bean
public WelcomePageHandlerMapping welcomePageHandlerMapping(ApplicationContext applicationContext,                                                           FormattingConversionService mvcConversionService,                                                           ResourceUrlProvider mvcResourceUrlProvider) {    WelcomePageHandlerMapping welcomePageHandlerMapping = new WelcomePageHandlerMapping(        new TemplateAvailabilityProviders(applicationContext), applicationContext, getWelcomePage(),
// getWelcomePage 获得欢迎页        
this.mvcProperties.getStaticPathPattern());    welcomePageHandlerMapping.setInterceptors(getInterceptors(mvcConversionService, mvcResourceUrlProvider));   
return welcomePageHandlerMapping;
}
</code></pre>
<p>点进去继续看</p>
<pre><code class="language-java">private Optional&lt;Resource&gt; getWelcomePage() {    String[] locations = getResourceLocations(this.resourceProperties.getStaticLocations());    
                                             // ::是java8 中新引入的运算符    
                                             // Class::function的时候function是属于Class的，应该是静态方法。    
                                             // this::function的funtion是属于这个对象的。    
                                             // 简而言之，就是一种语法糖而已，是一种简写    
                                             return Arrays.stream(locations).map(this::getIndexHtml).filter(this::isReadable).findFirst();}
// 欢迎页就是一个location下的的 index.html 而已
private Resource getIndexHtml(String location) {    return this.resourceLoader.getResource(location + &quot;index.html&quot;);
                                               }
</code></pre>
<p>欢迎页，静态资源文件夹下的所有 index.html 页面；被 /** 映射。</p>
<p>比如我访问  http://localhost:8080/ ，就会找静态资源文件夹下的 index.html</p>
<p>新建一个 index.html ，在我们上面的3个目录中任意一个；然后访问测试  http://localhost:8080/  看结果！</p>
<p><strong>关于网站图标说明</strong>：</p>
<p>与其他静态资源一样，Spring Boot在配置的静态内容位置中查找 favicon.ico。如果存在这样的文件，它将自动用作应用程序的favicon。</p>
<p>1、关闭SpringBoot默认图标</p>
<pre><code>#关闭默认图标spring.mvc.favicon.enabled=false
</code></pre>
<p>2、自己放一个图标在静态资源目录下，我放在 public 目录下</p>
<p>3、清除浏览器缓存！刷新网页，发现图标已经变成自己的了！</p>
<p>这个好像只有旧版的才能使用.....</p>
<figure data-type="image" tabindex="92"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<h1 id="springboot11thymeleaf模板引擎">SpringBoot11：Thymeleaf模板引擎</h1>
<p><strong>Thymeleaf</strong></p>
<h2 id="模板引擎">模板引擎</h2>
<p>前端交给我们的页面，是html页面。如果是我们以前开发，我们需要把他们转成jsp页面，jsp好处就是当我们查出一些数据转发到JSP页面以后，我们可以用jsp轻松实现数据的显示，及交互等。</p>
<p>jsp支持非常强大的功能，包括能写Java代码，但是呢，我们现在的这种情况，SpringBoot这个项目首先是以jar的方式，不是war，像第二，我们用的还是嵌入式的Tomcat，所以呢，<strong>他现在默认是不支持jsp的</strong>。</p>
<p>那不支持jsp，如果我们直接用纯静态页面的方式，那给我们开发会带来非常大的麻烦，那怎么办呢？</p>
<p><strong>SpringBoot推荐你可以来使用模板引擎：</strong></p>
<p>模板引擎，我们其实大家听到很多，其实jsp就是一个模板引擎，还有用的比较多的freemarker，包括SpringBoot给我们推荐的Thymeleaf，模板引擎有非常多，但再多的模板引擎，他们的思想都是一样的，什么样一个思想呢我们来看一下这张图：</p>
<figure data-type="image" tabindex="93"><img src="https://mmbiz.qpic.cn/mmbiz_png/uJDAUKrGC7Idia351qHgmH2vbzurk1Pp6V42bcomyzTYY0q6ic7AB8lvciaoicxyalNYQYZgslIrIjdXWLFNcOxUmQ/640?wx_fmt=png&amp;tp=webp&amp;wxfrom=5&amp;wx_lazy=1&amp;wx_co=1" alt="图片" loading="lazy"></figure>
<p>模板引擎的作用就是我们来写一个页面模板，比如有些值呢，是动态的，我们写一些表达式。而这些值，从哪来呢，就是我们在后台封装一些数据。然后把这个模板和这个数据交给我们模板引擎，模板引擎按照我们这个数据帮你把这表达式解析、填充到我们指定的位置，然后把这个数据最终生成一个我们想要的内容给我们写出去，这就是我们这个模板引擎，不管是jsp还是其他模板引擎，都是这个思想。只不过呢，就是说不同模板引擎之间，他们可能这个语法有点不一样。其他的我就不介绍了，我主要来介绍一下SpringBoot给我们推荐的Thymeleaf模板引擎，这模板引擎呢，是一个高级语言的模板引擎，他的这个语法更简单。而且呢，功能更强大。</p>
<p>我们呢，就来看一下这个模板引擎，那既然要看这个模板引擎。首先，我们来看SpringBoot里边怎么用。</p>
<h2 id="引入thymeleaf">引入Thymeleaf</h2>
<p>怎么引入呢，对于springboot来说，什么事情不都是一个start的事情嘛，我们去在项目中引入一下。给大家三个网址：</p>
<p>Thymeleaf 官网：https://www.thymeleaf.org/</p>
<p>Thymeleaf 在Github 的主页：https://github.com/thymeleaf/thymeleaf</p>
<p>Spring官方文档：找到我们对应的版本</p>
<p>https://docs.spring.io/spring-boot/docs/2.2.5.RELEASE/reference/htmlsingle/#using-boot-starter</p>
<p>找到对应的pom依赖：可以适当点进源码看下本来的包！</p>
<pre><code class="language-xml">&lt;!--thymeleaf--&gt;
&lt;dependency&gt;    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;    &lt;artifactId&gt;spring-boot-starter-thymeleaf
    &lt;/artifactId&gt;
&lt;/dependency&gt;
</code></pre>
<p>Maven会自动下载jar包，我们可以去看下下载的东西；</p>
<figure data-type="image" tabindex="94"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223205455333.png" alt="image-20210223205455333" loading="lazy"></figure>
<p>对应的版本随着父工程的版本而改变 如果版本对应不上就会报错</p>
<h2 id="thymeleaf分析">Thymeleaf分析</h2>
<p>前面呢，我们已经引入了Thymeleaf，那这个要怎么使用呢？</p>
<p>我们首先得按照SpringBoot的自动配置原理看一下我们这个Thymeleaf的自动配置规则，在按照那个规则，我们进行使用。</p>
<p>crtl+f4 才是刷新</p>
<pre><code class="language-java">public class ThymeleafProperties {
    private static final Charset DEFAULT_ENCODING;
    public static final String DEFAULT_PREFIX = &quot;classpath:/templates/&quot;;
    public static final String DEFAULT_SUFFIX = &quot;.html&quot;;
    private boolean checkTemplate = true;
    private boolean checkTemplateLocation = true;
    private String prefix = &quot;classpath:/templates/&quot;;
    private String suffix = &quot;.html&quot;;
    private String mode = &quot;HTML&quot;;
    private Charset encoding;
    }
</code></pre>
<p>我们去找一下Thymeleaf的自动配置类：ThymeleafProperties</p>
<p>点击查看</p>
<figure data-type="image" tabindex="95"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223205649421.png" alt="image-20210223205649421" loading="lazy"></figure>
<p>我们可以在其中看到默认的前缀和后缀！</p>
<p>我们只需要把我们的html页面放在类路径下的templates下，thymeleaf就可以帮我们自动渲染了。</p>
<p>使用thymeleaf什么都不需要配置，只需要将他放在指定的文件夹下即可！</p>
<p><strong>测试</strong></p>
<p>1、编写一个TestController</p>
<pre><code class="language-java">@Controllerpublic 
class TestController {        @RequestMapping(&quot;/t1&quot;)    public String test1(){        //classpath:/templates/test.html 
    return &quot;test&quot;;    }    }
</code></pre>
<p>2、编写一个测试页面  test.html 放在 templates 目录下</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot;&gt;&lt;head&gt;    &lt;meta charset=&quot;UTF-8&quot;&gt;    &lt;title&gt;Title&lt;/title&gt;&lt;/head&gt;&lt;body&gt;&lt;h1&gt;测试页面&lt;/h1&gt;
&lt;/body&gt;&lt;/html&gt;
</code></pre>
<p>3、启动项目请求测试</p>
<figure data-type="image" tabindex="96"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223210031783.png" alt="image-20210223210031783" loading="lazy"></figure>
<p>测试成功！</p>
<h2 id="thymeleaf-语法学习">Thymeleaf 语法学习</h2>
<p>要学习语法，还是参考官网文档最为准确，我们找到对应的版本看一下；</p>
<p>Thymeleaf 官网：https://www.thymeleaf.org/ ， 简单看一下官网！我们去下载Thymeleaf的官方文档！</p>
<p><strong>我们做个最简单的练习 ：我们需要查出一些数据，在页面中展示</strong></p>
<p>1、修改测试请求，增加数据传输；</p>
<pre><code class="language-java">@RequestMapping(&quot;/t1&quot;)
public String test1(Model model){    
    //存入数据    
    model.addAttribute(&quot;msg&quot;,&quot;Hello,Thymeleaf&quot;);    //classpath:/templates/test.html    
    return &quot;test&quot;;
}
</code></pre>
<p>2、我们要使用thymeleaf，<strong>需要在html文件中导入命名空间的约束，方便提示</strong>。</p>
<p>我们可以去官方文档的#3中看一下命名空间拿来过来：</p>
<pre><code class="language-html"> xmlns:th=&quot;http://www.thymeleaf.org&quot;
</code></pre>
<p>3、我们去编写下前端页面</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;&lt;html lang=&quot;en&quot; xmlns:th=&quot;http://www.thymeleaf.org&quot;&gt;&lt;head&gt;    
    &lt;meta charset=&quot;UTF-8&quot;&gt;   
    &lt;title&gt;狂神说&lt;/title&gt;&lt;/head&gt;&lt;body&gt;
    &lt;h1&gt;测试页面&lt;/h1&gt;
&lt;!--th:text就是将div中的内容设置为它指定的值，和之前学习的Vue一样--&gt;
    &lt;div th:text=&quot;${msg}&quot;&gt;&lt;/div&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>4、启动测试！</p>
<figure data-type="image" tabindex="97"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223210708574.png" alt="img" loading="lazy"></figure>
<p><strong>OK，入门搞定，我们来认真研习一下Thymeleaf的使用语法！</strong></p>
<p><strong>1、我们可以使用任意的 th:attr 来替换Html中原生属性的值！</strong></p>
<p><strong>2、我们能写哪些表达式呢？</strong></p>
<pre><code>Simple expressions:
    Variable Expressions: ${...}
    Selection Variable Expressions: *{...}
    Message Expressions: #{...}
    Link URL Expressions: @{...}
    Fragment Expressions: ~{...}
Literals
    Text literals: 'one text', 'Another one!',…
    Number literals: 0, 34, 3.0, 12.3,…
    Boolean literals: true, false
    Null literal: null
    Literal tokens: one, sometext, main,…
Text operations:
String concatenation: +
Literal substitutions: |The name is ${name}|
Arithmetic operations:
    Binary operators: +, -, *, /, %
    Minus sign (unary operator): -
Boolean operations:
    Binary operators: and, or
    Boolean negation (unary operator): !, not
Comparisons and equality:
    Comparators: &gt;, &lt;, &gt;=, &lt;= (gt, lt, ge, le)
    Equality operators: ==, != (eq, ne)
Conditional operators:
    If-then: (if) ? (then)
    If-then-else: (if) ? (then) : (else)
    Default: (value) ?: (defaultvalue)
Special tokens:
	No-Operation: _
</code></pre>
<p><strong>练习测试：</strong></p>
<p>1、 我们编写一个Controller，放一些数据</p>
<pre><code class="language-java">@RequestMapping(&quot;/t2&quot;)public String test2(Map&lt;String,Object&gt; map){    //存入数据   
    map.put(&quot;msg&quot;,&quot;&lt;h1&gt;Hello&lt;/h1&gt;&quot;);   
    map.put(&quot;users&quot;, Arrays.asList(&quot;qinjiang&quot;,&quot;kuangshen&quot;));    //classpath:/templates/test.html    
    return &quot;test&quot;;
}
</code></pre>
<p>2、测试页面取出数据</p>
<pre><code class="language-html">&lt;!DOCTYPE html&gt;
&lt;html lang=&quot;en&quot;  xmlns:th=&quot;http://www.thymeleaf.org&quot;&gt;
&lt;head&gt;
    &lt;meta charset=&quot;UTF-8&quot;&gt;
    &lt;title&gt;测试&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;h1&gt;测试页面&lt;/h1&gt;
&lt;!--th:text就是将div中的内容设置为它指定的值，和之前学习的Vue一样--&gt;
&lt;div th:text=&quot;${msg}&quot;&gt;&lt;/div&gt;
&lt;!--不转义 即是会输出里面的格式--&gt;
&lt;div th:utext=&quot;${msg}&quot;&gt;&lt;/div&gt;
&lt;!--遍历数据--&gt;&lt;!--th:each每次遍历都会生成当前这个标签：官网#9--&gt;
&lt;h3 th:each=&quot;user:${users}&quot; th:text=&quot;${user}&quot;&gt;&lt;/h3&gt;
&lt;!--行内写法：官网#12--&gt;

    &lt;h3 th:each=&quot;user:${users}&quot;&gt;[[${user}]]&lt;/h3&gt;

&lt;/body&gt;
&lt;/html&gt;
</code></pre>
<p>这里要注意如果页面里面的东西写错了会出现500的错误</p>
<p>3、启动项目测试！</p>
<p><strong>我们看完语法，很多样式，我们即使现在学习了，也会忘记，所以我们在学习过程中，需要使用什么，根据官方文档来查询，才是最重要的，要熟练使用官方文档！</strong></p>
<p>sts热部署，即是在项目中修改代码不用重新启动服务，提高效率。</p>
<p>方法如下：</p>
<p>1.在pom文件中引入  devtools  依赖：</p>
<p></p>
<dependency>
　　<groupId>org.springframework.boot</groupId>
　　<artifactId>spring-boot-devtools</artifactId>
　　<!-- optional=true, 依赖不会传递, 该项目依赖devtools; 之后依赖boot项目的项目如果想要使用devtools, 需要重新引入 -->
　　<optional>true</optional>
</dependency>
<p>2.在application.properties  文件中码上以下内容：</p>
<p>spring.thymeleaf.cache=true        //缓存<br>
spring.devtools.restart.enabled=true   //开启<br>
spring.devtools.restart.additional-paths=src/main/java  //监听目录</p>
<h1 id="springboot-12mvc自动配置原理">SpringBoot 12：MVC自动配置原理</h1>
<p><strong>MVC自动配置原理</strong></p>
<h2 id="官网阅读">官网阅读</h2>
<p>在进行项目编写前，我们还需要知道一个东西，就是SpringBoot对我们的SpringMVC还做了哪些配置，包括如何扩展，如何定制。</p>
<p>只有把这些都搞清楚了，我们在之后使用才会更加得心应手。途径一：源码分析，途径二：官方文档！</p>
<p>地址 ：https://docs.spring.io/spring-boot/docs/2.2.5.RELEASE/reference/htmlsingle/#boot-features-spring-mvc-auto-configuration</p>
<p>Spring MVC Auto-configuration<br>
// Spring Boot为Spring MVC提供了自动配置，它可以很好地与大多数应用程序一起工作。Spring Boot provides auto-configuration for Spring MVC that works well with most applications.</p>
<p>// 自动配置在Spring默认设置的基础上添加了以下功能：The auto-configuration adds the following features on top of Spring’s defaults:</p>
<p>// 包含视图解析器Inclusion of ContentNegotiatingViewResolver and BeanNameViewResolver beans.</p>
<p>// 支持静态资源文件夹的路径，以及webjarsSupport for serving static resources, including support for WebJars</p>
<p>// 自动注册了Converter：</p>
<p>// 转换器，这就是我们网页提交数据到后台自动封装成为对象的东西，比如把&quot;1&quot;字符串自动转换为int类型</p>
<p>// Formatter：【格式化器，比如页面给我们了一个2019-8-10，它会给我们自动格式化为Date对象】Automatic registration of Converter, GenericConverter, and Formatter beans.</p>
<p>// HttpMessageConverters</p>
<p>// SpringMVC用来转换Http请求和响应的的，比如我们要把一个User对象转换为JSON字符串，可以去看官网文档解释；Support for HttpMessageConverters (covered later in this document).</p>
<p>// 定义错误代码生成规则的Automatic registration of MessageCodesResolver (covered later in this document).</p>
<p>// 首页定制Static index.html support.</p>
<p>// 图标定制Custom Favicon support (covered later in this document).</p>
<p>// 初始化数据绑定器：帮我们把请求数据绑定到JavaBean中！Automatic use of a ConfigurableWebBindingInitializer bean (covered later in this document).<br>
/<em>如果您希望保留Spring Boot MVC功能，并且希望添加其他MVC配置（拦截器、格式化程序、视图控制器和其他功能），则可以添加自己的@configuration类，类型为webmvcconfiguer，但不添加@EnableWebMvc。如果希望提供RequestMappingHandlerMapping、RequestMappingHandlerAdapter或ExceptionHandlerExceptionResolver的自定义实例，则可以声明WebMVCregistrationAdapter实例来提供此类组件。</em>/If you want to keep Spring Boot MVC features and you want to add additional MVC configuration (interceptors, formatters, view controllers, and other features), you can add your own @Configuration class of type WebMvcConfigurer but without @EnableWebMvc. If you wish to provide custom instances of RequestMappingHandlerMapping, RequestMappingHandlerAdapter, or ExceptionHandlerExceptionResolver, you can declare a WebMvcRegistrationsAdapter instance to provide such components.<br>
// <strong>如果您想完全控制Spring MVC</strong>，可以添加自己的**@Configuration**，并用@EnableWebMvc进行注释。If you want to take complete control of Spring MVC, you can add your own @Configuration annotated with @EnableWebMvc.</p>
<p>我们来仔细对照，看一下它怎么实现的，它告诉我们SpringBoot已经帮我们自动配置好了SpringMVC，然后自动配置了哪些东西呢？</p>
<h2 id="contentnegotiatingviewresolver-内容协商视图解析器"><strong>ContentNegotiatingViewResolver 内容协商视图解析器</strong></h2>
<p><strong>自动配置了ViewResolver，就是我们之前学习的SpringMVC的视图解析器</strong>；</p>
<p>即根据方法的返回值取得视图对象（View），然后由视图对象决定如何渲染（转发，重定向）。</p>
<p>我们去看看这里的源码：我们找到 WebMvcAutoConfiguration ， 然后搜索ContentNegotiatingViewResolver。找到如下方法！</p>
<pre><code>@Bean@ConditionalOnBean(ViewResolver.class)@ConditionalOnMissingBean(name = &quot;viewResolver&quot;, value = ContentNegotiatingViewResolver.class)public ContentNegotiatingViewResolver viewResolver(BeanFactory beanFactory) {    ContentNegotiatingViewResolver resolver = new ContentNegotiatingViewResolver();    resolver.setContentNegotiationManager(beanFactory.getBean(ContentNegotiationManager.class));    // ContentNegotiatingViewResolver使用所有其他视图解析器来定位视图，因此它应该具有较高的优先级    resolver.setOrder(Ordered.HIGHEST_PRECEDENCE);    return resolver;}
</code></pre>
<p>我们可以点进这类看看！找到对应的解析视图的代码；</p>
<p>@Nullable // 注解说明：@Nullable 即参数可为nullpublic View resolveViewName(String viewName, Locale locale) throws Exception {    RequestAttributes attrs = RequestContextHolder.getRequestAttributes();    Assert.state(attrs instanceof ServletRequestAttributes, &quot;No current ServletRequestAttributes&quot;);    List<MediaType> requestedMediaTypes = this.getMediaTypes(((ServletRequestAttributes)attrs).getRequest());    if (requestedMediaTypes != null) {        // 获取候选的视图对象        List<View> candidateViews = this.getCandidateViews(viewName, locale, requestedMediaTypes);        // 选择一个最适合的视图对象，然后把这个对象返回        View bestView = this.getBestView(candidateViews, requestedMediaTypes, attrs);        if (bestView != null) {            return bestView;        }    }    // .....}</p>
<p>我们继续点进去看，他是怎么获得候选的视图的呢？</p>
<p>getCandidateViews中看到他是把所有的视图解析器拿来，进行while循环，挨个解析！</p>
<pre><code>Iterator var5 = this.viewResolvers.iterator();
</code></pre>
<p>所以得出结论：<strong>ContentNegotiatingViewResolver 这个视图解析器就是用来组合所有的视图解析器的</strong></p>
<p>我们再去研究下他的组合逻辑，看到有个属性viewResolvers，看看它是在哪里进行赋值的！</p>
<pre><code class="language-java">protected void initServletContext(ServletContext servletContext) {    
    // 这里它是从beanFactory工具中获取容器中的所有视图解析器    // ViewRescolver.class 把所有的视图解析器来组合的    
    Collection&lt;ViewResolver&gt; matchingBeans = BeanFactoryUtils.beansOfTypeIncludingAncestors(this.obtainApplicationContext(), ViewResolver.class).values();   
    ViewResolver viewResolver;    
    if (this.viewResolvers == null) {    
        this.viewResolvers = new ArrayList(matchingBeans.size());  
    }   
    // ...............
}
</code></pre>
<p>既然它是在容器中去找视图解析器，我们是否可以猜想，我们就可以去实现一个视图解析器了呢？</p>
<p>我们可以自己给容器中去添加一个视图解析器；这个类就会帮我们自动的将它组合进来；<strong>我们去实现一下</strong></p>
<p>1、我们在我们的主程序中去写一个视图解析器来试试；</p>
<pre><code class="language-java">@Bean 
//放到bean中
public ViewResolver myViewResolver(){    
    return new MyViewResolver();
}
//我们写一个静态内部类，视图解析器就需要实现ViewResolver接口
private static class MyViewResolver implements ViewResolver{   
    @Override   
    public View resolveViewName(String s, Locale locale) throws Exception {      
        return null;  
    }
}
</code></pre>
<p>2、怎么看我们自己写的视图解析器有没有起作用呢？</p>
<p>我们给 DispatcherServlet 中的 doDispatch方法 加个断点进行调试一下，因为所有的请求都会走到这个方法中</p>
<p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223230159597.png" alt="image-20210223230159597" loading="lazy"></p>
<p>3、我们启动我们的项目，然后随便访问一个页面，看一下Debug信息；</p>
<p>找到this<img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223230211038.png" alt="image-20210223230211038" loading="lazy"></p>
<figure data-type="image" tabindex="98"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>找到视图解析器，我们看到我们自己定义的就在这里了；</p>
<figure data-type="image" tabindex="99"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223230222976.png" alt="image-20210223230222976" loading="lazy"></figure>
<figure data-type="image" tabindex="100"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>所以说，我们如果想要使用自己定制化的东西，我们只需要给容器中添加这个组件就好了！剩下的事情SpringBoot就会帮我们做了！</p>
<h2 id="转换器和格式化器">转换器和格式化器</h2>
<p>找到格式化转换器：</p>
<pre><code class="language-java">@Bean
@Override
public FormattingConversionService mvcConversionService() {    // 拿到配置文件中的格式化规则    
    WebConversionService conversionService =         new WebConversionService(this.mvcProperties.getDateFormat());   
    addFormatters(conversionService);   
    return conversionService;}
</code></pre>
<p>点击去：</p>
<pre><code class="language-java">public String getDateFormat() {    
    return this.dateFormat;}
/*** Date format to use. For instance, `dd/MM/yyyy`. 默认的 */
private String dateFormat;
</code></pre>
<p>可以看到在我们的Properties文件中，我们可以进行自动配置它！</p>
<p>如果配置了自己的格式化方式，就会注册到Bean中生效，我们可以在配置文件中配置日期格式化的规则：</p>
<figure data-type="image" tabindex="101"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>其余的就不一一举例了，大家可以下去多研究探讨即可！</p>
<h2 id="修改springboot的默认配置">修改SpringBoot的默认配置</h2>
<p>这么多的自动配置，原理都是一样的，通过这个WebMVC的自动配置原理分析，我们要学会一种学习方式，通过源码探究，得出结论；这个结论一定是属于自己的，而且一通百通。</p>
<p>SpringBoot的底层，大量用到了这些设计细节思想，所以，没事需要多阅读源码！得出结论；</p>
<p>SpringBoot在自动配置很多组件的时候，先看容器中有没有用户自己配置的（如果用户自己配置@bean），如果有就用用户配置的，如果没有就用自动配置的；</p>
<p>如果有些组件可以存在多个，比如我们的视图解析器，就将用户配置的和自己默认的组合起来！</p>
<p><strong>扩展使用SpringMVC</strong>  官方文档如下：</p>
<p>If you want to keep Spring Boot MVC features and you want to add additional MVC configuration (interceptors, formatters, view controllers, and other features), you can add your own @Configuration class of type WebMvcConfigurer but without @EnableWebMvc. If you wish to provide custom instances of RequestMappingHandlerMapping, RequestMappingHandlerAdapter, or ExceptionHandlerExceptionResolver, you can declare a WebMvcRegistrationsAdapter instance to provide such components.</p>
<p>我们要做的就是编写一个@Configuration注解类，并且类型要为WebMvcConfigurer，还不能标注@EnableWebMvc注解；我们去自己写一个；我们新建一个包叫config，写一个类MyMvcConfig；</p>
<pre><code class="language-java">//应为类型要求为WebMvcConfigurer，所以我们实现其接口
//可以使用自定义类扩展MVC的功能
@Configuration
public class MyMvcConfig implements WebMvcConfigurer {
    @Override    
    public void addViewControllers(ViewControllerRegistry registry) {        // 浏览器发送/test ， 就会跳转到test页面；        
        registry.addViewController(&quot;/test&quot;).setViewName(&quot;test&quot;);    }
}
</code></pre>
<p><strong>确实也跳转过来了！所以说，我们要扩展SpringMVC，官方就推荐我们这么去使用，既保SpringBoot留所有的自动配置，也能用我们扩展的配置！</strong></p>
<p>我们可以去分析一下原理：</p>
<p>1、WebMvcAutoConfiguration 是 SpringMVC的自动配置类，里面有一个类<strong>WebMvcAutoConfigurationAdapter</strong></p>
<p>2、这个类上有一个注解，在做其他自动配置时会导入：<strong>@Import(EnableWebMvcConfiguration.class)</strong></p>
<p>3、我们点进EnableWebMvcConfiguration这个类看一下，它继承了一个父类：DelegatingWebMvcConfiguration</p>
<p>这个父类中有这样一段代码：</p>
<pre><code class="language-java">public class DelegatingWebMvcConfiguration extends WebMvcConfigurationSupport {    private final WebMvcConfigurerComposite configurers = new WebMvcConfigurerComposite();      
                                                                               // 从容器中获取所有的webmvcConfigurer    
                                                                                @Autowired(required = false)    public void setConfigurers(List&lt;WebMvcConfigurer&gt; configurers) {        if (!CollectionUtils.isEmpty(configurers)) {            this.configurers.addWebMvcConfigurers(configurers);        }    }}
</code></pre>
<p>4、我们可以在这个类中去寻找一个我们刚才设置的viewController当做参考，发现它调用了一个</p>
<pre><code class="language-java">protected void addViewControllers(ViewControllerRegistry registry) {    this.configurers.addViewControllers(registry);}
</code></pre>
<p>5、我们点进去看一下</p>
<pre><code class="language-java">public void addViewControllers(ViewControllerRegistry registry) {    Iterator var2 = this.delegates.iterator();
    while(var2.hasNext()) {        // 将所有的WebMvcConfigurer相关配置来一起调用！包括我们自己配置的和Spring给我们配置的        
        WebMvcConfigurer delegate = (WebMvcConfigurer)var2.next();        delegate.addViewControllers(registry); 
    }
}
</code></pre>
<p>所以得出结论：所有的WebMvcConfiguration都会被作用，不止Spring自己的配置类，我们自己的配置类当然也会被调用；</p>
<h2 id="全面接管springmvc">全面接管SpringMVC</h2>
<p>官方文档：</p>
<p>If you want to take complete control of Spring MVCyou can add your own @Configuration annotated without @EnableWebMvc.</p>
<p>全面接管即：SpringBoot对SpringMVC的自动配置不需要了，所有都是我们自己去配置！</p>
<p>只需在我们的配置类中要加一个@EnableWebMvc。</p>
<p>我们看下如果我们全面接管了SpringMVC了，我们之前SpringBoot给我们配置的静态资源映射一定会无效，我们可以去测试一下；</p>
<p>不加注解之前，访问首页：</p>
<figure data-type="image" tabindex="102"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p>给配置类加上注解：<strong>@EnableWebMvc</strong></p>
<figure data-type="image" tabindex="103"><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="img" loading="lazy"></figure>
<p><strong>我们发现所有的SpringMVC自动配置都失效了！回归到了最初的样子；</strong></p>
<p><strong>当然，我们开发中，不推荐使用全面接管SpringMVC</strong></p>
<h3 id="拓展1">拓展1</h3>
<p>所以我们要做的想要配置自己的解析器的时候就可以重写WebMvcConfigurer接口里面的方法</p>
<figure data-type="image" tabindex="104"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223221114276.png" alt="image-20210223221114276" loading="lazy"></figure>
<p>在官方文档中我们看到里面有个视图解析器的类ContentNegotiatingViewResolver</p>
<figure data-type="image" tabindex="105"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223221432078.png" alt="image-20210223221432078" loading="lazy"></figure>
<p>可以看到这里继承了viewResolver的接口</p>
<p>再点击这个接口可以看到他里面有重写了一个方法3</p>
<figure data-type="image" tabindex="106"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223221725336.png" alt="image-20210223221725336" loading="lazy"></figure>
<figure data-type="image" tabindex="107"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223221758873.png" alt="image-20210223221758873" loading="lazy"></figure>
<p>接下来我们在配置包中新建一个类来自定义我们想要的视图解析器</p>
<pre><code class="language-java">//拓展springmvc
@Configuration

public class MyMvcVIewer implements WebMvcConfigurer {
    //ViewResolver 实现了视图解析器接口的类，我们就可以吧它看做成视图解析器
    @Bean
    public ViewResolver MyviewResolver(){
        return new MyviewResolver();

    }

    public static class MyviewResolver implements ViewResolver{
        //重写里面的方法
        @Override
        public View resolveViewName(String s, Locale locale) throws Exception {
            return null;
        }

    }

    }
</code></pre>
<p>接下来我们在 DispatcherServletAutoConfiguration的DispatcherServle中找到doDispatch去打断点测试</p>
<figure data-type="image" tabindex="108"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223222908975.png" alt="image-20210223222908975" loading="lazy"></figure>
<p>因此我们可以自己写一个视图解析器放到bean里面 然后springboot就会帮我们自动装配</p>
<p>我们再写另一个方法进行测试一下</p>
<pre><code class="language-java">@Override
public void addViewControllers(ViewControllerRegistry registry) {
    //前面放的是路径 后面放的是试图的名字
    registry.addViewController(&quot;lin&quot;).setViewName(&quot;test&quot;);
}
</code></pre>
<figure data-type="image" tabindex="109"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223225133270.png" alt="image-20210223225133270" loading="lazy"></figure>
<p>测试成功</p>
<h3 id="拓展2">拓展2</h3>
<p>@EnableWebMvc这个注解就是导入了DelegatingWebMvcConfiguration：从容器厂中获取所有的webmvcconfig：</p>
<p>思考问题？为什么加了一个注解，自动配置就失效了！我们看下源码：</p>
<p>1、这里发现它是导入了一个类，我们可以继续进去看</p>
<figure data-type="image" tabindex="110"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223225236507.png" alt="image-20210223225236507" loading="lazy"></figure>
<p>它导入了一个类DelegatingWebMvcConfiguration</p>
<p>2、再点进去</p>
<figure data-type="image" tabindex="111"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223225323935.png" alt="image-20210223225323935" loading="lazy"></figure>
<p>它继承了一个父类 WebMvcConfigurationSupport</p>
<p>3、我们来回顾一下Webmvc自动配置类</p>
<p>我们打开webmvc的自动配置类进行查看</p>
<figure data-type="image" tabindex="112"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210223225435743.png" alt="image-20210223225435743" loading="lazy"></figure>
<p>@ConditionalOnMissingBean({WebMvcConfigurationSupport.class}) // 这个注解的意思就是：容器中没有这个组件的时候，这个自动配置类才生效</p>
<pre><code class="language-java">@Configuration(proxyBeanMethods = false)@ConditionalOnWebApplication(type = Type.SERVLET)@ConditionalOnClass({ Servlet.class, DispatcherServlet.class, WebMvcConfigurer.class })
// 这个注解的意思就是：容器中没有这个组件的时候，这个自动配置类才生效
@ConditionalOnMissingBean(WebMvcConfigurationSupport.class)@AutoConfigureOrder(Ordered.HIGHEST_PRECEDENCE + 10)@AutoConfigureAfter({ DispatcherServletAutoConfiguration.class, TaskExecutionAutoConfiguration.class,    ValidationAutoConfiguration.class })public class WebMvcAutoConfiguration {    }
</code></pre>
<p>然后我们发现在刚才的DelegatingWebMvcConfiguration的类当中他继承了WebMvcConfigurationSuppor这个类 所以一旦家了enable这个注解以后springboot里面默认的自动装配配置的东西就会失效</p>
<p>所以要十分小心！！</p>
<p>总结一句话：@EnableWebMvc将WebMvcConfigurationSupport组件导入进来了；相当于springMVC此时就由你接管了</p>
<p>而导入的WebMvcConfigurationSupport只是SpringMVC最基本的功能！</p>
<p><strong>在SpringBoot中会有非常多的扩展配置，只要看见了这个，我们就应该多留心注意~</strong></p>
<p>而且大部分的配置的都是类似的 我们也可以根据其相似的结构去写我们需要的启动器之类的东西</p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[多线程]]></title>
        <id>https://lindamao0753.github.io/post/duo-xian-cheng/</id>
        <link href="https://lindamao0753.github.io/post/duo-xian-cheng/">
        </link>
        <updated>2021-03-16T04:38:31.000Z</updated>
        <content type="html"><![CDATA[<h1 id="1-创建线程的两种方法">1、创建线程的两种方法</h1>
<h2 id="1-继承thread类">1、继承Thread类</h2>
<pre><code class="language-java">package com.lin;
//创建线程方式一： 继承Thread类 ，重写run()方法 ，调用start开启线程
public class TestThread1 extends Thread {
    //重写run方法线程体
    @Override
    public void run() {
        for (int i = 0; i &lt; 200; i++) {
            System.out.println(&quot;我在看代码&quot;+i);
        }
    }

    public static void main(String[] args) {
        //main线程 ，主线程、
        //创建一个线程对象
        TestThread1 testThread1 = new TestThread1();
        //调用start（）方法开启副线程
        testThread1.start();
        for (int i = 0; i &lt; 1000; i++) {
            System.out.println(&quot;我在学习多线程&quot;+i);
        }
    }
}

</code></pre>
<p>测试：</p>
<figure data-type="image" tabindex="1"><img src="//tvax3.sinaimg.cn/mw690/007bjbYxly1goo6u2wjv9j30mh0bl74i.jpg" alt="image-20210311185407365" loading="lazy"></figure>
<p>经过测试可以发现这些 这些线程的执行是<strong>交互</strong>进行的 并且每次运行的结果都会不一样</p>
<h2 id="2-实现runnable接口">2、实现Runnable接口</h2>
<pre><code class="language-java">package com.draco.demo01;

/**
 * 创建线程方式2，实现Runnable接口
 */
public class TestThread2 implements Runnable{
    @Override
    public void run() {
        for (int i = 0; i &lt; 200; i++) {
            System.out.println(&quot;我在看代码&quot;+i);
        }
    }

    public static void main(String[] args) {
        //main线程 ，主线程、
        //创建一个线程对象
        TestThread1 testThread1 = new TestThread1();
		// Thread thread = new Thread(testThread1);
		// thread.start();
        //利用反射的方式实现 
        new Thread(testThread1).start();//推荐用这种方式

        //调用start（）方法开启线程

        for (int i = 0; i &lt; 1000; i++) {
            System.out.println(&quot;我在学习多线程&quot;+i);
        }
    }
}

</code></pre>
<p>推荐使用第二种，<strong>实现Runnable接口</strong>。</p>
<p>点击源码可以发现Thread类就是集成Runnable接口的</p>
<figure data-type="image" tabindex="2"><img src="//tva2.sinaimg.cn/mw690/007bjbYxly1goo6w7cyqij30ia0fkq4c.jpg" alt="image-20210313130004060" loading="lazy"></figure>
<p>**所以说Thread是继承了Runnable接口的 **</p>
<p><strong>因为在第二种方式里还是得创建一个Thread线程的对象</strong></p>
<p>还有第三种<strong>Callable接口</strong></p>
<h2 id="3-下载网络图片">3、下载网络图片</h2>
<p>这个操作需要导入包 然后才能执行</p>
<p>1实现Runnable接口</p>
<pre><code class="language-java">package com.draco.demo01;

import org.apache.commons.io.FileUtils;

import java.io.File;
import java.io.IOException;
import java.net.URL;

public class Downloader implements Runnable {

    private String url;
    private String name;
	//写构造函数
    public Downloader(String url, String name) {
        this.url = url;
        this.name = name;
    }

    @Override
    //重写run方法
    public void run() {
        //new 一个下载器对象
        WebDownloader webDownloader = new WebDownloader();
        try {
            webDownloader.downloader(url,name);
            System.out.println(&quot;下载了文件名：&quot;+name);
        } catch (IOException e) {
            e.printStackTrace();
        }
    }

    public static void main(String[] args) {
        Downloader d1 = new Downloader(&quot;https://i0.hdslb.com/bfs/sycp/creative_img/202012/625fd761428be531b5405815ea33768b.jpg&quot;,&quot;第一个.jpg&quot;);
        Downloader d2 = new Downloader(&quot;https://i0.hdslb.com/bfs/sycp/creative_img/202102/d521778b3f50444d90e6fefc6719065c.jpg&quot;,&quot;第二个.jpg&quot;);
        Downloader d3 = new Downloader(&quot;https://i0.hdslb.com/bfs/activity-plat/static/20200714/953d0bfef678072bd11ec5fb6ff04190/nMOp2aGOf.png@400w_413h_1c_100q.webp&quot;,&quot;第三个.jpg&quot;);

        new Thread(d1).start();
        new Thread(d2).start();
        new Thread(d3).start();
    }
}
//下载器
class WebDownloader{
    //下载方法 参数传递的是路径和 名字
    public void downloader(String url,String name) throws IOException {
        //通过此方法将url变为文件。
        FileUtils.copyURLToFile(new URL(url),new File(name));
    }
}
</code></pre>
<p>2实现Callable接口</p>
<pre><code class="language-java">package com.lin;

import java.io.IOException;
import java.net.URL;
import java.util.concurrent.*;

public class Testcallable implements Callable&lt;Boolean&gt; {

    private String url;
    private String name;
    //写构造函数
    public Testcallable(String url, String name) {
        this.url = url;
        this.name = name;
    }

    @Override
    //重写run方法
    //对比之前的写的下载图片的方法有所不通的是 这里药品重写的是call方法 然后需要添加一个Boolean类型的值
    public Boolean call() throws IOException {
        WebDownloader webDownloader = new WebDownloader();
        webDownloader.downloader(url,name);
        System.out.println(&quot;下载了文件名为:&quot;+name);
        return true;
    }

    public static void main(String[] args) throws ExecutionException, InterruptedException {
        Testcallable t1 = new Testcallable(&quot;https://i0.hdslb.com/bfs/sycp/creative_img/202012/625fd761428be531b5405815ea33768b.jpg&quot;, &quot;第一个.jpg&quot;);
        Testcallable t2=  new Testcallable(&quot;https://i0.hdslb.com/bfs/sycp/creative_img/202102/d521778b3f50444d90e6fefc6719065c.jpg&quot;,&quot;第二个.jpg&quot;);
        Testcallable t3=  new Testcallable(&quot;https://i0.hdslb.com/bfs/activity-plat/static/20200714/953d0bfef678072bd11ec5fb6ff04190/nMOp2aGOf.png@400w_413h_1c_100q.webp&quot;,&quot;第三个.jpg&quot;);



        //下面的创建方式也不一样 这里用的是固定的方法 线程池里面的东西

        //创建服务 参数是服务的个数
        ExecutorService ser= Executors.newFixedThreadPool(3);

        //提交执行 将对象提交上去并创建一个对象
        Future&lt;Boolean&gt; r1=ser.submit(t1);
        Future&lt;Boolean&gt; r2=ser.submit(t2);
        Future&lt;Boolean&gt; r3=ser.submit(t3);

        //获取结果 获取的结果也可以返回 出来
        Boolean rs1 = r1.get();
        Boolean rs2 = r2.get();
        Boolean rs3 = r3.get();

        //最后一步是将服务关闭
        ser.shutdown();

        //也可以将获取的结构打印出来
        System.out.println(rs1);
        System.out.println(rs2);
        System.out.println(rs3);
    }

}
//下载器
class WebDownloader{
    //下载方法 参数传递的是路径和 名字
    public void downloader(String url,String name) throws IOException {
        //通过此方法将url变为文件。
        FileUtils.copyURLToFile(new URL(url),new File(name));
    }
}
</code></pre>
<p>Callable接口的实现的创建方式和前面两种方式都不相同 因为</p>
<p>总结：<strong>下载的顺序可能不一样 说明这个是同时进行</strong></p>
<h2 id="4-小结">4、小结</h2>
<ul>
<li>继承Thread类
<ul>
<li><strong>子类继承Thread类具有多线程能力</strong></li>
<li><strong>启动线程：子类对象.start()</strong></li>
<li><mark><strong>不建议使用，避免OOP单继承局限性</strong></mark></li>
</ul>
</li>
<li>实现Runnable接口
<ul>
<li><strong>实现接口Runnable具有多线程能力</strong></li>
<li>启动线程：传入目标对象+Thread对象.start()</li>
<li>==<strong>推荐使用，避免单继承局限性，灵活方便，方便同一个对象被多个线程使用=</strong>=</li>
</ul>
</li>
</ul>
<h1 id="2-静态代理">2、静态代理</h1>
<pre><code class="language-java">package com.draco.proxystatic;

/**
 * 静态代理
 */
public class Test {

    public static void main(String[] args) {
        You you = new You();
        WeddingCompany weddingCompany = new WeddingCompany(you);
        weddingCompany.marry();
    }

}

interface Marry{
    void marry();
}

//真实对象：结婚的人
class You implements Marry{

    @Override
    public void marry() {
        System.out.println(&quot;小明准备结婚了！&quot;);
    }
}

//代理对象：婚庆公司
class WeddingCompany implements Marry{

    private You you;

    public WeddingCompany(You you) {
        this.you = you;
    }

    @Override
    public void marry() {
        before();
        you.marry();
        after();
    }

    private void after() {
        System.out.println(&quot;婚庆公司在结婚之后，收钱~&quot;);
    }

    private void before() {
        System.out.println(&quot;婚庆公司在结婚之前，准备布置~&quot;);
    }
}
</code></pre>
<p>总结：</p>
<ul>
<li>真实对象和代理对象都要事先继承一个接口</li>
<li><strong>代理对象要代理真实的对象</strong>去帮他干事情</li>
</ul>
<p><strong>静态代理的好处：</strong></p>
<ul>
<li>​    代理对象可以做很多真实对象都做不了的事情</li>
<li>​    真实对象专注去做自己的事情</li>
</ul>
<h1 id="3-lambda">3、lambda</h1>
<p>首先要理解Functional Interface (函数式接口) 是学习java8 lambda 表达式的关键所在</p>
<p>函数式接口的定义是：</p>
<ul>
<li>
<p><strong>任何接口 ，如果只包含唯一一个抽象的方法</strong> ，那么他就是一个函数式接口</p>
<p>例如下面的例子</p>
<pre><code class="language-java">public interface Runnable(){
    public abstract void run();
}
</code></pre>
</li>
<li>
<p>对于函数式接口，我们可以通过lambda表达式来创建该接口的对象</p>
</li>
</ul>
<p>lambda表达式的本质是<strong>单方法接口的实现类的实例</strong>。</p>
<p>最后lambda表达式含义：接口 对象 = (参数（接口中函数的参数）) -&gt; {具体实现};</p>
<h2 id="1-逐渐演进的版本">1、逐渐演进的版本</h2>
<pre><code class="language-java">package com.lambda;

import com.sun.org.apache.bcel.internal.generic.NEW;

public class TestLambda1 {
    //静态内部类的实现
    static class Like2 implements Ilike{
        @Override
        public void like() {
            System.out.println(&quot;I Like Lambda 2 我实现了静态内部类&quot;);
        }
        //静态内部类方法的调用不能再主方法外面实现
    }



    public static void main(String[] args) {
        //普通的的实现方式
        Ilike like = new Like();
        like.like();
        //创建静态内部类的对象
        Like2 like2 = new Like2();
        like2.like();


        //4.局部内部类 定义在住方法里面
        class Like3 implements Ilike{
            @Override
            public void like() {
                System.out.println(&quot;I Like Lambda 3 我实现了局部内部类 &quot;);
            }

        }
        Like3 like3 = new Like3();
        like3.like();

        //5.匿名内部类 就是没有类的名字
            Ilike like4 = new Ilike(){
             @Override
             public void like() {
                 System.out.println(&quot;I Like Lambda 4 我实现了匿名内部类&quot;);
             }
         };
            like4.like();

            //6.lambad 表达式
        Ilike like5=()-&gt;{
            System.out.println(&quot;I Like Lambda 5 我实现了lambda表达式&quot;);
        };
        like5.like();




    }
}
//1.先定义一个函数式接口
interface Ilike{
    void like();

}
//定义一个类去实现这个接口
class Like implements Ilike{

    @Override
    public void like() {
        System.out.println(&quot;I Like Lambda&quot;);
    }
}

</code></pre>
<h2 id="2-最终简化版">2、最终简化版</h2>
<p>**单参数 **</p>
<p>多个简化的方式 根据喜好和具体的情况来进行选择</p>
<pre><code class="language-java">package com.lambda;

public class TestLambda2 {

    public static void main(String[] args) {
    //一般的表达式
    Ilove love =(int a)-&gt;{
        System.out.println(&quot;I Love You ---&gt;&quot;+a);
    };
    love.love(1);

    //简化1 参数类型
    Ilove love2 =(a)-&gt;{
        System.out.println(&quot;I Love You ---&gt;&quot;+a);
    };
    love2.love(2);
    //简化2 简化括号
    Ilove love3 =a-&gt;{
        System.out.println(&quot;I Love You ---&gt;&quot;+a);
        System.out.println(&quot;我有多行代码 所有我用上了代码块&quot;+a);
    };
    love3.love(3);

    //简化3，去掉花括号
    Ilove love4 =(a)-&gt; System.out.println(&quot;I Love You ---&gt;&quot;+a);
    love4.love(4);


    /*
    总结：
    lambda 表达式只写一行的情况下是当代码只有一行的时候如果的代码有多行 就必须用代码块包裹起来 也就是花括号
    使用lambda必须满足接口是函数式接口 也就是 接口里面只有一个方法
    接口里面的多个参数也可以去掉 但是去掉就得都去掉

     */

    }

}
interface Ilove{
    void love(int a);
}
</code></pre>
<p><strong>多参数</strong></p>
<pre><code class="language-java">package com.lambda;

public class TestLambda3 {
    public static void main(String[] args) {
        ILove love = null;

        love = (a,name)-&gt;{
            System.out.println(&quot;年龄=&quot;+a);
            System.out.println(&quot;名字=&quot;+name);
        };

        love.love(12,&quot;王菲&quot;);


        love = (int a,String name)-&gt;{
            System.out.println(&quot;年龄=&quot;+a);
            System.out.println(&quot;名字=&quot;+name);
        };

        love.love(12,&quot;小明&quot;);
    }

}

interface ILove{
    void love(int a,String name);
}

</code></pre>
<h2 id="3-总结">3、总结</h2>
<ul>
<li>lambda表达式只能有一行代码的情况下，才能简化为一行，如果有多行，那么就用代码块包裹</li>
<li>前提接口是函数式接口，只有一个方法的接口</li>
<li>多个参数也可以去掉参数，要去掉就都去掉，而且必须加上括号。</li>
</ul>
<h1 id="4-线程状态">4、线程状态</h1>
<figure data-type="image" tabindex="3"><img src="//tvax3.sinaimg.cn/mw690/007bjbYxly1goo6xfbroyj30vj0ao795.jpg" alt="image-20210204111058577" loading="lazy"></figure>
<p>详细</p>
<figure data-type="image" tabindex="4"><img src="//tvax3.sinaimg.cn/mw690/007bjbYxly1goo6xobqppj312x0jtk0c.jpg" alt="image-20210204112126116" loading="lazy"></figure>
<p><strong>创建，就绪，运行，等待，死亡</strong></p>
<p><strong>线程方法</strong></p>
<figure data-type="image" tabindex="5"><img src="//tvax2.sinaimg.cn/mw690/007bjbYxly1goo6xx4xilj313f0ibtkv.jpg" alt="image-20210313214445748" loading="lazy"></figure>
<h3 id="线程案例">线程案例</h3>
<p><strong>买票</strong></p>
<pre><code class="language-java">package com.lin;
/*
实现思路 让兔子设置延时模拟睡觉 然后让乌龟正常的进行跑步的过程
run方法用来实现龟兔赛跑 进行的条件是游戏还没有产生胜利者
GameOver 用来判断游戏的是否结束 并产生胜利者
 */
public class Race implements Runnable {
//    定义一个胜利者
    private static String Winner;
    @Override
    public void run() {
        for (int i = 1; i &lt;=100 ; i++) {
            //模拟让兔子跑一会停一会 所以就设宴延时
            if (Thread.currentThread().getName().equals(&quot;兔子&quot;)&amp;&amp; i%20==0)
            {
                try {
                    Thread.sleep(1);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }

            boolean flag= Gameover(i);
            //当返回值为真的时候比赛结束
            if (flag){

                break;
            }
            System.out.println(Thread.currentThread().getName()+&quot;跑了&quot;+i+&quot;步&quot;);


        }

    }
    //通过步数的完成来判断是否完成饿了比赛
    public Boolean Gameover(int Steps){
        //判断是否有胜利者
        if (Winner!=null)
        {
            //当不存在胜利者的时候就是比赛还没结束
            return true;
        }
         //当步数达到100以上比赛结束
            if (Steps&gt;=100){
                Winner=Thread.currentThread().getName();
                System.out.println(&quot;======游戏结束!! 胜利者是&quot;+Winner+&quot;======&quot;);
                return true;

            }

        return false;
    }

    public static void main(String[] args) {
        Race race=new Race();
        new Thread(race,&quot;兔子&quot;).start();
        new Thread(race,&quot;乌龟&quot;).start();
    }

}

</code></pre>
<p><strong>龟兔赛跑</strong></p>
<pre><code class="language-java">package com.lin;
/*
实现思路 让兔子设置延时模拟睡觉 然后让乌龟正常的进行跑步的过程
 */
public class Race implements Runnable {
//    定义一个胜利者
    private static String Winner;
    @Override
    public void run() {
        for (int i = 1; i &lt;=100 ; i++) {
            //模拟让兔子跑一会停一会 所以就设宴延时
            if (Thread.currentThread().getName().equals(&quot;兔子&quot;)&amp;&amp; i%20==0)
            {
                try {
                    Thread.sleep(1);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }

            boolean flag= Gameover(i);
            //当返回值为真的时候比赛结束
            if (flag){

                break;
            }
            System.out.println(Thread.currentThread().getName()+&quot;跑了&quot;+i+&quot;步&quot;);


        }

    }
    //通过步数的完成来判断是否完成饿了比赛
    public Boolean Gameover(int Steps){
        //判断是否有胜利者
        if (Winner!=null)
        {
            //当不存在胜利者的时候就是比赛还没结束
            return true;
        }
         //当步数达到100以上比赛结束
            if (Steps&gt;=100){
                Winner=Thread.currentThread().getName();
                System.out.println(&quot;胜利者是&quot;+Winner);
                return true;

            }

        return false;
    }

    public static void main(String[] args) {
        Race race=new Race();
        new Thread(race,&quot;兔子&quot;).start();
        new Thread(race,&quot;乌龟&quot;).start();
    }

}
</code></pre>
<h2 id="1-停止stop">1、停止stop</h2>
<ul>
<li>1、建议线程正常停止，利用次数，不建议死循环</li>
<li>2、<strong>建议使用标志位</strong>，设置一个标志位</li>
<li>3、<strong>不要使用stop或者destroy等过时或者JDK不建议使用的方法</strong></li>
</ul>
<pre><code class="language-java">package com.draco.staticThread;

/**
 * 1、建议线程正常停止，利用次数，不建议死循环
 * 2、建议使用标志位，设置一个标志位
 * 3、不要使用stop或者destroy等过时或者JDK不建议使用的方法
 */
public class TestStop implements Runnable{

    private boolean flag = true;

    @Override
    public void run() {
        int i = 0;
        while (flag){
            System.out.println(&quot;run....Runnable Thread &quot;+i++);
        }
    }

    public void over(){
        flag = false;
    }

    public static void main(String[] args) {

        TestStop testStop = new TestStop();
        new Thread(testStop).start();

        for (int i = 0; i &lt; 1000; i++) {
            System.out.println(&quot;test main &quot;+i);
            if(i==500){
                testStop.over();
                System.out.println(&quot;线程停止了&quot;);
            }
        }
    }
}
</code></pre>
<h2 id="2-睡眠sleep">2、睡眠sleep</h2>
<p><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo6y8x9e3j31ac0mn7in.jpg" alt="image-20210313220958238" loading="lazy"><br>
模拟网络延时</p>
<p><strong>注意！</strong></p>
<p>如果没有加上延时的话 会出现票数为负的情况 结果就可能为负数！！！</p>
<p>模拟网络延时：放大问题的发生性</p>
<pre><code class="language-java">package com.draco.staticThread;

/**
 * 模拟网络延时，放大问题的发生性
 */
public class TestSleep implements Runnable {

    private int tickteNum = 10;

    @Override
    public void run() {
        while (true){
            if(tickteNum&lt;=0){
                break;
            }else {

                //模拟延时
                try {
                    Thread.sleep(100);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }

                System.out.println(Thread.currentThread().getName()+&quot;得到了第 &quot;+tickteNum--+&quot; 票&quot;);
            }
        }
    }

    public static void main(String[] args) {
        TestSleep testSleep = new TestSleep();

        new Thread(testSleep,&quot;小红&quot;).start();
        new Thread(testSleep,&quot;小明&quot;).start();
        new Thread(testSleep,&quot;小黄&quot;).start();
    }
}
</code></pre>
<p>倒计时，输出当前时间</p>
<pre><code class="language-java">package com.state;

import java.text.SimpleDateFormat;
import java.util.Date;
/*
这里要注意 可能与我们调用的时间会有差别 因为cpu调度还需要一定的时间
 */

public class TestSleep {

    public static void main(String[] args) throws InterruptedException {
        TestSleep testSleep = new TestSleep();
        //可以通过类来直接调用静态方法
        //也可以直接调用
//        tendown();
        TestSleep.tendown();
        tendown();

        //需要通过对象来调用非静态方法
        testSleep.getcurrentTime();





    }
    public  void getcurrentTime(){
        //打印当前的时间
        Date currentTime = new Date(System.currentTimeMillis());

        while (true){
            //1000毫秒等于1s
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            //还得格式化时间 将其变为我们认识的格式
            System.out.println(new SimpleDateFormat(&quot;HH:mm:ss&quot;).format(currentTime));
            //还得再更新一次当前的时间 不然时间就停留在当前的时间了
            currentTime = new Date(System.currentTimeMillis());
        }


    }
    //倒计时器
    public  static void tendown() throws InterruptedException {
        int number=10;
        while (true){
            Thread.sleep(1000);
            System.out.println(number--);
            if (number&lt;0)
            {
                break;
            }

        }


    }

}

</code></pre>
<p><strong>每个对象都有一把锁，sleep不会释放锁~</strong></p>
<p><strong>sleep方法 让进程进入阻塞状态的时候不会把自己的位置让出去</strong></p>
<h2 id="3-礼让yield">3、礼让yield</h2>
<ul>
<li>礼让线程，让当前正在执行的线程暂停，但不阻塞</li>
<li><strong>将线程从运行状态变为就绪状态</strong></li>
<li>让cpu重新调度，礼让不一定成功！看cpu心情</li>
</ul>
<p>从运行到就绪状态，就是礼让，<strong>还是有抢夺CPU资源的机会的</strong></p>
<p><strong>代码测试</strong></p>
<pre><code class="language-java">package com.draco.staticThread;

public class TestYield {
    public static void main(String[] args) {
        MyYield myYield = new MyYield();

        new Thread(myYield,&quot;小红&quot;).start();
        new Thread(myYield,&quot;小名&quot;).start();
    }
}

class MyYield implements Runnable{

    @Override
    public void run() {
        System.out.println(Thread.currentThread().getName()+&quot;线程在执行&quot;);
        Thread.yield();
        System.out.println(Thread.currentThread().getName()+&quot;线程被暂停&quot;);
    }
}
</code></pre>
<p>礼让不一定成功，要看CPU的心情~</p>
<p>礼让线程，让当前正在执行的线程暂停，但不阻塞，将线程从运行状态转为就绪状态</p>
<p>所以也就会出现</p>
<p>礼让<strong>不成功</strong>的状态</p>
<p><img src="//tva4.sinaimg.cn/mw690/007bjbYxly1goo6yxojkoj30es0553yj.jpg" alt="image-20210314142023127" loading="lazy">7.png)</p>
<p>礼让<strong>成功</strong>的状态</p>
<figure data-type="image" tabindex="6"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo6z3ipjkj30gv06t3yl.jpg" alt="image-20210314142049174" loading="lazy"></figure>
<h2 id="4-合并join">4、合并Join</h2>
<p>Join合并线程，待次线程执行完成之后，再执行其他的线程，其他线程阻塞</p>
<p>可以想象成插队 是个很霸道的东西。</p>
<pre><code class="language-java">package com.state;

public class TestJoin implements Runnable{
    @Override
    public void run() {
        for (int i = 0; i &lt; 200; i++) {
            System.out.println(&quot;VIP线程来啦！！！！&quot;);
        }
    }

    public static void main(String[] args) throws InterruptedException {
        TestJoin testJoin = new TestJoin();
        Thread thread = new Thread(testJoin);

        for (int i = 0; i &lt; 300; i++) {
            if (i==200){
                //注意这里要将副线程放到if语句当中实现 不然副线程在主线程的运行刚开始的时候就会执行官就可能不会等到if条件满足的时候就开始执行了
                thread.start();
                //让副线程插队
                thread.join();
            }
            System.out.println(&quot;主线程正在运行当中&quot;+i);
        }
    }
}

</code></pre>
<p>也就是插队，待此线程执行完成后，再执行其他的线程，其他线程阻塞</p>
<p><img src="//tvax1.sinaimg.cn/mw690/007bjbYxly1goo6zixn87j31a90hvq3w.jpg" alt="image-20210317111406644" loading="lazy"><br>
<strong>注意：</strong></p>
<p>这里要将副线程放到if语句当中实现 不然副线程在主线程的运行刚开始的时候就会执行官 就可能不会等到if条件满足的时候就开始执行了</p>
<p>这里可以看到 当线程实现了Join方法后 前面正在执行的线程必须等待插队进来的线程执行完之后才能继续进行下去。</p>
<h2 id="5-线程的状态">5、线程的状态</h2>
<figure data-type="image" tabindex="7"><img src="//tvax1.sinaimg.cn/mw690/007bjbYxly1goo7316i2uj31aa0n547u.jpg" alt="image-20210314143557265" loading="lazy"></figure>
<pre><code class="language-java">package com.state;
/*
思路
启动线程
观察线程刚开始
观察线程进行
观察线程结束
 */
public class TestState {
    public static void main(String[] args) throws InterruptedException {
        Thread thread = new Thread(()-&gt;{
            for (int i = 0; i &lt; 5; i++) {
                //这里要让线程延时一会进行观察线程的状态
                //等待5秒之后输出 如果没有这段程序 就不会输出TIMED_WAITING的状态
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
            System.out.println(&quot;轮到我啦&quot;);
        });
        //观察线程
        Thread.State state = thread.getState();
        System.out.println(state);//NEW 线程在还没启动状态就有了

        thread.start();
        //更新线程 因为每一个状态的线程状态都不一样
        state=thread.getState();

        System.out.println(state);//RUNNABLE 

        while (state!=Thread.State.TERMINATED){
            //再次更新状态
            state=thread.getState();

            Thread.sleep(100);
            System.out.println(state);//会输出TIMED_WAITING 和 TERMINATED 后者在前者完成之后才进行运行

        }

    }
}

</code></pre>
<p>当线程TERMINATED后（死亡后），就不能启动了，<strong>所以线程只能启动一次</strong></p>
<p>在线程结束之后再启动程序会报错</p>
<figure data-type="image" tabindex="8"><img src="//tvax3.sinaimg.cn/mw690/007bjbYxly1goo737ze0cj30nl022aab.jpg" alt="image-20210314151322323" loading="lazy"></figure>
<p><strong>想要再执行线程就必须再new一个新的线程</strong></p>
<h2 id="6-线程的优先级">6、线程的优先级</h2>
<p><img src="//tva4.sinaimg.cn/mw690/007bjbYxly1goo756jzn1j31930oygsf.jpg" alt="image-20210314151423661" loading="lazy"><br>
<strong>两种方式</strong></p>
<p>getPrriority() <strong>获得</strong>现线程的优先级</p>
<p>setPriority（int XXX）<strong>设置</strong>线程的优先级</p>
<p><strong>注意</strong></p>
<p>优先级的设定建议要在start()调度前 否则线程在运行的时候再改变其优先级是无法实现的</p>
<p>要注意的是 <strong>优先级低只是意味着获得调度的概率低</strong> ，并不是优先级低就不会被调用了 这还得注意看cpu的调度</p>
<p>如果cpu调度了优先级比较低的线程 就会出现性能倒置的情况 所以我们要尽量避免这种情况的发生。</p>
<pre><code class="language-java">package com.state;

public class TestPriority {
    public static void main(String[] args) {
        //打印主函数的优先级
//        Thread.currentThread().setPriority(10); 修改主函的优先级
        System.out.println(Thread.currentThread().getName() + &quot;-----&gt;&quot; + Thread.currentThread().getPriority());

        MyPriority myPriority = new MyPriority();

        Thread t1 = new Thread(myPriority);
        Thread t2 = new Thread(myPriority);
        Thread t3 = new Thread(myPriority);
        Thread t4 = new Thread(myPriority);
        Thread t5 = new Thread(myPriority);
        Thread t6 = new Thread(myPriority);


        // 要注意 要设定优先级才能启动
        t1.setPriority(Thread.MAX_PRIORITY);//Thread.MAX_PRIORITY 设置最大优先级
        t1.start();

        t2.setPriority(Thread.NORM_PRIORITY);//Thread.NORM_PRIORITY) 设置中等优先级
        t2.start();

        t3.setPriority(Thread.MIN_PRIORITY);//Thread.MIN_PRIORITY 设置最小优先级
        t3.start();

        t4.setPriority(8);
        t4.start();

        t5.setPriority(3);
        t5.start();

        t6.start();

    }

    static class MyPriority implements Runnable {
        @Override
        public void run() {
            System.out.println(Thread.currentThread().getName() + &quot;-----&gt;&quot; + Thread.currentThread().getPriority());
        }
    }
}
</code></pre>
<p><strong>先设置优先级，再启动</strong></p>
<p><strong>优先级低的只是意味着获得CPU调度的概率低，并不是优先级低就不会被调用</strong>了</p>
<figure data-type="image" tabindex="9"><img src="//tvax2.sinaimg.cn/mw690/007bjbYxly1goo75eamxcj30ep05bmx8.jpg" alt="image-20210314153526471" loading="lazy"></figure>
<p><strong>注意</strong></p>
<p><strong>主函数的优先级默认为5 且能改变主函数优先级</strong></p>
<p><strong>最大优先级为10</strong></p>
<p><strong>未被设置优先级的线程优先级也默认为5</strong></p>
<h2 id="7-守护daemon线程">7、守护（daemon）线程</h2>
<p>线程分为<strong>用户线程</strong>和<strong>守护线程</strong></p>
<p>虚拟机<strong>必须确保用户线程执行完毕</strong></p>
<p>虚拟机<strong>不用等待守护线程执行完毕</strong>如，后台记录操作日志，监控内存，垃圾回收等等 这些都是属于守护线程</p>
<p>守护线程只管着用户的线程 伴随着用户线程的开始到结束</p>
<pre><code class="language-java">package com.state;

public class TestDaemon {
    public static void main(String[] args) {
        Buddha buddha = new Buddha();
        You you = new You();
        //线程代理佛祖 然后
        Thread thread = new Thread(buddha);
        //将线程设置为守护线程 里面放的是Boolean值 默认的是为false 将其设置为true 就是为守护线程

        thread.setDaemon(true);
        //守护线程启动
        thread.start();
        // 用户线程启动
        new Thread(new You()).start();


    }

}
class Buddha implements Runnable{

    @Override
    public void run() {
        while (true){
            //
            System.out.println(&quot;佛祖保佑着你&quot;);
        }
    }

}
class You implements Runnable{

    @Override
    public void run() {
        for (int i = 0; i &lt; 100; i++) {
            System.out.println(&quot;你每年都过得很开心&quot;);
        }
        System.out.println(&quot;再见啦 &quot;);
    }
}

</code></pre>
<p><strong>注意</strong></p>
<p>在用户线程结束了之后 守护线程还会再运行一段时间因为 虚拟机并没有去管 守护线程。</p>
<p>线程分为<mark>用户线程</mark>和<mark>守护线程</mark></p>
<p>虚拟机必须等到<mark>用户线程</mark>执行完毕【例如：main等】</p>
<p>虚拟机不用等到<mark>守护线程</mark>执行完毕【例如：GC等】</p>
<h1 id="5-线程同步">5、线程同步</h1>
<p><strong>多个线程想访问同一个对象的时候就是并发</strong> 我们需要改变你这个机制</p>
<p>所以就需要线程同步这个机制 线程同步就是一种等待机制</p>
<p>多个需要同时访问次此对象的线程进入这个<strong>对象的的等待池子</strong></p>
<p><strong>排队来解决问题</strong></p>
<p>队列和锁</p>
<p>举个简单的例子 去景区上厕所排队 排队形成队列 厕所上的门把锁就是队列的锁</p>
<p>把锁锁上才能保证自己方便的自由  也就是保证了线程的安全。</p>
<p>加入锁机制<strong>synchronized</strong></p>
<p>当然加入锁也会出现一些问题</p>
<p>鱼和熊掌不可兼得嘛</p>
<figure data-type="image" tabindex="10"><img src="//tvax2.sinaimg.cn/mw690/007bjbYxly1goo75wlgdtj318h0knk1z.jpg" alt="image-20210314163518662" loading="lazy"></figure>
<h2 id="1-三大线程不安全案例">1、三大线程不安全案例</h2>
<p><strong>不安全的情况出现的本质是</strong></p>
<p><strong>每个线程在自己的工作内存交互，内存控制不当会造成数据不一致。</strong></p>
<p><strong>不安全的买票</strong></p>
<pre><code class="language-java">package com.draco.syn;

/**
 * 不安全的买票
 */
public class UnSafeBuyTicket {
    public static void main(String[] args) {
        BuyTicket buyTicket = new BuyTicket();

        new Thread(buyTicket,&quot;小明&quot;).start();
        new Thread(buyTicket,&quot;小红&quot;).start();
        new Thread(buyTicket,&quot;黄牛党&quot;).start();
    }
}

class BuyTicket implements Runnable{
    private int ticketNums = 10;
    boolean flag = true;
    @Override
    public void run() {
        //买票
        
        while (flag){
            
        try {
            Thread.sleep(100);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
            buy();
        }
    }

    public void buy(){
        if (ticketNums&lt;=0){
            flag = false;
            return;
        }


        System.out.println(Thread.currentThread().getName()+&quot;--&gt;&quot;+ticketNums--);
    }
}
</code></pre>
<p><strong>出现的问题：</strong></p>
<p>因为当用户同一时间都取到最后一张票的时候票的数量就会同一时间的减少 就会取到负数的票</p>
<figure data-type="image" tabindex="11"><img src="//tva1.sinaimg.cn/mw690/007bjbYxly1goo76fhwruj309n04v745.jpg" alt="image-20210315133410036" loading="lazy"></figure>
<p><strong>不安全的取钱</strong></p>
<pre><code class="language-java">package com.UnSafe;

public class UnSafeBank {
    public static void main(String[] args) {
        //创建一个银行卡
        Account account = new Account(100,&quot;理财基金&quot;);


        TakeMoney you = new TakeMoney(account,50,&quot;你&quot;);
        TakeMoney gilrfriend = new TakeMoney(account,100,&quot;女朋友&quot;);
        you.start();
        gilrfriend.start();


    }
}
//卡
class Account{
    int money;//余额
    String name;//基金的名字

    public Account(int money, String name) {
        this.money = money;
        this.name = name;
    }
}
class TakeMoney extends Thread {
    Account account;
    int takemoney;//取走的钱
    int nowmoney;//用户现在持有的钱

    public TakeMoney(Account account, int takemoney, String name) {
        super(name);
        this.account = account;

        this.takemoney = takemoney;
    }
    @Override
    public void run(){
        //判断有没有这么多钱
        if (account.money-takemoney&lt;0){
            System.out.println(&quot;您账户的余额不足！,还剩&quot;+account.money);
        }
        account.money=account.money-takemoney;
        //手里的钱取到的钱加上本来有的钱
        nowmoney=nowmoney+takemoney;
        System.out.println(account.name+&quot;--&gt;&quot;+&quot;账户余额：&quot;+account.money);

        //这个this等等同于类
        System.out.println(this.getName()+&quot;--&gt;&quot;+&quot;手里钱：&quot;+nowmoney);
    }


    }
</code></pre>
<p><strong>出现的问题：</strong></p>
<p>钱不足的时候还能再进行取钱这个操作</p>
<p>假设这种情况发生在实际生活当中 那就会出现很危险的情况！！！</p>
<figure data-type="image" tabindex="12"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo76phnrbj30cc03uwee.jpg" alt="image-20210315132557572" loading="lazy"></figure>
<p><strong>不安全的集合</strong></p>
<pre><code class="language-java">package com.UnSafe;

import java.util.ArrayList;

public class UnSafeList {
    public static void main(String[] args) {
        ArrayList&lt;String&gt; list = new ArrayList&lt;&gt;();
        for (int i = 0; i &lt; 1000; i++) {
            new Thread(()-&gt;{
                //想集合里面加入当前线程的名字
                list.add(Thread.currentThread().getName());
            }).start();


        }
        try {
            Thread.sleep(10);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println(&quot;集合的大小为&quot;+list.size());
    }
}

</code></pre>
<p>出现的问题：</p>
<p>程在执行的过程 有的进程一起进行的时候同时将内存覆盖掉了  所以就会出现集合的大小填不满的情况 !<img src="//tva4.sinaimg.cn/mw690/007bjbYxly1goo773g8rnj30ee02m0sl.jpg" alt="image-20210315133807600" loading="lazy"><br>
为了解决这种机制 和避免在计算机中发生这种情况 我们使用了下面的同步机制</p>
<p>第一个是synchronize</p>
<p>第二个是Lock锁</p>
<h2 id="2-synchronize同步">2、synchronize同步</h2>
<figure data-type="image" tabindex="13"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo77dfolkj319m0omnat.jpg" alt="image-20210315134716665" loading="lazy"></figure>
<figure data-type="image" tabindex="14"><img src="//tvax2.sinaimg.cn/mw690/007bjbYxly1goo77lfoqzj310v0jcgys.jpg" alt="image-20210315140002986" loading="lazy"></figure>
<p><strong>对于上述三种线程不安全的案例解决方案</strong></p>
<pre><code class="language-java">package com.draco.syn;

/**
 * 不安全的买票
 */
public class UnSafeBuyTicket {
    public static void main(String[] args) {
        BuyTicket buyTicket = new BuyTicket();

        new Thread(buyTicket,&quot;小明&quot;).start();
        new Thread(buyTicket,&quot;小红&quot;).start();
        new Thread(buyTicket,&quot;黄牛党&quot;).start();
    }
}

class BuyTicket implements Runnable{
    private int ticketNums = 10;
    boolean flag = true;
    @Override
    public void run() {
        //买票
        while (flag){

            try {
                Thread.sleep(100);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }

            buy();
        }
    }

    //synchronized，同步方法，锁的是this
    public synchronized void buy(){
        if (ticketNums&lt;=0){
            flag = false;
            return;
        }

        System.out.println(Thread.currentThread().getName()+&quot;--&gt;&quot;+ticketNums--);
    }
}
</code></pre>
<p>解决同一时间取钱的问题</p>
<pre><code class="language-java">package com.synchronize;

public class SafeBank {
    public static void main(String[] args) {
        //创建一个银行卡
        Account account = new Account(100,&quot;理财基金&quot;);


        TakeMoney you = new TakeMoney(account,50,&quot;你&quot;);
       TakeMoney gilrfriend = new TakeMoney(account,100,&quot;女朋友&quot;);
        you.start();
        gilrfriend.start();


    }
}
//卡
class Account{
    int money;//余额
    String name;//基金的名字

    public Account(int money, String name) {
        this.money = money;
        this.name = name;
    }
}
class TakeMoney extends Thread {
    Account account;
    int takemoney;//取走的钱
    int nowmoney;//用户现在持有的钱

    public TakeMoney(Account account, int takemoney, String name) {
        super(name);
        this.account = account;

        this.takemoney = takemoney;
    }
//    synchronized 默认锁的是this 在run上面锁住并不能和真正的把对象锁住
    @Override
    public void run(){
        //所以我们锁的应当是account 因为是这个对象发生了增删改查
        // synchronized (TakeMoney.class) 这里将取钱这个类锁住了 也是可以实现的
        synchronized (account){
            //判断有没有这么多钱
            if (account.money-takemoney&lt;0){
                System.out.println(&quot;您账户的余额不足！,还剩&quot;+account.money);
                return;
            }
            try {
                //经测试发现 如果这个延时的时间不够长 也会出现锁不住的情况
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            account.money=account.money-takemoney;
            //手里的钱取到的钱加上本来有的钱
            nowmoney=nowmoney+takemoney;
            System.out.println(account.name+&quot;--&gt;&quot;+&quot;账户余额：&quot;+account.money);
            //Thread.currentThread().getName()=this.getName()=TakeMoney.this.getName()
            //这个this等等同于TakeMoney类
            System.out.println(this.getName()+&quot;--&gt;&quot;+&quot;手里钱：&quot;+nowmoney);
        }

        }
}
</code></pre>
<p><strong>分析问题：</strong></p>
<p>对比上面的取票案例 单单的只是锁住了run方法并不能实现同步</p>
<p>这里为什么出现了不同步的情况，是因为创建这个银行账户多线程的方式是继承Thread，也就是通过extend Thread来进行实现的，在main主方法中创建了两个银行账户，这两个账户对象都是独立的，只是共享一个Account对象数据，我们给Run方法加了锁，锁的是当前对象，但这两个银行账户的当前对象是自己 所以锁住了run'放法并不能真正的锁住对象</p>
<p>线程“you”执行run方法时发现加锁了，便找到加锁对象(you)，发现没有其他线程执行run方法，就持锁执行run方法</p>
<p>线程“girlfriend”执行run方法时发现加锁了，便找到枷锁对象(girlfriend,不是you)，也发现没有其他线程执行run方法，就持锁执行run方法</p>
<p><strong>注意</strong></p>
<p>这里的锁银行而不是锁this  <strong>指的是当前类 也就是银行的class TakeMoney.class</strong></p>
<p>而synchronized方法 想让锁对象是当前类的class ，要么只能有一个银行的实例对象， 要么ynchronized方法前用static修饰 这样也能保证锁对象是类的class</p>
<p>而他有两个银行的实例对象  you 和girlfriend 两个实例对象 那么这两个实例对象开启了两条线程，每条线程用的锁对象都是当前实例对象 锁对象不同是无法实现同步的。</p>
<p>最简单的方法就是 用synchronized代码块 在synchronized(){}， 小括号中放当前类的class，因为一个类是只能有一个class的 所以 能保证锁对象是一样的 而实现同步<br>
ps:<strong>小括号中只要是一个唯一的就行 对象 class都可以</strong></p>
<p>其实只要记住下面这三点 任何时候都不会搞错同步方法或同步代码块中的锁对象是什么了<br>
对于普通同步方法，<strong>锁是当前实例对象。 如果有多个实例 那么锁对象必然不同无法实现同步。</strong><br>
对于<strong>静态同步方法</strong>，锁是当前类的Class对象。有多个实例 但是锁对象是相同的  可以完成同步。<br>
对于同步方法块，锁是Synchonized括号里配置的对象。</p>
<p><strong>对象最好是只有一个的 如当前类的 class 是只有一个的  锁对象相同 也能实现同步</strong>。</p>
<p>我们应该找出到底是哪个对象在进行真正的增删改查 再进行操作</p>
<pre><code class="language-java">package com.draco.syn;

import java.util.ArrayList;

/**
 * 不安全的集合
 */
public class UnSafeList {
    public static void main(String[] args) throws InterruptedException {
        ArrayList&lt;String&gt; list = new ArrayList&lt;&gt;();
        for (int i = 0; i &lt; 10000; i++) {
            new Thread(()-&gt;{
                synchronized (list){
                    list.add(Thread.currentThread().getName());
                }
            }).start();
        }

        Thread.sleep(10);

        System.out.println(&quot;list's size = &quot;+list.size());
    }
}
</code></pre>
<p><strong>记住：对于synchronized的对象就是我们需要<mark>修改的对象</mark>即可</strong></p>
<p><strong>对比之前的加锁案例我们可以发现 Lock 锁我们可以很清楚的看到加锁和解锁的位置</strong></p>
<h1 id="6-锁">6、锁</h1>
<figure data-type="image" tabindex="15"><img src="//tvax2.sinaimg.cn/mw690/007bjbYxly1goo780lhp4j318b0nj11z.jpg" alt="image-20210315195114285" loading="lazy"></figure>
<h2 id="synchronized与lock对比"><strong>synchronized与Lock对比</strong></h2>
<figure data-type="image" tabindex="16"><img src="//tva2.sinaimg.cn/mw690/007bjbYxly1goo787z7gkj31co0n1gty.jpg" alt="image-20210315194059435" loading="lazy"></figure>
<h2 id="1-死锁">1、死锁</h2>
<pre><code class="language-java">package com.draco.lock;

public class DeadLock {
    public static void main(String[] args) {
        Makeup g1 = new Makeup(0,&quot;小红&quot;);
        Makeup g2 = new Makeup(1,&quot;小明明&quot;);

        g1.start();
        g2.start();
    }
}

class LipStick{}
class Mirror{}

class Makeup extends Thread{

    //需要一份资源，用static来保证只有一份资源
    static LipStick lipStick = new LipStick();
    static Mirror mirror = new Mirror();

    private int choice;
    private String girlName;

    public Makeup(int choice,String girlName){
        this.choice = choice;
        this.girlName = girlName;
    }

    @Override
    public void run() {
        makeup();
    }

    private void makeup() {
        if(choice==0){
            //不要同时抱一把锁就好了
            synchronized (lipStick){
                System.out.println(this.girlName+&quot;获得口红&quot;);
//                synchronized (mirror){
//                    System.out.println(this.girlName+&quot;获得镜子&quot;);
//                }
            }
            synchronized (mirror){
                System.out.println(this.girlName+&quot;获得镜子&quot;);
            }
        }else{
            synchronized (mirror){
                System.out.println(this.girlName+&quot;获得镜子&quot;);
//                synchronized (lipStick){
//                    System.out.println(this.girlName+&quot;获得口红&quot;);
//                }
            }
            synchronized (lipStick){
                System.out.println(this.girlName+&quot;获得口红&quot;);
            }
        }
    }
}
</code></pre>
<figure data-type="image" tabindex="17"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo78gb92xj305c02d0rf.jpg" alt="image-20210315201107334" loading="lazy"></figure>
<p><strong>解决方法：</strong></p>
<p><strong>不一次性将锁全部获取到 用完就放开</strong></p>
<figure data-type="image" tabindex="18"><img src="//tva1.sinaimg.cn/mw690/007bjbYxly1goo78mbx1ij30d503e0si.jpg" alt="image-20210315201213576" loading="lazy"></figure>
<h2 id="2-死锁产生的必要条件">2、死锁产生的必要条件</h2>
<figure data-type="image" tabindex="19"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo7a45b4gj30z30fjjyw.jpg" alt="image-20210204214449140" loading="lazy"></figure>
<h2 id="3-reentrantlock可重入锁">3、ReentrantLock可重入锁</h2>
<p>对比之前的取票案例</p>
<pre><code class="language-java">package com.draco.lock;

import java.util.concurrent.locks.ReentrantLock;

public class TestReentrantLock {
    public static void main(String[] args) {
        Lock2 lock2 = new Lock2();

        new Thread(lock2).start();
        new Thread(lock2).start();
    }
}

class Lock2 implements Runnable{
    private int ticketNums = 10;
	//根据ReentrantLock创建一个对象 再通过调用相关方法 再进行实现
    private final ReentrantLock reentrantLock = new ReentrantLock();

    @Override
    public void run() {
        while (true){
            try{
                //加锁
                reentrantLock.lock();
                if(ticketNums&lt;=0){
                    break;
                }else{
                    try {
                        Thread.sleep(1000);
                    } catch (InterruptedException e) {
                        e.printStackTrace();
                    }
                    System.out.println(ticketNums--);
                }
            }
            finally {
                //解锁
                reentrantLock.unlock();
            }


        }
    }
}
</code></pre>
<p><strong>对比之前的加锁案例我们可以发现 Lock 锁我们可以很清楚的看到加锁和解锁的位置</strong></p>
<figure data-type="image" tabindex="20"><img src="//tva3.sinaimg.cn/mw690/007bjbYxly1goo7abjwfoj30pe09ydjd.jpg" alt="image-20210204220748282" loading="lazy"></figure>
<h1 id="7-线程通信">7、线程通信</h1>
<h2 id="1-管程法">1、管程法</h2>
<pre><code class="language-java">package com.draco.gaoji;

/**
 *
 */
public class TestPC {
    public static void main(String[] args) {
        KeepSpace keepSpace = new KeepSpace();

        new Thread(new Producer(keepSpace)).start();
        new Thread(new Customer(keepSpace)).start();

    }

}

class Food{
    int id;
    public Food(int id) {
        this.id = id;
    }
}

class Producer implements Runnable{

    KeepSpace keepSpace;

    public Producer(KeepSpace keepSpace) {
        this.keepSpace = keepSpace;
    }

    @Override
    public void run() {
        for (int i = 0; i &lt; 100; i++) {
            keepSpace.push(new Food(i));
            System.out.println(&quot;生产了第&quot;+i+&quot;个食物&quot;);
        }
    }
}

class Customer implements Runnable{

    KeepSpace keepSpace;

    public Customer(KeepSpace keepSpace) {
        this.keepSpace = keepSpace;
    }

    @Override
    public void run() {
        //消费者消费
        for (int i = 0; i &lt; 100; i++) {
            System.out.println(&quot;消费了第&quot;+keepSpace.pop().id+&quot;个食物&quot;);
        }
    }
}

class KeepSpace{
    //空间大小
    Food[] foods = new Food[10];
    //空间计数器
    int count = 0;

    public synchronized void push(Food food){
        //空间满了，等待消费者消费
        if (count==foods.length){
            //通知消费者消费
            try {
                this.wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }

        //空间没有满，继续制作
        foods[count] = food;
        count++;
        //有吃的，可以通知消费者消费了
        this.notifyAll();
    }

    //消费者消费
    public synchronized Food pop(){
        //判断能否消费
        if (count==0){
            //等待生产者生产
            try {
                this.wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
        count--;
        Food food = foods[count];
        //吃完了，通知生产者生产
        this.notifyAll();
        return food;
    }
}
</code></pre>
<p>具体的思路 ：</p>
<p>生产者生产的仓库放满了之后就该停止生产并让用户消费 用户消费时即应当提醒生产者进行生产</p>
<h2 id="2-信号灯法">2、信号灯法</h2>
<h2 id="3-消费者生产者模式"><strong>3、消费者生产者模式</strong></h2>
<figure data-type="image" tabindex="21"><img src="//tvax4.sinaimg.cn/mw690/007bjbYxly1goo7avf9m9j31ar0n8wo3.jpg" alt="image-20210315201440570" loading="lazy"></figure>
<p><strong>分析问题</strong></p>
<figure data-type="image" tabindex="22"><img src="//tvax3.sinaimg.cn/mw690/007bjbYxly1goo7b13sqqj317r0pwk0m.jpg" alt="image-20210315201451306" loading="lazy"></figure>
<p><strong>常用方法</strong></p>
<figure data-type="image" tabindex="23"><img src="//tva3.sinaimg.cn/mw690/007bjbYxly1goo7b64smhj317l0pv7j3.jpg" alt="image-20210315201509636" loading="lazy"></figure>
<pre><code class="language-java">package com.commun;

import com.sun.org.apache.bcel.internal.generic.NEW;

//测试：生产者消费者模型-&gt;利用缓冲区解决：管程法
public class TestPC {
    public static void main(String[] args) {
        synContioner synContioner = new synContioner();
        new Thread(new Producer(synContioner)).start();
        new Thread(new Consumer(synContioner)).start();
    }
}
//生产者
class Producer implements Runnable{
    synContioner synContioner;

    public Producer(com.commun.synContioner synContioner) {
        this.synContioner = synContioner;
    }

    @Override
    public void run() {
        //生产者生产
        for (int i = 1; i &lt;= 30; i++) {
            synContioner.push(new Food(i));
            System.out.println(&quot;生产者生产了第&quot;+i+&quot;个食物&quot;);
        }

    }
}
//消费者
class Consumer implements Runnable{
    synContioner synContioner;

    public Consumer(synContioner synContioner) {
        this.synContioner = synContioner;
    }

    @Override
    public void run() {
        //消费者消费
        for (int i = 1; i &lt; 30; i++) {
            System.out.println(&quot;消费者吃了第&quot;+synContioner.pop().id+&quot;个食物&quot;);
        }

    }

}
//食品
class Food{
    int id;//食品编号

    public Food(int id) {
        this.id = id;
    }
}
//缓冲区
class synContioner{
    //需要一个容器并有一定的大小
    Food[] foods=new Food[5];
    //计数器
    int count=0;
    // 生产者放入食品
    public synchronized void push(Food food){
        //如果容器满了就需要等待消费者消费
        while (count==foods.length){
            //生产者就需要等待
            try {
                this.wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
        //如果容器还没与偶满 就要放入食品 将food 放入数组当中
        foods[count]=food;
        count++;
        //然后就得通知消费者了
        this.notifyAll();

    }

    //消费者消费食品
    public synchronized Food pop(){
        //判断能否消费
        while (count==0){
            try {
                this.wait();
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
        count--;
        //吃完了再通知生产者
        Food food=foods[count];
        this.notifyAll();
        return food;

        //吃完了，通知生产者生产
    }

}


</code></pre>
<p><strong>注意</strong></p>
<p>应该用while，否则当有多个消费者的时候，会出现脏判断的</p>
<p>if语句中醒来的线程 不会再一次进行判断了 而while会重新再判断</p>
<h2 id="线程池">线程池</h2>
<figure data-type="image" tabindex="24"><img src="//tva2.sinaimg.cn/mw690/007bjbYxly1goo7bexypjj318a0qiwpj.jpg" alt="image-20210315210736862" loading="lazy"></figure>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[JVM虚拟机]]></title>
        <id>https://lindamao0753.github.io/post/jvm-xu-ni-ji/</id>
        <link href="https://lindamao0753.github.io/post/jvm-xu-ni-ji/">
        </link>
        <updated>2021-03-13T08:07:47.000Z</updated>
        <content type="html"><![CDATA[<h1 id="jvm初探">JVM初探</h1>
<h2 id="1-jvm的位置">1、JVM的位置</h2>
<figure data-type="image" tabindex="1"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210129163223110.png" alt="image-20210129163223110" loading="lazy"></figure>
<ul>
<li>
<p>简单的来说就时JVM是运行java程序的一个<strong>环境</strong> 你也可以理解成是一个<strong>软件</strong></p>
</li>
<li>
<p>它存在于操作系统的上面  所有说只要有这个环境 不管你在哪个操作系统上面都能够运行这个程序。</p>
</li>
</ul>
<h2 id="2-jvm体系结构">2、JVM体系结构</h2>
<figure data-type="image" tabindex="2"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210131113845513.png" alt="image-20210131113845513" loading="lazy"></figure>
<p>栈，本地方法栈，程序计数器 不会存在垃圾回收</p>
<p>调优大部分时间都是在调堆</p>
<p>本地方法接口：JNI（<strong>Java Native Interface</strong>）</p>
<h2 id="3-类加载器">3、类加载器</h2>
<p><strong>作用：加载Class文件。</strong></p>
<figure data-type="image" tabindex="3"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210129173753042.png" alt="image-20210129173753042" loading="lazy"></figure>
<p>查看类加载器可以通过getClassloadr来获取</p>
<p>获取其父的类加载器通过getparent</p>
<p>这些记载器的位置如果在代码中获取不到的话 可以通过java的编译环境jre中进行获取 在lib目录下可以查找的到</p>
<ul>
<li>1、虚拟机自带的加载器</li>
<li>2、启动类（根）加载器【BOOT】</li>
<li>3、扩展类加载器【EXT】</li>
<li>4、应用程序加载器【APP】</li>
</ul>
<p><strong>注：从4到1进行加载 从下往上进行一次加载</strong></p>
<p>实例化的过程就是实例化对象就是new</p>
<p>然后将实例化的对象变回成class 是 通过getClass方法</p>
<p>简单的来说就子女与父母</p>
<p>类模板是抽象的 而对象实例是具体的</p>
<pre><code class="language-java">public class Car(){
    Car car1=new Car();
    Car car2=new Car();
    Car car3=new Car();

}
</code></pre>
<p>这个car1 2 3对象的名字是存在在栈里面 然后其真正的数据存在堆里面</p>
<p>因为栈是引用地址</p>
<h3 id="1-双亲委派">1、双亲委派</h3>
<p>**解释：双亲委派机制（安全）：APP—&gt;EXT—&gt;BOOT【最终执行】 **</p>
<p><strong>流程</strong></p>
<ol>
<li>
<p>类加载器收到类加载的请求</p>
</li>
<li>
<p>将这个请求向上委托为父类加载器去完成，一直向上委托，直到启动类加载器</p>
</li>
<li>
<p>启动类加载器检查是否能够加载当前的这个类，能加载就结束，使用当前的加载器，否则抛出异常，通知子加载器进行加载</p>
<p>简单的来说就是小的就想偷懒来让大的去干事情 除非是大的干不了才让小的去干！</p>
</li>
<li>
<p>重复步骤 3</p>
</li>
</ol>
<p>最经典的错误就是class Not Found</p>
<p>但是BOOT根加载器输出是null，这是java调用不到~</p>
<p>因为java 底层的东西就是 C，C++ 写的</p>
<p>Java = C++-- ：去掉繁琐的东西（指针、内存管理等）</p>
<p><strong>注意：</strong></p>
<p>java只会去调用那些本地的方法 就是<strong>native</strong> 在操作系统之上的或者其他东西实现的java是实现不了的</p>
<pre><code class="language-java">package com.draco.parents;

/**
 * 双亲委派机制
 */
public class Car {
    public static void main(String[] args) {
        Car car1 = new Car();
        Car car2 = new Car();
        Car car3 = new Car();

        //不同的实例
        System.out.println(&quot;car1 hashCode=&quot;+car1.hashCode());
        System.out.println(&quot;car2 hashCode=&quot;+car2.hashCode());
        System.out.println(&quot;car3 hashCode=&quot;+car3.hashCode());

        //同一个类模版
        Class&lt;? extends Car&gt; aClass1 = car1.getClass();
        Class&lt;? extends Car&gt; aClass2 = car2.getClass();
        Class&lt;? extends Car&gt; aClass3 = car3.getClass();

        System.out.println(&quot;aClass1 hashCode=&quot;+aClass1.hashCode());
        System.out.println(&quot;aClass2 hashCode=&quot;+aClass2.hashCode());
        System.out.println(&quot;aClass3 hashCode=&quot;+aClass3.hashCode());

        //由于类模版都是一个，以下就选择一个进行测试
        ClassLoader classLoader = aClass1.getClassLoader();

        System.out.println(classLoader);  //AppClassLoader

        System.out.println(classLoader.getParent());  //ExtClassLoader  所在位置：\jre\lib\ext

        System.out.println(classLoader.getParent().getParent());  //null 1.不存在  2.java程序获取不到  所在位置：rt.jar

    }
}

</code></pre>
<ul>
<li>运行测试结果：</li>
</ul>
<figure data-type="image" tabindex="4"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210129185753713.png" alt="image-20210129185753713" loading="lazy"></figure>
<h3 id="2-面试问题">2、面试问题</h3>
<ul>
<li>
<p>为什么需要双亲委派机制?（也就是双亲委派的优点）</p>
<p>①双亲委派机制使得类加载出现<strong>层级</strong>，父类加载器加载过的类，子类加载器不会重复加载，可以<strong>防止类重复加载</strong>；</p>
<p>②使得类的加载出现优先级，<strong>防止了核心API被篡改</strong>，提升了安全，所以越基础的类就会越上层进行加载，反而一般自己的写的类，就会在应用<strong>程序加载器（Application）直接加载</strong>。</p>
<p>比如我自己重写了一个lang包下面的String类方法 大事执行的时候会找不到main方法 因为它会层层递进的去找 直到找到根加载器中的方法</p>
<p>简单的来说就是你没有资格去改变原本已经在环境下定好的类 除非是你自己将环境中的东西给改掉。安全起见！！</p>
</li>
<li>
<p>如何打破双亲委派？</p>
<p>①<strong>自定义类加载器，重写loadClass方法</strong></p>
<p>②<strong>使用线程上下文类加载器</strong></p>
</li>
</ul>
<h2 id="4-沙箱安全机制">4、沙箱安全机制</h2>
<h3 id="1-什么是沙箱">1、什么是沙箱</h3>
<p>​		沙箱是一个<strong>限制程序运行的环境</strong>。沙箱机制就是将 Java 代码限定在虚拟机(JVM)特定的运行范围中，并且严格限制代码<strong>对本地系统资源访问</strong>，通过这样的措施来保证对代码的有效隔离，防止对本地系统造成破坏。沙箱<strong>主要限制系统资源访问</strong>，那系统资源包括什么？——<mark>CPU、内存、文件系统、网络</mark>。不同级别的沙箱对这些资源访问的限制也可以不一样。</p>
<p>简单的来说就是对资源进行了限制保护 就比如学校不让那些坏蛋来跑到学校里面来 而学校的保安就是沙箱保护机制</p>
<p>​		所有的Java程序运行都可以指定沙箱，可以定制安全策略。</p>
<h3 id="2-组成沙箱的基本组件">2、组成沙箱的基本组件</h3>
<ul>
<li><code>字节码校验器</code>（bytecode verifier）：<strong>确保Java类文件遵循Java语言规范。这样可以帮助Java程序实现内存保护</strong>。但并不是所有的类文件都会经过字节码校验，比如核心类。</li>
<li><code>类装载器</code>（class loader）：其中类装载器在3个方面对Java沙箱起作用
<ul>
<li>它防止恶意代码去干涉善意的代码；//双亲委派机制</li>
<li>它守护了被信任的类库边界；//不让你去调用其他的库</li>
<li>它将代码归入保护域，确定了代码可以进行哪些操作。</li>
</ul>
</li>
</ul>
<p>虚拟机为不同的类加载器载入的类提供不同的命名空间，命名空间由一系列唯一的名称组成，每一个被装载的类将有一个名字，这个命名空间是由Java虚拟机为每一个类装载器维护的，它们互相之间甚至不可见。</p>
<p><strong>类装载器采用的机制是<mark>双亲委派模式</mark>。</strong></p>
<ol>
<li>从最内层JVM自带类加载器开始加载，外层恶意同名类得不到加载从而无法使用；</li>
<li>由于严格通过包来区分了访问域，外层恶意的类通过内置代码也无法获得权限访问到内层类，破坏代码就自然无法生效。</li>
</ol>
<ul>
<li><code>存取控制器</code>（access controller）：存取控制器可以控制核心API对操作系统的存取权限，而这个控制的策略设定，可以由用户指定。</li>
<li><code>安全管理器</code>（security manager）：是核心API和操作系统之间的主要接口。实现权限控制，比存取控制器优先级高。</li>
<li><code>安全软件包</code>（security package）：java.security下的类和扩展包下的类，允许用户为自己的应用增加新的安全特性，包括：
<ul>
<li>安全提供者</li>
<li>消息摘要</li>
<li>数字签名</li>
<li>加密</li>
<li>鉴别</li>
</ul>
</li>
</ul>
<h2 id="5-native">5、Native</h2>
<p>编写一个多线程启动类</p>
<pre><code class="language-java">public static void main(String[] args) {
    new Thread(() -&gt; {},&quot;Thread1&quot;).start();
}
</code></pre>
<p>点进去start方法</p>
<pre><code class="language-java">public synchronized void start() {

    if (threadStatus != 0)
        throw new IllegalThreadStateException();

    group.add(this);

    boolean started = false;
    try {
        start0(); // 调用了start0()方法
        started = true;
    } finally {
        try {
            if (!started) {
                group.threadStartFailed(this);
            }
        } catch (Throwable ignore) {

        }
    }
}
// 凡是带了native关键字的，就说明Java的作用范围达不到了，会去调用底层C语言的库
private native void start0(); //start0()方法的定义，这个方法会调用底层C
</code></pre>
<p>Java在内存区域中专门开辟了一块标记区域——<strong>本地方法栈</strong>，用来<strong>登记native方法</strong>，凡是带了native关键字的，会进入到本地方法栈中，<strong>调用本地方法接口（JNI），在最终执行的时候，加载本地方法库中的方法通过JNI</strong></p>
<figure data-type="image" tabindex="5"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210131125351239.png" alt="image-20210131125351239" loading="lazy"></figure>
<ul>
<li>JNI的作用：<strong>扩展Java的使用，融合不同的编程语言为Java所用</strong>，不过最初是想融合C，C++的，因为Java诞生的时候，C，C++横行，想要立足的话就要有能调用C的程序。</li>
<li>本地方法栈：具体做法是，在Native Method Stack中<strong>登记native</strong>方法，<strong>在执行引擎执行的时候加载Native Libraies【本地库】</strong>，加载本地方法库的方通过JNI</li>
<li>所以当调入其他的接口的时候: native可以完成我们想要的操作。</li>
</ul>
<h2 id="6-pc寄存器">6、PC寄存器</h2>
<p>程序计数器：Program Counter Register</p>
<p>​		每个线程都有一个程序计数器， 是线程私有的，就是一个<strong>指针</strong>， 指向方法区中的方法字节码(用来存储指向像一条指令的地址， 也即将要执行的指令代码)，在执行引擎读取下一条指令, 是一个非常小的内存空间，几乎可以忽略不计 。作用是用来计算程序的数量。</p>
<h2 id="7-方法区和元空间和永久代">7、方法区和元空间和永久代</h2>
<p>方法区：Method Area</p>
<p>​		方法区是被所有线程共享，所有字段和方法字节码，以及一些特殊方法，如构造函数，接口代码也在此定义，简单说，所有定义的方法的信息都保存在该区域，<strong>此区域属于共享区间</strong>；</p>
<p>​		<strong>静态变量、常量、类信息(构造方法、接口定义)、运行时的常量池存在方法区中，但是实例变量存在堆内存中，和方法区无关</strong>。</p>
<p>简单的来说就是：==<strong>static、final、Class、常量池=</strong>=</p>
<p>⽅法区与 Java 堆⼀样，是各个线程共享的内存区域，它⽤于存储已被虚拟机加载的类信息、常量、静态变量、即时编译器编译后的代码等数据。虽然 Java 虚拟机规范把⽅法区描述为堆的⼀个逻辑部分，但是它却有⼀个别名叫做 Non-Heap（⾮堆），⽬的应该是与 Java 堆区分开来。</p>
<h3 id="方法区和永久代的关系">⽅法区和永久代的关系</h3>
<p>《Java 虚拟机规范》只是规定了有⽅法区这么个概念和它的作⽤，并没有规定如何去实现它。那么，在不同的 JVM 上⽅法区的实现肯定是不同的了。 <strong>⽅法区和永久代的关系很像Java 中接⼝和类的关系，类实现了接⼝，⽽永久代就是 HotSpot 虚拟机对虚拟机规范中⽅法区的⼀种实现⽅式。</strong> 也就是说，永久代是 HotSpot 的概念，⽅法区是 Java 虚拟机规范中的定义，是⼀种规范，⽽永久代是⼀种实现，⼀个是标准⼀个是实现。</p>
<p>永久代是一种实现  实现了方法区当中的定义。</p>
<p><strong>其他的虚拟机实现并没有永久代这⼀说法。</strong></p>
<p>JDK 1.8 之前永久代还没被彻底移除的时候通常通过下⾯这些参数来调节⽅法区⼤⼩</p>
<pre><code>-XX:PermSize=N //⽅法区 (永久代) 初始⼤⼩
-XX:MaxPermSize=N //⽅法区 (永久代) 最⼤⼤⼩,超过这个值将会抛出 OutOfMemoryError 异
常:java.lang.OutOfMemoryError: PermGen
</code></pre>
<p>相对⽽⾔，垃圾收集⾏为在这个区域是比较少出现的，但并⾮数据进⼊⽅法区后就“永久存在”了。</p>
<p>JDK 1.8 的时候，⽅法区（HotSpot 的永久代）被彻底移除了（JDK1.7 就已经开始了），<strong>取⽽代之是元空间，元空间使⽤的是直接内存</strong>。<br>
下⾯是⼀些常⽤参数：</p>
<p>-XX:MetaspaceSize=N //设置 Metaspace 的初始（和最⼩⼤⼩）<br>
-XX:MaxMetaspaceSize=N //设置 Metaspace 的最⼤大小</p>
<p><strong>元空间与永久代很⼤的不同就是，如果不指定⼤⼩的话，随着更多类的创建，虚拟机会耗尽所有可⽤的系统内存</strong></p>
<h3 id="为什么要将永久代-permgen-替换为元空间-metaspace-呢">为什么要将永久代 (PermGen) 替换为元空间 (MetaSpace) 呢?</h3>
<ol>
<li>整个永久代有⼀个 JVM 本身设置<strong>固定⼤⼩上限</strong>，⽆法进⾏调整，⽽元空间使⽤的是直接内存，受本机可⽤内存的限制，虽然元空间仍旧可能溢出，但是⽐原来出现的⼏率会更⼩。当你元空间溢出时会得到如下错误：</li>
</ol>
<p><strong>java.lang.OutOfMemoryError: MetaSpace</strong></p>
<p>你可以使⽤ -XX MaxMetaspaceSize 标志设置最⼤元空间⼤⼩，默认值为 unlimited，这意味着它只受系统内存的限制。 -XX MetaspaceSize 调整标志定义元空间的初始⼤⼩如果未指定此标志，则 Metaspace 将根据运⾏时的应⽤程序需求动态地重新调整⼤⼩。</p>
<ol start="2">
<li>元空间⾥⾯存放的是类的元数据，这样加载多少类的元数据就不由 MaxPermSize 控制了,<br>
⽽由系统的实际可⽤空间来控制，这样能加载的类就更多了。</li>
<li>在 JDK8，合并 HotSpot 和 JRockit 的代码时, JRockit 从来没有⼀个叫永久代的东⻄, 合并<br>
之后就没有必要额外的设置这么⼀个永久代的地⽅了。</li>
</ol>
<p>总结就是永久代的大小受JVM本身内存的限制 而元空间就可以动态的调整内存的大小</p>
<p>使得发生OOM的情况更少。</p>
<h3 id="运行时常量池">运行时常量池</h3>
<p>运⾏时常量池是⽅法区的⼀部分。Class ⽂件中除了有类的版本、字段、⽅法、接⼝等描述信息外，还有常量池表（⽤于存放编译期⽣成的各种字⾯量和符号引⽤）<br>
既然运⾏时常量池是⽅法区的⼀部分，⾃然受到⽅法区内存的限制，当常量池⽆法再申请到内存时会抛出 OutOfMemoryError 错误。</p>
<p><strong>修正：</strong></p>
<ol>
<li>JDK1.7之前运⾏时常量池逻辑包含字符串常量池存放在⽅法区, 此时hotspot虚拟机对⽅法区的实现为永久代</li>
<li>JDK1.7 字符串常量池被从⽅法区拿到了堆中, 这⾥没有提到运⾏时常量池,也就是说字符串常量池被单独拿到堆,运⾏时常量池剩下的东⻄还在⽅法区, 也就是hotspot中的永久代 。</li>
<li>JDK1.8 hotspot移除了永久代⽤元空间(Metaspace)取⽽代之, <strong>这时候字符串常量池还在堆, 运⾏时常量池还在⽅法区, 只不过⽅法区的实现从永久代变成了元空间</strong><br>
<strong>(Metaspace)</strong></li>
</ol>
<h3 id="直接内存">直接内存</h3>
<p>直接内存并不是虚拟机运⾏时数据区的⼀部分，也不是虚拟机规范中定义的内存区域，但是这部分内存也被频繁地使⽤。⽽且也可能导致 OutOfMemoryError 错误出现。JDK1.4 中新加⼊的 NIO(New Input/Output) 类，引⼊了⼀种基于通道（Channel） 与缓存区（Buffer） 的 I/O ⽅式，它可以直接使⽤ Native 函数库直接分配堆外内存，然后通过⼀个存储<br>
在 Java 堆中的 DirectByteBuffer 对象作为这块内存的引⽤进⾏操作。这样就能在⼀些场景中显著提⾼性能，因为避免了在 Java 堆和 Native 堆之间来回复制数据。<br>
本机直接内存的分配不会受到 Java 堆的限制，但是，既然是内存就会受到本机总内存⼤⼩以及处理器寻址空间的限制。</p>
<h2 id="8-栈">8、栈</h2>
<ul>
<li>
<p>程序 = 数据结构 + 算法 【持续学习】</p>
</li>
<li>
<p>大多数人程序 = 框架 + 业务逻辑【饭碗】</p>
</li>
</ul>
<p><strong>栈</strong>：先进后出，后进先出 （LIFO：last input first output）</p>
<p><strong>队列</strong>：先进先出（FIFO：first input first output）</p>
<p><strong>栈</strong>：栈内存，<strong>主管程序的运</strong>行，生命周期和线程同步；</p>
<p>main（）方法是最先执行，最后结束。</p>
<p>线程结束，栈内存也就释放了，对于栈来说<mark>不存在垃圾回收问题</mark>，</p>
<p>一旦线程结束，栈就Over了 意思就是当栈方法当中的事情都干完之后就会让他们都滚蛋释放出去。</p>
<h3 id="1-栈里面存放什么">1、栈里面存放什么</h3>
<p>栈：<strong>8大基本类型</strong> + 对象的<strong>引用</strong> + <strong>实例的方法</strong></p>
<h3 id="2-栈运行原理">2、栈运行原理</h3>
<p><strong>栈帧</strong></p>
<figure data-type="image" tabindex="6"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201182918164.png" alt="image-20210201182918164" loading="lazy"></figure>
<p>下面的子帧调用上面的 上面的父帧调用下面的</p>
<p>实行每一个方法都会产生一个栈帧。</p>
<p>所以说正在执行的程序一定在栈的上面的位置 也就是栈顶。</p>
<figure data-type="image" tabindex="7"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210310150551148.png" alt="image-20210310150551148" loading="lazy"></figure>
<p>栈满了：就会抛出错误 StackOverflowError</p>
<pre><code class="language-java">package com.lin.my;
public class myTest2 {
    public static void main(String[] args) {
        new myTest2().a();
    }
    public void a(){
        b();
    }
    public void b(){
        a();
    }
}
</code></pre>
<figure data-type="image" tabindex="8"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210310150931634.png" alt="image-20210310150931634" loading="lazy"></figure>
<p>这里的线程在mian方法中不断的被调用执行 被不断的放入栈中 最后栈的空间不够方法的存放 所以会抛出栈溢出的错误。</p>
<h3 id="3-栈堆方法区的交互关系">3、栈堆方法区的交互关系</h3>
<figure data-type="image" tabindex="9"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201185248813.png" alt="image-20210201185248813" loading="lazy"></figure>
<h3 id="4java对象在内存中实例化的过程">4.Java对象在内存中实例化的过程</h3>
<p>在讲 Java 对象在内存中的实例化过程前，先来说下在类的实例化过程中，内存会使用到的三个区域：栈区、堆区、方法区。</p>
<p><strong>堆区：</strong><br>
存储的全部都是<strong>对象</strong>，每个对象包含了一个与之对应的 class 类的信息。</p>
<p>​    jvm 只有<strong>一个</strong>堆区(steap)，它会被<strong>所有线程共享</strong>，<strong>堆中不存放基本数据类型和对象引用，它只存放对象本身。<strong>就是真实的对象<br>
<strong>栈区:</strong><br>
​	<strong>每个线程都包含一个栈区</strong>，栈中</strong>只保存基本数据类型的值和对象以及基础数据的引用</strong>。<br>
​</p>
<p>​	每个栈中的数据（基本数据类型和对象的引用）都是<strong>私有</strong>的，<strong>其它栈是无法进行访问的</strong>。<br>
​</p>
<p>​	栈分为<strong>三个部分</strong>：<strong>基本类型变量区、执行环境上下文、操作指令区(存放操作指令)</strong>。</p>
<p><strong>方法区：</strong></p>
<p>​	又被称为<strong>静态区</strong>，它跟堆一样，被所有的线程<strong>共享</strong>，方法区包含所有的 class 信息 和 static修饰的变量。<br>
​</p>
<p>​	<strong>方法区中包含的都是整个程序中永远唯一的元素，如：class、static变量</strong>。<br>
<strong>简谈 JDK8 前后 JVM 内存变化</strong><br>
JDK8 之前对内存划分为:新生代（YOUNG）—老年代（Tenured）—永久代(PermGen)<br>
<strong>新生代：</strong><br>
新生代又分为伊甸区（Eden） 存活区(Survivor)，其中存活区又分为两个大小空间一样的s0、s1，而且s0 和 s1 可以互相转化，存活区保存的一定是在伊甸区保存了很久的，并且经过好几次小的GC还存活下来的对象，存活区一定会有两块大小相等的空间。目的是一块存活区未来的晋升，另一块存活区是为了对象的回收。需要注意的是：<strong>这两块存活区一定有一块是空的。</strong></p>
<p><strong>新生代中的 GC:</strong><br>
新生代大小（PSYoungGen total 9216K）=eden大小（eden space 8192K）+1个survivor大小（from space 1024K）</p>
<p>HotSpot JVM把年轻代分为了三部分：1个Eden区和2个Survivor区（分别叫from和to）。默认比例为8（Eden）：1（一个survivor）,一般情况下，新创建的对象都会被分配到Eden区(一些大对象特殊处理),这些对象经过第一次Minor GC后，如果仍然存活，将会被移到Survivor区。对象在Survivor区中每熬过一次Minor GC，年龄就会增加1岁，当它的年龄增加到一定程度时，就会被移动到老年代中。<br>
　　因为年轻代中的对象基本都是朝生夕死的(80%以上)，所以在年轻代的垃圾回收算法使用的是复制算法，复制算法的基本思想就是将内存分为两块，每次只用其中一块，当这一块内存用完，就将还活着的对象复制到另外一块上面。复制算法不会产生内存碎片。<br>
在GC开始的时候，对象只会存在于Eden区和名为“From”的Survivor区，Survivor区“To”是空的。紧接着进行GC，Eden区中所有存活的对象都会被复制到“To”，而在“From”区中，仍存活的对象会根据他们的年龄值来决定去向。年龄达到一定值(年龄阈值，可以通过-XX:MaxTenuringThreshold来设置)的对象会被移动到年老代中，没有达到阈值的对象会被复制到“To”区域。经过这次GC后，Eden区和From区已经被清空。这个时候，“From”和“To”会交换他们的角色，也就是新的“To”就是上次GC前的“From”，新的“From”就是上次GC前的“To”。不管怎样，都会保证名为To的Survivor区域是空的。Minor GC会一直重复这样的过程，直到“To”区被填满，“To”区被填满之后，会将所有对象移动到老年代中。</p>
<p><strong>为什么要设置两个Survivor区？</strong></p>
<p>设置两个Survivor区最大的好处就是解决了碎片化；<br>
假设现在只有一个survivor区，我们来模拟一下流程：<br>
刚刚新建的对象在Eden中，一旦Eden满了，触发一次Minor GC，Eden中的存活对象就会被移动到Survivor区。这样继续循环下去，下一次Eden满了的时候，问题来了，此时进行Minor GC，Eden和Survivor各有一些存活对象，如果此时把Eden区的存活对象硬放到Survivor区，很明显这两部分对象所占有的内存是不连续的，也就导致了内存碎片化。</p>
<p>碎片化带来的风险是极大的，严重影响Java程序的性能。堆空间被散布的对象占据不连续的内存，最直接的结果就是，堆中没有足够大的连续内存空间，接下去如果程序需要给一个内存需求很大的对象分配内存。。。画面太美不敢看。。。这就好比我们上学时背包里所有东西紧挨着放，最后就可能省出一块完整的空间放饭盒。如果每件东西之间隔一点空隙乱放，很可能最后就要手提一路了。</p>
<p>1.1、Java 中的数据类型<br>
Java 中的数据类型有两种：</p>
<p>1、基本类型（primitive types）: 共有8种，即：int、short、long、byte、char、float、double、boolean（注意并没有 String 的基本类型），这8中类型的定义是通过诸如：int a = 5；long b = 22L;的形式来定义的，称为自动变量。<strong>注意：自动变量存的是字面值，</strong></p>
<p><strong>不是类的实例，即不是类的引用，这里并没有类的存在</strong>；</p>
<p>如：int a = 5; 这里的 a 是一个指向 int 类型的引用，指向 5 这个字面值，这些字面值的数据由于大小可知，生存期可知（ 这些字面值固定定义在某个程序块里面，程序块退出后，字段值就消失了 ），出于追求速度的原因，这些字面值就存在于栈区中；</p>
<p>另外，<strong>栈有一个很重要的特殊性，就是存在栈中的数据可以共享</strong>。假设我们同时定义<br>
　 　int a = 3;<br>
　　 int b = 3；<br>
　　 编译器先处理int a = 3；首先它会在栈中创建一个变量为a的引用，然后查找有没有字面值为3的地址，没找到，就开辟一个存放3这个字面值的地址，然后将a指向3的地址。接着处理int b = 3；在创建完b的引用变量后，由于在栈中已经有3这个字面值，便将b直接指向3的地址。这样，就出现了a与b同时均指向3的情况。</p>
<p>特别注意的是：这种<strong>字面值的引用与类对象的引用不同</strong>。假定两个类对象的引用同时指向一个对象，如果一个对象引用变量修改了这个对象的内部状态，那么另一个对象引用变量也即刻反映出这个变化。相反，通过字面值的引用来修改其值，不会导致另一个指向此字面值的引用的值也跟着改变的情况。如上例，我们定义完a与 b的值后，再令a=4；那么，b不会等于4，还是等于3。在编译器内部，遇到a=4；时，它就会重新搜索栈中是否有4的字面值，如果没有，重新开辟地址存放4的值；如果已经有了，则直接将a指向这个地址。因此a值的改变不会影响到b的值。</p>
<p>简单的说就是不是类创建的对象在后面改变字面值的引用之后并不会影响原来的数据 但是如果是类创建的对象就会收到影响。</p>
<p>2、 包装类数据：如：String、Integer、Double等将相应的基本数据类型包装起来的类，这些数据全部存放在 堆 中， Java 用 new()语句来显示地告诉编译器，在运行时才根据需要动态创建，因此比较灵活，但缺点是要占用更多的时间。</p>
<p>1.2、类实例化时内存中发生的变化<br>
首先我们先对下面的代码进行分析：</p>
<pre><code class="language-java">public class People{
    String name; // 定义一个成员变量 name
    int age; // 成员变量 age
    Double height; // 成员变量 height
    void sing(){
        System.out.println(&quot;人的姓名：&quot;+name);
        System.out.println(&quot;人的年龄：&quot;+age);
        System.out.println(&quot;人的身高：&quot;+height);
    }
    

public static void main(String[] args) {
    String name; // 定义一个局部变量 name
	int age; // 局部变量 age
	Double height; // 局部变量 height
    
    People people = new People() ; //实例化对象people
    people.name = &quot;张三&quot; ;       //赋值
    people.age = 18;             //赋值
    people.stuid = 180.0 ;   //赋值
    people.sing();              //调用方法sing

}
    }
</code></pre>
<p>代码解析：</p>
<p>这段代码首先定义三个成员变量：String name、int age、Double height 这三个变量都是只声明了没有初始化，然后定义了一个成员方法 sing();</p>
<p>在 main()方法里同样定义了三个一样的变量，只不过这些是<strong>局部变量</strong>；</p>
<p>在main() 函数里实例化对象 people , <strong>内存中在堆区内会给实例化对象 people 分配一片地址</strong>，紧接着我们对实例化对象 people 进行了赋值。people 调用成员方法 sing() 。mian()函数打印输入人的姓名，人的年龄和人的身高，系统执行完毕。</p>
<p><strong>下面通过图解法展示实例化对象的过程中内存的变化：</strong></p>
<figure data-type="image" tabindex="10"><img src="https://img-blog.csdnimg.cn/20200727150524513.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>在程序的执行过程中，<strong>首先类中的成员变量和方法体会进入到方法区</strong>，如图:</p>
<figure data-type="image" tabindex="11"><img src="https://img-blog.csdnimg.cn/20200727150545479.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>程序执行到 main() 方法时，<strong>main()函数方法体会进入栈区</strong>，这一过程叫做进栈(压栈)，定义了一个用于指向 Person 实例的变量 person。如图：</p>
<figure data-type="image" tabindex="12"><img src="https://img-blog.csdnimg.cn/20200727150608130.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>程序执行到 Person person = new Person();</p>
<p>就会在<strong>堆</strong>内存开辟一块内存区间，用于存放 <strong>Person 实例对象</strong>，<strong>然后将成员变量和成员方法放在 new 实例中都是取成员变量&amp;成员方法的地址值 如图：</strong></p>
<figure data-type="image" tabindex="13"><img src="https://img-blog.csdnimg.cn/20200727150630116.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>接下来对 person 对象进行赋值， person.name = “小二” ; perison.age = 13; person.height= 180.0;</p>
<p>先在栈区找到 person，然后根据地址值找到 new Person() 进行赋值操作。</p>
<p>如图：</p>
<figure data-type="image" tabindex="14"><img src="https://img-blog.csdnimg.cn/2020072715065299.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>当程序走到 sing() 方法时，先到<strong>栈区</strong>找到 person这个引用变量，然后根据<strong>该地址值在堆内存中找到 new Person() 进行方法调用</strong>。</p>
<p>在方法体void speak()被调用完成后，就会立刻马上从栈内弹出（出栈 )</p>
<p>最后，在main()函数完成后，main()函数也会出栈 如图：</p>
<figure data-type="image" tabindex="15"><img src="https://img-blog.csdnimg.cn/20200727150715386.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L1RvbnlfX0phYQ==,size_16,color_FFFFFF,t_70" alt="img" loading="lazy"></figure>
<p>以上就是Java对象在内存中实例化的全过程。</p>
<p>总结：</p>
<p>可以简单的理解就是 方法区就是商品区 成员变量和成员方法放在这个地方 然后客户想要买这些东西必须通过老板指定的地方就是栈中行咨询 等待 然后老板给我们查看这些东西的价钱 堆中存放的地址我们通过栈的引用去获取  也就是说 我们必须引用和值一一对应才能拿到我们想要的东西 就比如钥匙必须放在和它相符的门上才能打的开</p>
<h2 id="9-三种jvm">9、三种JVM</h2>
<p>通过命令查看对应的版本</p>
<p>java -version</p>
<figure data-type="image" tabindex="16"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210310153537996.png" alt="image-20210310153537996" loading="lazy"></figure>
<ul>
<li>Sun公司 HotSpot <code>Java HotSpot(TM) 64-Bit Server VM (build 25.121-b13, mixed mode)</code>（我们用的 基本都是第一个）</li>
<li>BEA <code>JRockit</code></li>
<li>IBM <code>J9VM</code></li>
</ul>
<h2 id="10-堆">10、堆</h2>
<p><strong>Heap</strong>，一个JVM只有一个<strong>堆内存</strong>，堆内存的<strong>大小是可以调节</strong>的。</p>
<p>下面会提到堆调节大小的相关操作。</p>
<h3 id="1-堆里面存放什么">1、堆里面存放什么</h3>
<p>类加载器读取了类文件后，一般会把什么东西放在堆中？，<strong>类，方法，常量，变量</strong>。保存我们所有引用类型的<strong>真实对象</strong></p>
<p>堆内存中还要细分为三个区域：</p>
<ul>
<li>新生区（伊甸园区）young/new</li>
<li>养老区 old</li>
<li>永久区 perm</li>
</ul>
<p><mark>GC垃圾回收，主要在<strong>伊甸园区</strong>和<strong>养老区</strong></mark></p>
<figure data-type="image" tabindex="17"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201210010927.png" alt="image-20210201210010927" loading="lazy"></figure>
<p>GC垃圾回收，主要是在伊甸园区和养老区。</p>
<p>假设内存满了，就会出现OOM错误，堆内存不够！<code>java.lang.OutOfMemoryError: Java heap space</code></p>
<pre><code class="language-java">package com.draco.heapOverflow;
import java.util.Random;

/**
 * 堆溢出
 */
public class HeapOver {
    public static void main(String[] args) {
        String name = &quot;adfjkllkjfdsa&quot;;
        while(true){
            name += name + new Random().nextInt(888888888)+new Random().nextInt(999999999);
        }
    }
}
</code></pre>
<figure data-type="image" tabindex="18"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201212708979.png" alt="image-20210201212708979" loading="lazy"></figure>
<p>在JDK8以前的时候名字不变。所以上面的图就是JDK8以前的东西。</p>
<p>在JDK8以后，永久存储区改了个名字叫：<strong><mark>元空间</mark></strong></p>
<h3 id="2-新生区">2、新生区</h3>
<ul>
<li>新生区：<strong>类诞生和成长的地方</strong>，<strong>甚至死亡</strong>；
<ul>
<li>伊甸园区，所有对象都是在伊甸园区new出来的</li>
<li>幸存者区（0、1）</li>
</ul>
</li>
</ul>
<figure data-type="image" tabindex="19"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201215342076.png" alt="image-20210201215342076" loading="lazy"></figure>
<p>真理：进过研究，99%的对象都是临时对象！（就是用一次就再也不会去调用它） 也就是说进养老区的东西也就不多 出现OOM情况的次数也就不是很多。</p>
<h3 id="3-养老区">3、养老区</h3>
<p><strong>⼤对象直接进⼊⽼年代</strong><br>
⼤对象就是需要⼤量连续内存空间的对象（⽐如：字符串、数组）。</p>
<p>新生区没干掉，没杀死的来到了养老区~</p>
<h3 id="4-永久区">4、永久区</h3>
<p>这个区域是常驻内存的。用来存放JDK自身携带的Class对象，Interface元数据，存储的是Java运行时的一些环境或类信息，这个区域不存在垃圾回收！</p>
<p>因此这个区很重要  如果没有这个东西java就运行不了。</p>
<p>当关闭VM虚拟机就会释放这个区域的内存。</p>
<p>在永久区出现OOM的情况：</p>
<p>一个启动类加载了大量的第三方jar包；Tomcat部署了太多的应用；大量动态生成的反射类等 <strong>不断的被加载</strong>，永久区放不下了，直到内存满，就会出现OOM。</p>
<ul>
<li>jdk1.6 之前：永久代，常量池是在<strong>方法区</strong>中；</li>
<li>jdk1.7         ：永久代，但是慢慢退化了，<code>去永久代</code>，常量池在<strong>堆</strong>中</li>
<li>jdk1.8 之后：<strong>无永久代</strong>，<strong>常量池在元空间</strong>。</li>
<li>java8中，<strong>取消永久代，方法存放于元空间(Metaspace)，元空间仍然与堆不相连，但与堆共享物理内存，逻辑上可认为在堆中 所以说元空间有些人还会说是非堆 但是元空间和堆还是有一定的联系的。</strong></li>
</ul>
<figure data-type="image" tabindex="20"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201222307209.png" alt="image-20210201222307209" loading="lazy"></figure>
<p>但是，元空间：<strong>逻辑上存在</strong>，<strong>物理上不存在</strong>  在右边才可以理解成为真正的堆。</p>
<pre><code class="language-java">package com.draco.heapOverflow;

/**
 * 元空间逻辑上存在，物理上不存在
 */
public class SanQu {
    public static void main(String[] args) {
        // 返回jvm试图使用的最大内存
        long max = Runtime.getRuntime().maxMemory();//返回的是字节
        // 返回jvm的初始化的总内存
        long total = Runtime.getRuntime().totalMemory();

        System.out.println(&quot;max=&quot;+max+&quot;字节\t&quot;+(max/(1024*1024))+&quot;MB&quot;);
        System.out.println(&quot;total=&quot;+total+&quot;字节\t&quot;+(total/(1024*1024))+&quot;MB&quot;);

        //默认情况下，试图分配的最大内存是电脑内存的1/4，而初始化的内存是1/64
        // -Xms1024m -Xmx1024m -XX:+PrintGCDetails
    }
}
</code></pre>
<p>运行结果：</p>
<figure data-type="image" tabindex="21"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201230739376.png" alt="image-20210201230739376" loading="lazy"></figure>
<p>在类编辑处可以修参数</p>
<figure data-type="image" tabindex="22"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210310161812663.png" alt="image-20210310161812663" loading="lazy"></figure>
<p>当修改了VM选项后：<code>-Xms1024m -Xmx1024m -XX:+PrintGCDetails</code>，输出结果：调虚拟机的最大内存和初始内存 并将详细的信息输出出来。</p>
<figure data-type="image" tabindex="23"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210201230953431.png" alt="image-20210201230953431" loading="lazy"></figure>
<p>从这里可以看到幸存区域内的东西是不断的from 和 to  的过程 ，也就是他们不断的动态过程 当这些都完成之后才能到养老区当中。</p>
<p>经过这次 GC 后，Eden 区和&quot;From&quot;区已经被清空。这个时候，&quot;From&quot;和&quot;To&quot;会交换他们的⻆<br>
⾊，也就是新的&quot;To&quot;就是上次 GC 前的“From”，新的&quot;From&quot;就是上次 GC 前的&quot;To&quot;。不管怎样，<br>
都会保证名为 To 的 Survivor 区域是空的。Minor GC 会⼀直重复这样的过程，直到“To”区被填<br>
满，&quot;<strong>To&quot;区被填满之后</strong>，会将所有对象移动到⽼年代中。</p>
<p><strong>让我们来算一笔账，</strong></p>
<p>新生区：305664k；养老区：699392k</p>
<p>加在一起：1,005,056k，除以1024后 = 981.5MB，等于jvm试图分配的最大内存</p>
<p><strong>所以说元空间逻辑上存在，物理上不存在</strong>。因为</p>
<p>堆内并没有为他们分配空间。</p>
<h3 id="5-出现oom">5、出现OOM</h3>
<p>当我们把那些参数的都调小以后可以看到出现了OOM的错误</p>
<figure data-type="image" tabindex="24"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210310163213786.png" alt="image-20210310163213786" loading="lazy"></figure>
<p>我们来分析一下 前面几次是出现晴GC 当轻GC 都完成之后 执行的就是重GC full  GC  当这些走完的时候 轻GC 重GC 和元空间内的东西都走不动的时候 就会出现OOM 的错误。</p>
<p><strong>解决方法：</strong>（调优）</p>
<ol>
<li>
<p>尝试扩大堆内存去查看内存结果</p>
<p><strong>通过命令来改变</strong></p>
<p><code>-Xms1024m -Xmx1024m -XX:+PrintGCDetails</code></p>
</li>
<li>
<p>若不行，分析内存，看一下是哪个地方出现了问题（专业工具）</p>
<ul>
<li>能够看到代码第几行出错：内存快照分析工具，MAT（eclipse），Jprofiler</li>
<li>Dubug，一行行分析代码！（不现实）</li>
</ul>
</li>
</ol>
<p><strong>假设在调优过后还是出现了这错误  我们就可以通过专业的工具来实现。</strong></p>
<p>MAT（ecilpse工具下面的），</p>
<p>我们用Jprofiler(IDEA)</p>
<p>Jprofiler作用：</p>
<ul>
<li>分析Dump内存文件，<strong>快速定位内存泄漏</strong></li>
<li><strong>获得堆中的数据</strong></li>
<li><strong>获得大的对象~</strong></li>
<li>......</li>
</ul>
<p>这个是在IDEA的插件下面的 去里面安装 安装完 再去客户端安装后 重启IDEA 才行。保证安装的路径没有中文和空格。弄好了之后还必须在插件当中将环境配置进去。</p>
<p>运行出现堆溢出：</p>
<pre><code class="language-java">package com.draco.heapOverflow;

import java.util.ArrayList;

public class JprofilerTest {
    byte[] array = new byte[1*1024*1024];

    public static void main(String[] args) {
        ArrayList&lt;JprofilerTest&gt; list = new ArrayList&lt;&gt;();
        int count = 0;
        try {
            while(true){
                list.add(new JprofilerTest());
                count++;
            }
        }catch (Exception e){
            System.out.println(&quot;count=&quot;+count);
            e.printStackTrace();
        }
    }
}
</code></pre>
<p>设置VM options <code>-Xms1m -Xmx8m -XX:+HeapDumpOnOutOfMemoryError</code>后，再次运行，通过命令来查看哪里出了问题。</p>
<p>这里要注意的是OOM 是error 不是exception 所以 如果抓取异常的话是抓取不到的  但是错误可以捕获的到</p>
<p>控制台输出：</p>
<figure data-type="image" tabindex="25"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202134131995.png" alt="image-20210202134131995" loading="lazy"></figure>
<p>找到HPROF快照</p>
<figure data-type="image" tabindex="26"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202140158342.png" alt="image-20210202140158342" loading="lazy"></figure>
<p>双击打开</p>
<figure data-type="image" tabindex="27"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202140606282.png" alt="image-20210202140606282" loading="lazy"></figure>
<p>修改代码，把try catch中的Exception改成Error后运行：</p>
<figure data-type="image" tabindex="28"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202172145431.png" alt="image-20210202172145431" loading="lazy"></figure>
<h3 id="6-vm-options参数">6、VM options参数</h3>
<p>-Xms	设置初始化内存分配大小，默认1/64</p>
<p>-Xmx	设置最大分配内存，默认1/4</p>
<p>-XX:+PrintGCDetails	打印GC垃圾回收信息</p>
<p>-XX:+HeapDumpOnOutOfMemoryError	生成oomDump文件</p>
<p><code>-Xms1m -Xmx8m -XX:+HeapDumpOnOutOfMemoryError</code></p>
<p><code>-Xms1024m -Xmx1024m -XX:+PrintGCDetails</code></p>
<h2 id="11-gc垃圾回收">11、GC垃圾回收</h2>
<p><strong>GC 堆（Garbage Collected Heap）.</strong></p>
<h3 id="1-gc的作用区域">1、GC的作用区域</h3>
<figure data-type="image" tabindex="29"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202173224815.png" alt="image-20210202173224815" loading="lazy"></figure>
<p>JVM在进行GC时，并不是对这三个区域统一回收，大部分时候，回收都是<strong>新生代</strong></p>
<ul>
<li>新生代</li>
<li>幸存区（form to）【<strong>会交换的，不是一成不变的</strong>】</li>
<li>老年区</li>
</ul>
<p><strong>GC两种类型</strong>：轻GC（普通的GC），重GC（全局GC）就是全部都要清理</p>
<h3 id="2-gc相关题目">2、GC相关题目</h3>
<ul>
<li>
<p>JVM的内存模型和分区~详细到每个区放什么？</p>
<p>堆 栈  本地方法栈 方法区 程序计数器</p>
<p>堆中放的是真实的对象 <strong>类，方法，常量，变量</strong>。</p>
<p>堆内存中还要细分为三个区域：</p>
<ul>
<li>新生区（伊甸园区）young/new</li>
<li>养老区 old</li>
<li>永久区 perm</li>
</ul>
</li>
</ul>
<p>栈中 放的是栈：<strong>8大基本类型</strong> + 对象的<strong>引用</strong> + <strong>实例的方法</strong></p>
<p>本地方法栈 放的是一些本地的方法</p>
<p>方法区放的是：		<strong>静态变量、常量、类信息(构造方法、接口定义)、运行时的常量池存在方法区 简单的来说就是static  final  class 常量池 但是实例变量存在堆内存中，和方法区无关</strong>。</p>
<ul>
<li>
<p>堆里面的分区有哪些？Eden，from，to，old，说说他们的特点~</p>
<p>Eden区 对象的生死都是从这个里面开始的 from 和 to 区是幸存者区里面的</p>
<p>里面的对象化存活率一般比较低</p>
<p>old区里面的对象的存活率比较高 这里面存活的对象是在新生区当中存活下来的</p>
</li>
<li>
<p>GC的算法有哪些？<strong>标记清除法</strong>，<strong>标记整理/压缩法</strong>，<strong>复制算法</strong>，<strong>引用计数法</strong>，怎么用的？</p>
<p>引用计数算法是 为每一个对象计数器 引用了几次对象就计数几次 并清除掉 没有引用的对象</p>
<p>复制算法：一般发生在新生区 当中 针对于存活率比较低的对象 将里面的对象经过动态的交换 目的是将活着的对象放在from的区当中 让to为空 避免内存碎片的发生</p>
<p>标记清除算法：先扫描内存的对象 并将活着的对象进行标记 并清除掉没有存活的对象</p>
<p>标记整理/压缩算法 先完成永恒标记清除算 然后再扫描一次对象 并将这些对象压缩整理来避免内存碎片化的发生</p>
</li>
<li>
<p>轻GC和重GC分别在什么时候发生？</p>
<p>轻GC是发在新生区当中也就是伊甸区和幸存区的阶段</p>
<p>重GC是发生在养老区当中 当存活率比较高的对象在幸存区装不下的时候就会跑到养老区 当中 此时重GC也将发生。</p>
</li>
<li>
<p>HotSpot 为什么要分为新⽣代和⽼年代？<br>
主要是为了提升 GC 效率。上⾯提到的分代收集算法已经很好的解释了这个问题。</p>
</li>
</ul>
<h3 id="3-gc算法">3、GC算法</h3>
<h4 id="引用计数法">引用计数法</h4>
<figure data-type="image" tabindex="30"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202191444745.png" alt="image-20210202191444745" loading="lazy"></figure>
<p><strong>哪个对象的引用数为0，就会回收哪个对象</strong></p>
<p>简单的来说就是哪个没用就把哪个清除掉 并把每一个有调用的对象加上一个计数器 引用几次 计数器就想对应的加上几次。</p>
<p>缺点是：浪费空间 每个对象都要为其加上计数器  现在都不怎么用了</p>
<h4 id="复制算法">复制算法</h4>
<figure data-type="image" tabindex="31"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202200613995.png" alt="image-20210202200613995" loading="lazy"></figure>
<p><strong>-XX:MaxTenuringThreshold</strong></p>
<p>通过这个参数可以设定进入老年代的时间 最大的时间只能设置到15</p>
<p>一般<strong>新生代（伊甸园区、幸存区</strong>）会使用复制算法，生成新的to区</p>
<p>这就相当于剪切的操作 目的就是把from区里面的东西给放到to区 后让to区保留干净的状态 就是幸存区完成动态的操作之后 to区为空 不能存活下来的就滚出去</p>
<p>存活下来的之后满了之后就要跑到养老区去了 因为没地方住了</p>
<ul>
<li>好处：没有内存的碎片 就不会把东西乱放</li>
<li>坏处：浪费了内存空间，多了一半空间永远是空to</li>
</ul>
<p><mark>复制算法最佳使用场景：<strong>对象存活度较低的时候，也就是新生区</strong></mark></p>
<p><strong>因为只有强者才能留到最后！！！</strong></p>
<p>因为假设对象存活率为100% 那from区很快就会被用完了</p>
<h4 id="标记清除">标记清除</h4>
<p>对活着的对象进行标记</p>
<figure data-type="image" tabindex="32"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202203154896.png" alt="image-20210202203154896" loading="lazy"></figure>
<ul>
<li>缺点：<strong>两次扫描，严重浪费时间，会产生内存碎片</strong> （乱七八糟的第放也就放不下大东西了）</li>
<li>优点：不需要额外的空间 相对于复制算法不需要另一块区域</li>
</ul>
<h4 id="标记压缩针对清除算法的再优化">标记压缩（针对清除算法的再优化）</h4>
<p>对于标记清除的再压缩</p>
<p>将有用的对象放在一起整齐划一 让空间连续就不存在内存碎片了</p>
<figure data-type="image" tabindex="33"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202204042836.png" alt="image-20210202204042836" loading="lazy"></figure>
<p><strong>但是又多了一个移动成本</strong></p>
<h4 id="标记清除压缩">标记清除压缩</h4>
<p>三部曲</p>
<p>先标记清除一次，然后再压缩</p>
<p>也可以标记清除多次以后再进行压缩</p>
<h2 id="总结">总结</h2>
<p>内存效率：复制算法 &gt; 标记清除算法 &gt; 标记压缩算法（时间复杂度）</p>
<p>内存整齐度：复制算法 = 标记压缩算法 &gt; 标记清除算法</p>
<p>内存利用率：标记压缩算法 = 标记清除算法 &gt; 复制算法</p>
<p>思考：难道没有最优算法吗？</p>
<p>答案：没有，没有最好的算法，只有最合适的——&gt;</p>
<p>GC：也被分为分代收集算法</p>
<p>针对不同的代别采用不同的算法</p>
<p><strong>年轻代：</strong></p>
<ul>
<li>存活率低</li>
<li>复制算法</li>
</ul>
<p><strong>老年代：</strong></p>
<ul>
<li>区域大，存活率高</li>
<li>标记清除（内存碎片不是太多） + 标记压缩混合实现</li>
</ul>
<p>JVM调优就是经过这些操作来完成</p>
<p>还是要多看书《深入理解JVM》，花时间去深究，多看面试题。</p>
<h3 id="12运行时常量池">12.运行时常量池</h3>
<p>运⾏时常量池是⽅法区的⼀部分。Class ⽂件中除了有类的版本、字段、⽅法、接⼝等描述信息外，还有常量池表（⽤于存放编译期⽣成的各种字⾯量和符号引⽤）<br>
既然运⾏时常量池是⽅法区的⼀部分，⾃然受到⽅法区内存的限制，当常量池⽆法再申请到内存时会抛出 OutOfMemoryError 错误。</p>
<p><strong>修正：</strong></p>
<ol>
<li>JDK1.7之前运⾏时常量池逻辑包含字符串常量池存放在⽅法区, 此时hotspot虚拟机对⽅法区的实现为永久代</li>
<li>JDK1.7 字符串常量池被从⽅法区拿到了堆中, 这⾥没有提到运⾏时常量池,也就是说字符串常量池被单独拿到堆,运⾏时常量池剩下的东⻄还在⽅法区, 也就是hotspot中的永久代 。</li>
<li>JDK1.8 hotspot移除了永久代⽤元空间(Metaspace)取⽽代之, <strong>这时候字符串常量池还在堆, 运⾏时常量池还在⽅法区, 只不过⽅法区的实现从永久代变成了元空间</strong><br>
<strong>(Metaspace)</strong></li>
</ol>
<h1 id="jmm">JMM</h1>
<ol>
<li>
<p>什么是JMM？</p>
<p>java内存模型</p>
<p><strong>JMM：Java Memory Mod</strong>el的缩写</p>
</li>
<li>
<p>它干嘛的？</p>
<p><strong>作用</strong>：<strong>缓存一致性协议，用于定义数据读写的规则</strong></p>
<p>简单的来说就是协议  说白了就是规则嘛</p>
<p>JMM定义了<strong>线程工作内存和主内存之间的抽象关系</strong>，线程之间的**共享内存存储在主内存（Main Memory）**中，<strong>每个线程都有一个私有的本地内存（Local Memory）</strong></p>
</li>
</ol>
<figure data-type="image" tabindex="34"><img src="%E7%AC%AC%E4%B8%80%E7%89%88.assets/image-20210202211809212.png" alt="image-20210202211809212" loading="lazy"></figure>
<p>主内存只有一个</p>
<p>工作内容=本地内存</p>
<p>关键是下面这个！！！</p>
<p><strong>解决共享对象可见性这个问题：voliate关键字</strong></p>
<p>voliate保证可见性，不保证原子性。</p>
<p><a href="https://www.cnblogs.com/null-qige/p/9481900.html">JMM原理</a></p>
<ol start="3">
<li>
<p>它该如何学习？</p>
<p>JMM：<strong>抽象的概念</strong>，理论</p>
<p><strong>voliate</strong>等等</p>
</li>
</ol>
<p>最后：<mark>拥抱开源时代，享受技术给我们带来的新体验</mark></p>
<h2 id="常问题">常问题</h2>
<ul>
<li>请你谈谈你对JVM的理解？java8虚拟机和之前的变化更新？</li>
<li>什么是OOM，什么是栈溢出Stack OverflowError？怎么分析？</li>
<li>JVM的常用调优参数有哪些？</li>
<li>内存快照如何抓取，怎么分析Dump文件？</li>
<li>谈谈JVM中，类加载器你的认识？</li>
<li>说一下java对象创建的过程</li>
</ul>
<h2 id="补充">补充</h2>
<h3 id="介绍下-java-内存区域运行时数据区">介绍下 Java 内存区域(运⾏时数据区)</h3>
<p>Java 虚拟机在执⾏ Java 程序的过程中会把它管理的内存划分成若⼲个不同的数据区域。JDK.<br>
1.8 和之前的版本略有不同，下⾯会介绍到。</p>
<p>JDK 1.8 之前：</p>
<figure data-type="image" tabindex="35"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210311125510607.png" alt="image-20210311125510607" loading="lazy"></figure>
<p><strong>JDK 1.8</strong></p>
<figure data-type="image" tabindex="36"><img src="C:%5CUsers%5Clin%5CAppData%5CRoaming%5CTypora%5Ctypora-user-images%5Cimage-20210311125530901.png" alt="image-20210311125530901" loading="lazy"></figure>
<p>从这里可以看出 方法区移出去了 就不在运行时的数据区域当中</p>
<p>移到了元空间当中</p>
<h3 id="程序计数器">程序计数器</h3>
<p>程序计数器是⼀块᫾⼩的内存空间，可以看作是当前线程所执⾏的字节码的⾏号指示器。字节码解释器⼯作时通过改变这个计数器的值来选取下⼀条需要执⾏的字节码指令，分⽀、循环、跳转、异常处理、线程恢复等功能都需要依赖这个计数器来完成。</p>
<p>另外，为了线程切换后能恢复到正确的执⾏位置，每条线程都需要有⼀个独⽴的程序计数器，各<br>
线程之间计数器互不影响，独⽴存储，我们称这类内存区域为“线程私有”的内存。</p>
<p>从上⾯的介绍中我们知道程序计数器主要有两个作⽤：</p>
<ol>
<li>字节码解释器通过改变程序计数器来依次读取指令，从⽽实现代码的流程控制，如：顺序执⾏、选择、循环、异常处理。</li>
<li>在多线程的情况下，程序计数器⽤于记录当前线程执⾏的位置，从⽽当线程被切换回来的时<br>
候能够知道该线程上次运⾏到哪⼉了。</li>
</ol>
<p>注意：程序计数器是<strong>唯⼀⼀个不会出现 OutOfMemoryError 的内存区域</strong>，它的⽣命周期随着线程的创建⽽创建，随着线程的结束⽽死亡。</p>
<h3 id="java-虚拟机栈">Java 虚拟机栈</h3>
<p>与程序计数器⼀样，Java 虚拟机栈也是线程私有的，它的⽣命周期和线程相同，描述的是 Java⽅法执⾏的内存模型，每次⽅法调⽤的数据都是通过栈传递的。</p>
<p>Java 内存可以粗糙的区分为堆内存（Heap）和栈内存 (Stack),其中栈就是现在说的虚拟机栈，<br>
或者说是虚拟机栈中局部变量表部分。 （实际上，Java 虚拟机栈是由⼀个个栈帧组成，⽽每个栈帧中都拥有：局部变量表、操作数栈、动态链接、⽅法出⼝信息。）</p>
<p>局部变量表主要存放了编译期可知的各种数据类型（boolean、byte、char、short、int、float、<br>
long、double）、对象引⽤（reference 类型，它不同于对象本身，可能是⼀个指向对象起始地址<br>
的引⽤指针，也可能是指向⼀个代表对象的句柄或其他与此对象相关的位置）。</p>
<p>Java 虚拟机栈会出现两种错误： StackOverFlowError 和 OutOfMemoryError 。</p>
<p>​	StackOverFlowError ： 若 Java 虚拟机栈的内存⼤⼩不允许动态扩展，那么当线程请求栈的深度超过当前 Java 虚拟机栈的最⼤深度的时候，就抛出 StackOverFlowError 错误。<br>
​	OutOfMemoryError ： 若 Java 虚拟机堆中没有空闲内存，并且垃圾回收器也⽆法提供更多内存的话。就会抛出 OutOfMemoryError 错误。</p>
<p>Java 虚拟机栈也是线程私有的，每个线程都有各⾃的 Java 虚拟机栈，⽽且随着线程的创建⽽创建，随着线程的死亡⽽死亡。</p>
<p>扩展：那么⽅法/函数如何调⽤？<br>
Java 栈可⽤类⽐数据结构中栈，Java 栈中保存的主要内容是栈帧，每⼀次函数调⽤会有⼀个对应的栈帧被压⼊ Java 栈，每⼀个函数调⽤结束后，都会有⼀个栈帧被弹出。</p>
<p><strong>Java ⽅法有两种返回⽅式：</strong></p>
<ol>
<li>return 语句。</li>
<li>抛出异常。</li>
</ol>
<p><strong>不管哪种返回⽅式都会导致栈帧被弹出</strong></p>
<h3 id="本地方法栈">本地⽅法栈</h3>
<p>和虚拟机栈所发挥的作⽤⾮常相似，区别是： 虚拟机栈为虚拟机执⾏ Java ⽅法 （也就是字节码）服务，⽽本地⽅法栈则为虚拟机使⽤到的 <strong>Native</strong> ⽅法服务。 HotSpot 虚拟机中和 Java虚拟机栈合⼆为⼀。</p>
<p>本地⽅法被执⾏的时候，在本地⽅法栈也会创建⼀个栈帧，⽤于存放该本地⽅法的局部变量表、操作数栈、动态链接、出⼝信息。</p>
<p>⽅法执⾏完毕后相应的栈帧也会出栈并释放内存空间，也会出现 StackOverFlowError 和OutOfMemoryError 两种错误。</p>
<h3 id="说一下java对象创建的过程">说一下java对象创建的过程</h3>
<p><strong>Step1 类加载检查</strong></p>
<p>当虚拟机收到new对象的命令的时候，首先要做的事情就是看一下能否在常量池中定位到这个类符号引用，</p>
<p>并且检查这个符号引用代表的类是否已经被加载过，解析和初始化过。如果没有话 就必须先执行相应的类加载过程。</p>
<p><strong>Step2分配内存</strong></p>
<p>在类加检查通过的时候，接下来就要对新生对象分配内存。对象所需的内存大小在类加载完成后就可以确定，味道</p>
<p>为对象；分配空间的任务就等相当于把一块确定大小的内存从java堆中划分出来。</p>
<p>然后分配内存的方式又分为两种 指针碰撞 和空闲列表</p>
<p><strong>内存分配并发问题（补充内容，需要掌握）</strong><br>
在创建对象的时候有⼀个很重要的问题，就是线程安全，因为在实际开发过程中，创建对象是很<br>
频繁的事情，作为虚拟机来说，必须要保证线程是安全的，通常来讲，虚拟机采⽤两种⽅式来保<br>
证线程安全：</p>
<p>CAS+失败重试： CAS 是乐观锁的⼀种实现⽅式。所谓乐观锁就是，每次不加锁⽽是假设<br>
没有冲突⽽去完成某项操作，如果因为冲突失败就重试，直到成功为⽌。虚拟机采⽤ CAS<br>
配上失败重试的⽅式保证更新操作的原⼦性。</p>
<p>TLAB： 为每⼀个线程预先在 Eden 区分配⼀块⼉内存，JVM 在给线程中的对象分配内存<br>
时，⾸先在 TLAB 分配，当对象⼤于 TLAB 中的剩余内存或 TLAB 的内存已⽤尽时，再采⽤<br>
上述的 CAS 进⾏内存分配</p>
<p>Step3：初始化零值</p>
<p>内存分配完成后，虚拟机需要将分配到的内存空间都初始化为零值(不包括对象头) ，这一步骤</p>
<p>保证了对象的实例字段在啊java代中可以不赋值就直接使用，程序就可以访问到这些字段的数据类型所对应的零值。</p>
<p>Step4：设置对象头</p>
<p>初始化零值完成之后，虚拟机要对对象进行必要的设置，例如这个对象是哪个类的实例、如何才能找到类的元数据信息，对象的哈希码，对象的GC分代年龄等信息。这些信息存放在对象头中，另外，根据虚拟机当前运行状态的不同，如是否启用偏向锁等，对象头会有不同的设置⽅式。</p>
<p>Step5：执行Init方法</p>
<p>在上面那些步骤都完成的时候 好像一个对象创建的过程就完成了 但是实际上还没有</p>
<p>因为对象创建才刚开始，inti方法还没有执行 ，所有的字段都还为0 。所以一般来说 ，执行new指令之后会接着执行init方法 ，把对象按照程序的意愿进行初始化，这样一个真正的对象才算完成产生出来。</p>
]]></content>
    </entry>
</feed>